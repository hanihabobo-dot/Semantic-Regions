================================================================================
CODEBASE AUDIT — OPEN ISSUES
================================================================================
Date: 2026-02-25 (original audit, merged from 3 audit passes)
Restructured: 2026-03-24 — open issues renumbered in priority order.
Slimmed:      2026-04-24 — moved older RESOLVED inline entries (#3, #24, #25,
              #26, #27, #28, #35) and the pre-2026-04-22 changelog journal to
              archive/CODEBASE_AUDIT_RESOLVED.txt.
Slimmed:      2026-04-27 — moved #29, #33, #39, #43, #44 (fully DONE) and the
              #30 implementation debrief to archive/CODEBASE_AUDIT_RESOLVED.txt;
              moved #37, #38 (low-priority deferred) to
              archive/CODEBASE_AUDIT_DEFERRED.txt.  Collapsed the old
              "META-ISSUE DISSOLUTION" block (old #95) into a one-line note
              in the policy section — all four drift channels now live as
              individual resolved/open entries.  Merged two overlapping
              boxel_fits / static-init policy blocks.
Slimmed:      2026-05-03 — moved #45, #46 (fully DONE) to
              archive/CODEBASE_AUDIT_RESOLVED.txt.
Added:        2026-05-03 — CODEBASE POLICY: always push to both remotes
              (`github` = GitHub, `origin` = RWTH GitLab).
Restructured: 2026-05-03 — roadmap (pre-eval / eval); archive #4; drop #6;
              mistakenly dropped #41 from open list (later corrected);
              defer #42 collisions; refresh FOR LATER.
Corrected:    #41 — back in open audit (was dropped in error in 7003dca;
              user had asked for open/closed status only, not closure).
Slimmed:      2026-05-05 — moved #21 to archive/CODEBASE_AUDIT_RESOLVED.txt
              (log-only band-aid landed: print messages distinguish
              observed-empty shadows from blocked-unresolved 3-strike
              give-ups; behavior unchanged); added #47 to the open list
              (real fix — re-ground planner blocker atoms after repeated
              still_blocked).
Updated:      2026-05-05 — #40 marked [DONE] (per-action _verify_cube_on
              gate on the on_relations write + end-of-run
              _verify_goal_physics walk over the goal AST; both feed
              timing_summary.json via physical_failures_per_action /
              physical_failures_at_goal); added #48 (pick from a stack
              does not clear on_relations[X] — write-side counterpart
              to #40); added FOR LATER bullet for safe-inset XY check.
Updated:      2026-05-06 — supervisor sync.  Added #49 (find-hidden-then-
              stack-on-tray goal — new evaluation goal mode requested by
              user/supervisor: discover targets via sense and stack them
              on a designated tray entity); added #50 (planner perf
              investigation — find and remove big redundancies in
              pddlstream_planner / streams; user-flagged "planner is
              too slow").  Updated #36: recommended fix is now a
              hardcoded small post-action lift in execute_place /
              execute_stack (primary — plan_motion has to start clear
              of the freshly placed/stacked cube; pick lift is cosmetic
              only).  Supervisor: "this is not cheating, it's a
              workaround for shotty motion planning."  The prior
              at_config-q_home goal-clause approach is dropped.  Marked #30 [WONTFIX
              2026-05-06] — supervisor scrapped the split-pick / strip-
              stack-action mitigation experiment as "not interesting or
              relevant for the thesis"; the ~2× holding-goal slowdown
              is accepted as the cost of the stack feature.  Confirmed
              archive-deferred #42 stance (no replan on collisions —
              "no need to track knocked-down objects").  Confirmed #10
              (uniform-vox baseline) and #12 (scalability eval) remain
              in scope.  Added LW1 / TAMPURA empirical comparison to
              FOR LATER (post-thesis-writing, conditional on ≥1 free
              week).
Updated:      2026-05-06 (later) — triage pass.  Marked #47 [DEFERRED
              OUT OF SCOPE] per user decision: the archived #21
              log-only band-aid is the accepted level of correctness
              for the thesis; eval (#9–#12) can filter "gave up
              blocked" cases as failures.  Open count: 14 → 13.
              TIER 0 is now empty.  THESIS_NOTES §18 updated to
              note the deferral.
Updated:      2026-05-06 (triage) — promoted #41 from TIER 3 to
              TIER 1 per user decision ("make top prio").  Reordered
              pre-evaluation roadmap to put #41 (on_table) first.
              Reasoning: the (on a b) chain alone admits floating-
              tower interpretations the #40 verifier cannot rule out
              without an on_table clause, and #49 (tray goal) reuses
              the same predicate plumbing — fixing the goal language
              once before either lands.
Updated:      2026-05-06 (triage) — merged #32 (comment/code drift
              sweep) into #31 per user decision; promoted the merged
              issue to TIER 1 and put it FIRST in the pre-evaluation
              roadmap so the comment-quality category is "knocked
              out and not thought about" before the substantive
              pre-eval work begins.  #32 now [MERGED INTO #31] —
              moves to RESOLVED on next slim.  Open count: 13 → 12.
Updated:      2026-05-06 (triage) — killed #34 [WONTFIX] per user
              decision: redundant with #50 (planner perf
              investigation).  #34's own fix instructions already
              conditioned on "if #12 reveals this as a bottleneck,"
              which is exactly #50's profile-then-fix flow.  If
              profiling shows _compute_placement_view_blocks as a
              real bottleneck, the fix lands in #50 with measurements
              rather than as a speculative one-off.  Open count:
              12 → 11.
Updated:      2026-05-06 (triage) — promoted #48 from TIER 3 to
              TIER 1 per user decision; bundled with #41 in the
              stack-language correctness pass.  Reasoning: #49
              (tray goal) makes pick→place(tray) plan shapes normal,
              and any target starting on top of another cube triggers
              the stale-(on X Y) gap.  Fix is one line; landing it
              alongside #41's on_table clause is a single
              symbolic-plumbing commit.
Updated:      2026-05-06 (triage) — killed #11 [WONTFIX] per user
              decision: the semantic-vs-uniform comparison via #10
              alone carries the thesis claim that semantic boxels do
              real work.  A third "semantic-but-static" baseline
              would add 3–5 days for marginal narrative gain.  The
              tray entity in #49 already exemplifies a fixed
              labelled region, so extending to a 4×4 fixed-regions
              sweep can be cited as straightforward future work
              without running it.  Open count: 11 → 10.
Updated:      2026-05-06 (triage) — merged #12 (scalability analysis)
              into #9 per user decision.  Scope (n_occluders ×
              n_targets matrix, scalability plots) folded into #9's
              "remaining work" so there's a single eval-implementation
              milestone instead of two.  Open count: 10 → 9.
Updated:      2026-05-06 (triage) — killed #13 [WONTFIX] per user
              decision: recursive free-space discovery is dead code
              under this thesis's fixed-camera + no-concave-geometry
              setup; would only matter for partial-coverage sensors
              or objects hidden inside containers.  Already documented
              as accepted simplification in THESIS_NOTES §20.  Open
              count: 9 → 8.  Triage pass complete.
Slimmed:      2026-05-06 — moved 7 entries to archives:
              archive/CODEBASE_AUDIT_RESOLVED.txt: #11 (WONTFIX),
              #12 (MERGED INTO #9), #13 (WONTFIX), #30 (WONTFIX),
              #32 (MERGED INTO #31), #34 (WONTFIX), #40 (DONE
              2026-05-05).
              archive/CODEBASE_AUDIT_DEFERRED.txt: #47 (DEFERRED
              OUT OF SCOPE).
              Body of each closed issue preserved verbatim under a
              "2026-05-06 SLIM ADDITIONS" section in the archive.
              Open audit shrunk to 8 active items.
Added:        2026-05-07 — #51 (stale shadow blocker fallback after
              occluder relocation; user-discovered runtime bug —
              compute_shadow_blockers parent-relationship fallback
              wrongly force-adds creator after creator has moved
              out of corridor; visible shadows symbolically reported
              as view_blocked; sense action precondition fails).
              Open count: 8 -> 9.
Updated:      2026-05-07 (later) — #31 marked [DONE 2026-05-07].
              Comment-quality sweep across 9 files completed (17 drift
              edits, no behavioral change, py_compile clean).  Scope
              narrowed mid-task per user direction: pure narration
              kept (NOT deleted as the original rubric prescribed);
              only comments that were actively wrong / outdated were
              touched.  Common drift categories: stale "push" wording
              from #53 push -> pick/place transition; "tracked as
              audit #47" framings (deferred 2026-05-06); "audit #30
              keep/kill" parentheticals (decided WONTFIX 2026-05-06);
              missing newer streams in module/class docstrings.
              Open count: 9 -> 8.
Updated:      2026-05-07 (later) — Marked #51 [DEFERRED 2026-05-07]
              per user decision.  A fix attempt was committed to main
              (compute_shadow_blockers hit_pos filter on the primary
              5x5 ray batch + slab-method outside-shadow gate on the
              parent-relationship fallback, regression tests in
              tests/test_audit_51_blocker.py covering both bug paths
              and the geometry-skew regression) and then reverted.
              Work preserved on branch audit-51-shadow-blocker-fix
              (pushed to both github and origin) for later revisit.
              Open count: 8 -> 7.  TIER 0 again empty.
Slimmed:      2026-05-07 (same day) — moved #51 (DEFERRED 2026-05-07)
              to archive/CODEBASE_AUDIT_DEFERRED.txt.  Body preserved
              verbatim with Source / Archived headers per existing
              archive convention.  Fix attempt remains reachable via
              branch audit-51-shadow-blocker-fix on both github and
              origin.
Updated:      2026-05-07 (later) — #41 marked [DONE 2026-05-07].
              Added (on_table ?o) predicate to the domain (pick removes,
              place emits; stack untouched — held cube was already off-
              table); _build_init emits (on_table X) for every cube not
              stacked; build_stack_goal appends (on_table base) so the
              tower base is grounded in the goal AST; goal_satisfied
              and the #40 physics verifier (_verify_on_table) handle
              the new head.  py_compile clean across the three modules.
              Open count: 7 -> 6.  TIER 1 reduces to #48 alone.
Updated:      2026-05-07 (later) — #48 marked [DONE 2026-05-07].
              on_relations.pop(obj_str, None) in the pick handler after
              the PICKED UP log line; mirrors the PDDL pick conditional
              effect that clears (on ?o ?x).  py_compile clean.
              Open count: 6 -> 5.  TIER 1 now empty.
Updated:      2026-05-07 (later) — #36 marked [DONE 2026-05-07].
              New _apply_post_action_lift helper in execution.py:
              Cartesian +Z lift seeded from the contact joints, fail-safe
              fall-through on IK failure.  10 cm in execute_place and
              execute_stack (gives the next plan_motion safe headroom
              over the freshly placed/stacked cube), 5 cm cosmetic in
              execute_pick.  actual_joints read AFTER the lift so
              final_config carries the lifted pose forward as the next
              move's plan_motion seed.  Docstrings updated to point at
              THESIS_NOTES §19.  py_compile clean.  Open count: 5 -> 4.
Updated:      2026-05-07 (later) — #49 commit 1/4 landed (tray entity
              in scene).  ObjectInfo.is_tray flag, SceneConfig.enable_tray
              flag (default False), BoxelTestEnv._create_tray() loads
              pybullet_data tray/traybox.urdf via _mirror_load_urdf
              (mass=0, globalScaling=0.4) at (-0.130, 0.380) — back-left
              tray corner exactly on the back-left table corner.  Tray
              footprint reserved by _create_occluders / _create_targets
              for random-placement scenes.  generate_boxels skips
              is_tray=True as a shadow CASTER so no shadow_of_tray boxel
              or blocks_view_at facts emit.  Visual verification done
              against default_scene with a temporary enable_tray=True
              toggle (reverted before commit).  py_compile clean.
              #49 still open — commits 2-4 (PDDL is_tray gate, --goal
              find-and-tray-stack, polish/scalability sweep) remain.
Updated:      2026-05-07 (later) — #49 commit 2/4 landed (PDDL is_tray
              gate).  Added (is_tray ?o) predicate to
              pddl/domain_pddlstream.pddl alongside the existing stacking
              predicates; no action body changes.  PDDLStreamPlanner
              constructor takes a new tray_name kwarg (default None);
              _build_init emits (Obj tray) + (is_tray tray) when set,
              and the existing all_obj_ids loop emits (clear tray) +
              (on_table tray) automatically.  test_full_pipeline.py
              detects the tray by scanning env.objects for
              ObjectInfo.is_tray and passes tray_name=... to the
              planner; backward compatible (tray_name=None when
              enable_tray=False default).  pick is implicitly unpickable
              on tray since no (obj_at_boxel tray ?b) is emitted; place
              targets free-space boxels (tray has none); stack accepts
              tray as ?on_obj once (Obj tray) + (clear tray) are in
              init.  py_compile clean across pddlstream_planner.py and
              test_full_pipeline.py.  #49 still open — commits 3-4
              (streams placement on tray top + stack_kin for tray;
              --goal find-and-tray-stack + verifier extension) remain.
Updated:      2026-05-07 (later) — #49 commit 3/4 landed (Boxel tray
              init fact for move-to-tray plannability).  Triage of the
              existing pipeline showed that compute_stack_kin_solution
              already handles the tray as a generic Obj (resolves the
              tray body via object_body_ids, reads its AABB, drops the
              held cube at tray_center_xy + wall_top + cube_half_height;
              physics settles the cube into the tray cavity).  The real
              gap was symbolic: commit 2/4 emitted (Obj tray) +
              (is_tray tray) but NOT (Boxel tray); since
              compute-stack-kin certifies (config_for_boxel ?q tray),
              the move action's (Boxel ?b) precondition would have
              failed when delivering the arm to the stack-on-tray pose.
              One-line fix in pddlstream_planner._build_init: when
              tray_name is set, also append ('Boxel', tray_name).
              Multi-xy sampling within the tray footprint deferred —
              not required for the goal AST (on t_1 tray) (on t_2 t_1)
              ... which always uses centered placement on tray (and
              cube-on-cube for the rest of the tower).  py_compile
              clean.  #49 still open — commit 4 (--goal find-and-
              tray-stack + verifier extension + smoke tests) remains.
Updated:      2026-05-07 (later) — #49 commit 4/4 landed (--goal find-
              and-tray-stack + verifier extension).  CLI: 'find-and-
              tray-stack' added to --goal choices in run_logger.py;
              auto-enables the tray entity in test_full_pipeline.py
              __main__ regardless of selected scene; --n-hidden > 0
              now allowed with this goal (sense-side use case
              symmetric with holding).  Goal builder:
              build_tray_stack_goal(targets, tray_name) produces (and
              (on c_0 tray) (on c_1 c_0) ... (on c_{H-1} c_{H-2})) —
              base-up convention inverted from build_stack_goal
              because the tray plays the role of the table-anchor (no
              on_table tray clause emitted; the planner-side init fact
              already covers it).  main() branch: discovers env tray
              via ObjectInfo.is_tray, sets visible_target_locations
              only for VISIBLE targets (hidden ones must be sensed),
              generalises the 'stack'-only plumbing
              (planner_target_objects, on_relations /
              stackable_objects into planner.plan(), per-iteration
              banner) to include find-and-tray-stack; max_replans =
              4*shadows + 2*targets + 3.  Verifier: _verify_cube_on
              now branches on support_info.is_tray — cube settles
              INSIDE the cavity (not on the wall-top), so bottom-z
              match becomes a z-range check; XY centre check
              unchanged; cube-on-cube path bit-identical to pre-#49.
              py_compile clean; build_tray_stack_goal smoke-tested
              standalone (1/2/3-target shapes + empty-list ValueError).
              Manual end-to-end smoke tests on default and scalability
              scenes (audit step f) deferred to next WSL session.
              #49 [DONE 2026-05-07].  Open count: 4 -> 3.  TIER 2
              reduces to #50 alone.
Updated:      2026-05-07 (later) — #49 hotfix: exclude is_tray=True
              from all_targets in test_full_pipeline.py main().  First
              WSL run of --goal find-and-tray-stack on default_scene
              produced (and (on cyan tray) (on blue cyan) (on yellow
              blue) (on tray yellow) (on purple tray)) — the (on tray
              yellow) clause is geometric nonsense (the tray is fixed-
              base) and the planner returned no plan after 46s.  Root
              cause: the all_targets filter dropped is_occluder /
              plane / table / robot but NOT is_tray, so the tray
              leaked into stack_target_objects and the build_tray_-
              stack_goal shuffle landed it mid-chain.  One-line fix
              at the source ("not info.is_tray" added to the filter)
              — no behaviour change on enable_tray=False runs (the
              tray never spawns), and removes the same latent bug
              from the holding / stack branches if either is run
              with enable_tray=True manually.  py_compile clean.
              #49 stays [DONE 2026-05-07] — hotfix is a follow-up
              within the same audit, not a new audit item.
Added:        2026-05-07 (later) — #52 (find-one-hidden-target-stack-
              on-big-visible-base — new TIER 0 demo goal mode
              requested by user post-#49 hotfix).  Last WSL run of
              --goal find-and-tray-stack still failed after commit
              62d435b; root-cause analysis flagged size-compat gap
              (no big-on-small check anywhere — confirmed by
              inspection of domain_pddlstream.pddl :action stack
              and streams.compute_stack_kin_solution) and goal-AST
              length scaling with --n-targets.  User proposal:
              simplest possible end-to-end goal = one big visible
              base + one small hidden target stacked on top.
              Replaces find-and-tray-stack as the demo default;
              tray goal stays available.  New FOR LATER bullet
              records the size-compat gap as a parallel concern.
              Open count: 3 -> 4.
Slimmed:      2026-05-07 (later) — moved #31, #36, #41, #48, #49
              (all DONE 2026-05-07) to archive/CODEBASE_AUDIT_-
              RESOLVED.txt.  Bodies preserved verbatim under a new
              "2026-05-07 SLIM ADDITIONS" section at the end of the
              archive; #49 entry includes a 4-commit + hotfix recap.
              Open audit shrunk from ~395 lines of issue bodies to
              the four still-active items (#9, #10, #50, #52).
              No code changes.
Added:        2026-05-07 (later) — #53 (TIER 0 diagnostic — trim
              --goal find-and-tray-stack to 1 target temporarily so
              we can tell whether the planner failure is goal-AST
              length, size-compat, or something else.  Gates #52:
              if the 1-cube run succeeds, #52 is "nicer demo" not
              "only path to passing"; if it also fails, the size-
              compat FOR LATER bullet promotes to fix-this status).
              Open count: 4 -> 5.
Updated:      2026-05-07 (later) — #53 marked [DONE 2026-05-07].
              Diagnostic slice landed in test_full_pipeline.py
              find-and-tray-stack branch (commit 4d7fa37); WSL run
              logs/run_2026-05-07_21-26-18 read off the triage
              signal: 1-CUBE TRAY-STACK ALSO FAILS.  Slice fired
              correctly — chose hidden target cyan_object, goal AST
              collapsed to ('and', ('on', 'cyan_object', 'tray'))
              (the smallest possible shape).  Planner returned NO
              PLAN after a single 45.87 s attempt; exit_reason
              "planner_failed", plan_count 1.  Implications for the
              gate logic in #53's "Outcomes inform the next steps"
              block:
                - Hypothesis (a) goal-AST length: REJECTED.  The
                  1-clause goal is the floor; cap-N on
                  build_tray_stack_goal is NOT the fix.
                - Hypothesis (b) size-compat: NOT the diagnosis
                  either, at least not on its own — the 1-cube tray
                  goal never invokes cube-on-cube stacking (the cube
                  drops into the tray cavity per
                  compute_stack_kin_solution + #49 commit 3/4), so
                  the big-on-small footprint check would fire on
                  zero stacking operations.  The size-compat FOR
                  LATER bullet does NOT auto-promote.
                - The "or another deeper issue" branch is the live
                  one.  #52's full big-base/small-target redesign
                  proceeds as the recommended path; understanding
                  WHY the 1-cube tray goal failed (sense
                  precondition?  stack-into-cavity stream sampling?
                  single (Boxel tray) pose vs. the move action?)
                  is a separate sub-investigation that can fold
                  into #50 (planner perf / hotspot hunt) or stay
                  out of scope if #52 succeeds without it.
              No domain / verifier changes from the slice itself.
              Slice itself is "intentionally temporary" per the
              issue body — left in place pending #52 lands and the
              tray goal's role is settled (revert, --tray-stack-cap
              N flag, or audit-history note).  py_compile clean.
              Open count: 5 -> 4.
Added:        2026-05-07 (later) — #54 (TIER 0 follow-up triage —
              force VISIBLE target on --goal find-and-tray-stack to
              isolate sense from pick/stack/streams.  #53 ruled out
              goal-AST length and size-compat as causes; the "deeper
              issue" branch has three live candidates — sense-side
              (stale view_blocked / blocker re-grounding), pick/
              stack-into-cavity sampling, and single (Boxel tray)
              move plannability.  Visible-only run drops sense from
              the AST, separating sense-side from {pick, stack,
              streams}).  Slice landed in commit 826bb21
              (test_full_pipeline.py find-and-tray-stack branch);
              supersedes #53's hidden-preferred slice.  py_compile
              clean.  Open count: 4 -> 5.
Updated:      2026-05-07 (later) — #54 marked [DONE 2026-05-07].
              Visible-only triage run (logs/run_2026-05-07_21-48-02)
              succeeded in 5.19 s (1 plan, success: true, 0 physical
              failures, full pipeline ran end-to-end).  Diagnosis:
              sense is NOT broken, but it is the dominant search-cost
              driver for this goal — the prior 1-cube hidden run
              (#53, logs/run_2026-05-07_21-26-18) gave up at 45.87 s
              on adaptive-search exhaustion UNDER the 120 s max_time
              cap, meaning the planner ran out of skeletons / stream
              samples before time expired.  Slice reverted to the
              original #49 multi-cube behaviour (commit b287450); the
              planner.plan() max_time cap bumped 120 -> 1800 s in
              the same commit so the adaptive sampler has more rope
              for the multi-cube hidden+visible mix.  py_compile
              clean.  Next: WSL run on default scene with the
              reverted goal + uncapped time to confirm whether
              sense-heavy multi-cube tray-stack converges with more
              budget or fails on a deeper bottleneck.  Open count:
              5 -> 4.
Added:        2026-05-07 (later) — #55 (TIER 0 — find-and-tray-stack
              redesigned as 2-cube goal: 1 VISIBLE cube on tray + 1
              HIDDEN cube stacked on top.  Smallest goal that
              exercises sense → pick → stack end-to-end.  User
              direction post-#54: "place a big visible object on
              tray and then find a small hidden object and place it
              on top of the object on top of tray. just that."
              Replaces #49's multi-cube definition; build_tray_-
              stack_goal preserved per CODEBASE POLICY but unused
              by current goal.  Commit fa069bd.  Cap inventory at
              landing: max_time=1800 s (#54 follow-up), max_replans
              healthy for 2-cube, ik_max_iterations=100; PDDLStream
              solve internals (max_skeletons / effort_weight /
              max_planner_time) still on defaults — next lever if
              2-cube also fails.  py_compile clean.  Open count:
              4 -> 5.
Updated:      2026-05-08 — #55 Path C explored on branch audit-55-pose-
              aware-stack and DEFERRED in favour of a simpler lazy-
              collision hack on main (commit 9a64aa1).  Branch
              convention shift: this is the first multi-commit refactor
              to follow the new "branch first" rule.  Branch pushed to
              both github and origin for later revisit (audit-51
              precedent).

              Diagnosis from logs/run_2026-05-07_22-07-31 (commit
              fa069bd on main): the 9-action skeleton found at iter 4
              didn't ground because plan_motion couldn't connect from
              above-cyan-shadow to "above yellow's initial pose" (the
              IK target compute_stack_kin_solution derived via getAABB
              on yellow's static planning-time body).  yellow at its
              planning-time location blocks the RRT path; the
              trajectory is rejected.  This is the SUPPORT analogue of
              the moved-occluder gap that compute_kin_solution
              sidesteps via lazy collision-check + ignored_body_ids
              (its docstring at L843-851 explicitly calls out this
              pattern).  The analog for stack was simply missing.

              Path C exploration (6 commits on branch, in order):
                9eca2a1 — predicates (Pose, at_pose) + SymbolicPose
                          dataclass + initial-pose facts in
                          _build_init.  No-op groundwork.
                ed7ecff — audit changelog for commit 1.
                81c0405 — pose-aware compute-stack-kin (?p_on, ?p_new
                          inputs/outputs) + stack action signature
                          updated; at_pose updated by stack effect via
                          forall-when delete + add.  Real fix layer.
                8b6b68d — stack_kin predicate declaration arity 4->6
                          (FastDownward parser hotfix from WSL run
                          #1).
                5c10e36 — tighten compute-stack-kin :domain to
                          (at_pose ?on_obj ?p_on) only (kill
                          combinatorial blowup from WSL run #2 — 10-
                          min hang, ~750+ IK calls per round).  Over-
                          corrected.
                d5c2a2d — add (Pose ?p_on) back alongside (at_pose)
                          (fluent-stream pattern from WSL run #3
                          KeyError on '?p_on' not covered by static
                          :domain).

              Result (logs/run_2026-05-08_09-05-23, WSL run #4):
              skeleton found at iteration 5 / complexity 4 / attempt 2
              in 56.6 s, with the second stack correctly chaining
              ?p_on = ?p_new of the first stack — Path C functionally
              correct.  Stream sample-validation phase started; user
              killed at 10 min, ~3 min into iteration 6.  The 7x
              skeleton-search slowdown vs baseline comes from
              PDDLStream's exogenous-axiom compilation
              (compile_to_exogenous_axioms in
              pddlstream_lib/.../exogenous.py), triggered whenever a
              stream :domain references a fluent that is also an
              action effect.  Cost is structural — derived predicates
              and added axioms inflate the grounded planning problem.
              Acceptable for a one-shot demo, too slow for the eval
              sweep (#9).

              Decision: defer Path C, apply the simpler lazy-collision
              hack on main as commit 9a64aa1 — a one-place edit in
              streams.compute_stack_kin_solution that adds the support
              cube's body to ignored_body_ids alongside the held cube.
              This is the direct analog of compute_kin_solution's
              pattern; should have been applied from the start.  IK
              target stays "above support's planning-time pose" (the
              pre-#55 wrong-pose behaviour); execute_stack at runtime
              reads the support's LIVE AABB and re-solves IK seeded
              with the planner's config, so the wrong-pose IK becomes
              correct at execution.  plan_motion ignores the support
              body throughout the trajectory (per
              RobotConfig.ignored_body_ids), so RRT can route through
              where the support statically sits — no more "trajectory
              blocked by support's static body" rejection.

              New FOR LATER bullet records the residual edge case:
              the planned trajectory may pass through the support's
              RUNTIME location, so the arm could collide at execution
              if the support's destination happens to lie on the path.
              For find-and-tray-stack on default scene the destination
              (back-left tray corner) is unlikely to intersect a
              typical arm route between two table-spread cubes;
              flagged for revisit if any goal mode lands the support
              somewhere a planner trajectory naturally traverses.
              #55 still open pending WSL retry on main.
Updated:      2026-05-08 (later) — #55 marked [DONE 2026-05-08].  Real
              root cause was DIFFERENT from the prompt's diagnosis: it
              was the held-cube boxel guard, not the support's pose.
              compute_stack_kin_solution at streams.py:962-965 returned
              early when held_boxel was None, which is the case for
              hidden targets — they have no OBJECT-type registry
              boxel until sense reveals them.  The on_obj pose
              "wrongness" Path C tried to fix was a downstream symptom
              that never actually got hit because the function returned
              at the held-boxel check first.  Confirmed via diff
              between logs/run_2026-05-08_10-20-03 (failure with the
              lazy-collision hack only — no compute_stack_kin debug
              entries past sample-grasp + compute-kin, indicating early
              return before the IK loop) and the Path C run #4
              (logs/run_2026-05-08_09-05-23, where future-compute-
              stack-kin placeholders masked the gap during skeleton
              search — sample validation would have hit the same held-
              boxel check eventually).

              Fix landed on main as commit 8281d73: PyBullet AABB
              fallback for held cube half-height when no registry
              boxel exists.  The hidden cube's body IS mirrored in
              plan_client via _mirror_load_urdf (independent of sense),
              so getAABB returns its actual extent.  Half-height for
              IK = (h_max[2] - h_min[2]) / 2.  Cube z-extent doesn't
              depend on knowing where it is, so reading it pre-sense
              doesn't leak position info into init facts.

              WSL verification (logs/run_2026-05-08_10-49-01) on
              default scene with --goal find-and-tray-stack:
              success=true, plan_count=4, total_planning_time=76.593 s
              (per-call: 9.129 / 13.528 / 27.757 / 26.180 s),
              physical_failures_per_action=0,
              physical_failures_at_goal=0.  Goal AST satisfied:
              (on yellow_object tray) + (on cyan_object yellow_object).
              No regression on --goal holding (logs/run_2026-05-08_-
              10-46-40: success in 3 plans / 58.4 s).

              #55 [DONE 2026-05-08].  Open count: 5 -> 4.  TIER 0
              now contains only #52 (find-and-stack-on-base) which
              stays as a backup demo; the find-and-tray-stack 2-cube
              minimum demonstrates sense -> pick -> stack end-to-end
              and is the cleaner thesis story.  Path C (branch
              audit-55-pose-aware-stack) remains archived on both
              remotes for revisit if a future goal mode requires
              precise pre-execution arm-routing through clutter.  The
              lazy-collision hack (commit 9a64aa1) + the held-boxel
              fallback (commit 8281d73) together close all known #55
              gaps; the trajectory-clipping FOR LATER bullet remains
              as the documented residual.
Slimmed:      2026-05-08 — moved #53 (DONE 2026-05-07), #54 (DONE
              2026-05-07), and #55 (DONE 2026-05-08) to
              archive/CODEBASE_AUDIT_RESOLVED.txt under a new
              "2026-05-08 SLIM ADDITIONS" section.  #55's body
              includes a 2-commit (lazy-collision hack +
              held-cube AABB fallback on main) + 6-commit-archived-
              branch (Path C exploration on audit-55-pose-aware-stack)
              recap.  Open audit shrunk to the four still-active
              items (#9, #10, #50, #52).  No code changes.
Added:        2026-05-08 (later) — #56 (CLI flag for post-action
              safe-distance lift; currently hardcoded ~10 cm in
              execute_place / execute_stack and 5 cm in execute_pick
              per audit #36).  Code-quality / eval-knob; affects no
              current functionality but unblocks sensitivity analysis
              and parameterisation in the eval sweep (#9).
Added:        2026-05-08 (later) — #57 (registry should own cube
              geometry instead of compute_stack_kin_solution reading
              p.getAABB on the plan-client).  Architectural cleanup;
              the AABB fallback (commit 8281d73) reads only z-extent
              (size, not position) so it's not an oracle leak in
              practice, but the registry is the proper "what the
              planner knows" boundary.  Replace the PyBullet read
              with a registry.object_geometry table populated at
              scene init.
Added:        2026-05-08 (later) — #58 (preserve grasp across replans;
              currently release_held_object_in_place drops the held
              object before each replan to keep symbolic state
              consistent with planner._build_init's unconditional
              ('handempty',) emission).  User-flagged 2026-05-08
              after observing yellow_object dropped mid-air at the
              PLAN #1 -> PLAN #2 transition in
              logs/run_2026-05-08_10-49-01 when sense returned empty
              post-pick.  Wasteful — the next plan re-picks the
              dropped object, adding ~4 actions per re-grasp cycle.
              Fix: thread held_obj + grasp into planner.plan(), have
              _build_init emit ('holding', ?o) instead of
              ('handempty',) when set.
Killed:       2026-05-08 (later) — #52 (find-and-stack-on-base) dropped
              from open audit per user direction: "scrab 52 ... just
              delete from existance".  NOT marked WONTFIX (no archive
              trace) — issue body, roadmap entry, SUMMARY slot, and
              OPEN ISSUES header reference all removed.  Historical
              changelog entries that reference #52 (2026-05-07
              additions, the #53 / #54 / #55 diagnostic chain) left
              untouched as factual record.  Reasoning: "we already
              have find-and-stack [via #55]; we dont need to make it
              simpler" — scale UP (#59) instead of DOWN.
Added:        2026-05-08 (later) — #59 (scaled-up find-and-stack —
              N hidden targets stacked physically-sensibly with a
              (compat_stack ?o ?on_obj) symbolic predicate gating
              :action stack).  Very low priority per user direction.
              Replaces #52 as the demo-evolution path; absorbs
              option (i) of the FOR LATER size-compat bullet (which
              now back-links here).  Open count net 6 -> 6 (#52
              out, #59 in).
Updated:      2026-05-08 (later) — #56 marked [DONE 2026-05-08].
              Single CLI flag --post-action-lift FLOAT (default 0.10
              m, range [0.0, 0.30]) added in run_logger.py with
              parse-time range validation; threaded through
              test_full_pipeline.main() into execute_place /
              execute_stack, both of which now take post_action_lift
              kwarg defaulting to 0.10 (preserves pre-#56 behaviour
              exactly when the flag is omitted).  execute_pick's
              5 cm cosmetic lift unchanged per #56 spec ("pick stays
              cosmetic at 5 cm unless the user wants three separate
              flags").  run_config dict + timing_summary.json pick
              up the new field automatically for cross-run
              correlation (audit #9 dep).  Commit bf0800c.
              py_compile clean across run_logger.py /
              test_full_pipeline.py / execution.py.  Open count:
              7 -> 6.  TIER 3 reduces to #9, #10, #57.
Reverted:     2026-05-08 (later) — #56 rolled back on main (commit
              ac47aa6 reverts bf0800c) because --post-action-lift had
              no observable effect on robot behaviour at any value
              tested in the validated [0.0, 0.30] range.  Hardcoded
              10 cm restored via _apply_post_action_lift's default
              lift_height kwarg (execution.py:395); 5 cm cosmetic
              pick lift unchanged.  Manual override now requires
              editing the kwarg default or passing an explicit
              lift_height at execution.py:644 / 761.  #56 stays
              [DONE then REVERTED 2026-05-08] in the closed list —
              the plumbing landed correctly, but the underlying lift
              mechanism is broken (see #60).  Re-attempt #56 only
              after #60 lands.
Added:        2026-05-08 (later) — #60 (TIER 0 — post-action lift
              fails to prevent collisions during the next move
              action; root cause behind #56's ineffectiveness).
              User-flagged 2026-05-08 after find-and-place /
              find-and-tray-stack runs collided despite manual lift
              values up to 30 cm.  Symptom: the arm visibly drops
              back toward contact height before the next move's
              traversal phase, then routes through the
              just-placed cube.  Open count: 6 -> 7.  TIER 0
              repopulated (was empty since #52's removal 2026-05-08).
Updated:      2026-05-08 (later) — #58 marked [DONE 2026-05-08].
              Held-object name now threaded through planner.plan() as
              held_obj kwarg (commit 690f86d); _build_init emits
              ('holding', held_obj) instead of ('handempty',) when
              set, sample-grasp re-fires on (Obj ?o) for a fresh
              (Grasp ?g) + (valid_grasp ?o ?g).  Mid-plan-break
              replans now continue from (holding ?o) instead of
              dropping the cube, eliminating the ~4-action re-grasp
              cycle observed in logs/run_2026-05-08_10-49-01 at the
              PLAN #1 -> PLAN #2 transition.  release_held_object_-
              in_place stays as a SAFETY NET: triggered only when
              planner.plan() returns None with held_obj set, falls
              back to release-and-replan from (handempty).
              py_compile clean across pddlstream_planner.py /
              test_full_pipeline.py.  Other init facts
              (obj_at_boxel ?o, on_table ?o) intentionally left
              emitting for held cubes — pick's (handempty)
              precondition blocks any misuse, and the audit's spec
              was minimal-swap.  Companion commit b248c76 raises
              execute_pick post-action lift from 5 cm to 10 cm
              (drops explicit lift_height=0.05 override, uses
              _apply_post_action_lift default) — equalises per-
              action lift while #60 investigation is open.  Open
              count: 7 -> 6.  TIER 2 reduces to #50 alone.
Updated:      2026-05-08 (later) — #60 step (a) landed (commit
              23687af): diagnostic-only logging across three sites,
              no behaviour change.  (1) execution.py
              _apply_post_action_lift — IK-failure branch was silent,
              now prints "[#60-diag] lift IK FAILED..." (candidate
              iii signal); success branch prints target vs actual EE
              z and |joint_diff|.  (2) execution.py execute_place /
              execute_stack (end of body, before final_config return)
              — query the placed/stacked body's pose in BOTH
              env.client_id and env.plan_client_id; print DESYNC
              warning when xyz delta > 1 mm (candidate ii signal —
              sync_to_plan_client only fires once per replan, mid-
              plan placements do not propagate).  (3)
              test_full_pipeline.py move handler — log
              |current_config - traj.waypoints[0]| and warn if
              > 1e-4 rad (candidate i signal — arm starts move
              ABOVE the trajectory's first waypoint).  All three
              gated on signal so clean runs (e.g. holding-only
              goals) print nothing.  py_compile clean.  #60 stays
              OPEN — next commit applies the targeted fix once a
              WSL run of --goal find-and-tray-stack identifies
              which candidate dominates.  Audit's own ranking is
              (i) > (ii) > (iii); #56 revert ("no observable effect
              at any value tested") corroborates that the lifted
              pose is not reaching plan_motion regardless of
              magnitude.  Open count unchanged at 6.
Added:        2026-05-08 (later) — #61 (TIER 3 — smart console filter
              still leaks startup/shutdown garbage and has no thinking-
              animation affordance during long blocking calls).  User-
              flagged 2026-05-08: "the terminal output is still not
              smart enough.  in the begining and the end there still
              garbage that no one cares about and it would be cute to
              have thinking animations or stuff like that so that it
              really looks like claude."  smart_filter.py already
              implements the drop / dedupe / aggregate / transform
              pipeline (verbosity='smart' default since #56-era log
              hygiene work) but two gaps remain: (1) the leak set
              outside the "Phase 1 ... -> SUCCESS / FAIL" envelope is
              not in _DROP_PATTERNS, (2) long planner.plan() / RRT /
              stream-sample calls show nothing on the console — looks
              frozen.  Open count: 6 -> 7.
Updated:      2026-05-08 (later) — #60 marked [DONE 2026-05-08].
              Option B (sync + replan) landed in commit c08dc65 —
              addresses the two firing causes from logs/run_-
              2026-05-08_12-57-42 [#60-diag] data: candidate (i)
              fired 7/7 moves with |current - traj[0]| 0.064–0.498
              rad, candidate (ii) fired 3/3 place/stack with cube
              pose desync 0.124–0.686 m; candidate (iii) silent IK
              fall-through never fired.  Fix (ii): execute_place /
              execute_stack mirror the placed/stacked body's
              runtime pose into env.plan_client_id via
              p.resetBasePositionAndOrientation right after the
              post-action lift.  Fix (i): the move action handler
              compares current_config against the planner's q1 and
              if |diff| > 1e-2 rad, builds a runtime q1 carrying
              the original q1's collision-check metadata
              (ignored_body_ids, held_body_ids, grasp_ee_offset)
              and replans via planner.streams.plan_motion;
              StopIteration breaks the dispatch loop to trigger an
              outer replan from current_config (same path as IK
              failures).  Diagnostic prints from commit 23687af
              ([#60-diag] lift OK / DESYNC / move-above-traj[0])
              all removed; the new [#60-fix(i)] one-liner only
              fires when a runtime replan happens, and the lift
              IK-failure print stays as a hard-error signal.
              Mechanism behind #56's revert ("no observable
              effect at any value tested") is now closed: the
              lifted pose now reaches plan_motion via the
              execution-time replan, so re-attempting the
              --post-action-lift CLI flag becomes meaningful as a
              regression test (lower values should now break the
              run when cube top exceeds lifted altitude).
              py_compile clean across execution.py and
              test_full_pipeline.py.  TIER 0 again empty.  Open
              count: 7 -> 6.
Added:        2026-05-08 (later) — #62 (TIER 2 — replan-while-holding
              still slow after #58.  User-flagged 2026-05-08: "the
              planner still takes too much time to replan while
              holding object.  its faster but still slow.").  Sub-
              hotspot of #50 (planner perf investigation) localised
              to the held-cube replan path.  After #58 commit
              690f86d eliminated the ~4-action re-grasp cycle by
              emitting ('holding', ?o) instead of dropping the cube,
              the user observes a measurable improvement but
              residual slowness on the same path.  Profile-first
              suspects: (a) sample-grasp re-firing every replan to
              certify a fresh (Grasp ?g) + (valid_grasp ?o ?g) on
              the same held cube — grasp atom could be threaded
              through planner.plan() the same way held_obj is;
              (b) compute-stack-kin / compute-kin re-running IK
              against the unchanged held-cube transform on every
              candidate skeleton; (c) full _build_init rebuild on
              every replan (broader #50 concern but felt sharply on
              the held branch — (holding ?o) grounds more per-
              skeleton state than (handempty)).  Open count: 6 -> 7.
Slimmed:      2026-05-08 (later) — moved #56 (DONE then REVERTED
              2026-05-08), #58 (DONE 2026-05-08), and #60 (DONE
              2026-05-08) to archive/CODEBASE_AUDIT_RESOLVED.txt
              under a new "2026-05-08 SLIM ADDITIONS" section.
              Bodies preserved verbatim with Outcome paragraphs
              recapping the actual landings (commits bf0800c +
              ac47aa6 for #56; commit 690f86d + companion b248c76
              for #58; commits 23687af + c08dc65 for #60).  Open
              audit shrunk by ~270 lines of issue bodies; still-
              active items: #9, #10, #50, #57, #59, #61, #62.
              No code changes.
Restructured: 2026-05-08 (later) — pre-evaluation correctness chain
              complete (#31, #36, #41, #48, #49, #53, #54, #55, #58,
              #60 all DONE).  Eval (#9, #10) is now the user's
              critical path to the thesis Results chapter; promoted
              from TIER 3 to TIER 1.  Re-tiered SUMMARY block:
                TIER 0 — empty (no open correctness blockers).
                TIER 1 — ACTIVE EVAL: #9 (eval runner + scalability
                         matrix), #10 (uniform-vox baseline).
                TIER 2 — PERF: #50 (planner perf investigation),
                         #62 (replan-while-holding sub-hotspot).
                TIER 3 — CODE QUALITY / LOW-PRIO: #57 (registry
                         geometry), #59 (scaled find-and-stack —
                         very low prio per user), #61 (smart console
                         filter UX polish).
              SUMMARY total-count line corrected from "6 items" to
              "7 items" (#62 was added 2026-05-08 but the count was
              never refreshed).  Roadmap "Pre-evaluation" /
              "Evaluation prerequisites" / "Parallel" sections kept
              as-is; the TIER move is a SUMMARY-only shift that
              reflects reality without rewriting the roadmap
              narrative.  No code changes.
Added:        2026-05-08 (later) — #63 (TIER 3 — README + wiki
              final doc sweep, gated on eval.  User-flagged
              2026-05-08: "we need to update the read me and
              wiki.  this is should be done post eval.  most
              likly the last thing we change.").  Doc-state
              catch-up at thesis-submission time: walk audit
              changelog since the last full wiki pass, page-by-
              page diff against current code (PDDL_Domain_-
              Reference, Execution_Pipeline, Planning_System,
              Architecture_Overview, Known_Issues_and_Roadmap,
              Design_Decisions per the obvious drift hotspots
              from the 2026-05-07/08 audit chain), refresh
              README flags table (missing --goal, --n-hidden,
              --n-occluders, --seed today).  Per CODEBASE POLICY
              L823-828 the wiki is updated after every behaviour
              change anyway — this issue is the safety-net
              final pass, not the primary update mechanism.
              Gate: do NOT start before #9 / #10 land; eval
              results drive the Known_Issues_and_Roadmap +
              Design_Decisions narrative.  New "Post-evaluation
              (final doc sweep)" section added to the ROADMAP
              between "Evaluation phase" and "Parallel".  Open
              count: 7 -> 8.

Updated:      2026-05-08 (later) — #9 [DONE].  eval_runner.py +
              eval_plotter.py landed.  Subprocess-per-cell driver;
              built-in "scalability" preset (n_occluders 1..6 ×
              n_targets 1..3 × 5 seeds × goal=holding, n_hidden =
              min(n_targets, n_occluders) for deepest search) and
              "smoke" preset (1 cell).  Aggregator collates
              cells/*/timing_summary.json into aggregated.csv +
              aggregated.jsonl.  Plotter consumes aggregated.csv,
              produces planning_time / success_rate / plan_count
              vs n_occluders PNGs (matplotlib) or text tables
              (fallback); --baseline-csv hook stubbed for #10
              uniform-vox overlay.  Master --seed plumbing in
              test_full_pipeline.py __main__: random.seed() +
              np.random.seed() right after parse_pipeline_args() so
              the 8 process-global RNG call sites (target choice,
              PDDL init shuffle, IK joint sampling, RRT goal bias,
              placement offsets, path-smoothing pivots) become
              deterministic.  FD's internal random_seed=-1 left
              alone — same noise hits every approach at the same
              seed, mean comparison still valid.  Open count: 8 -> 7
              (#10, #50, #57, #59, #61, #62, #63).  TIER 1 narrows
              to #10 alone.

Updated:      2026-05-08 (later) — README updated with a "Running on
              this machine (WSL + PowerShell)" section enumerating the
              three project-specific PowerShell aliases and what they
              translate to.  Aliases (defined in
              $PROFILE = Microsoft.PowerShell_profile.ps1):
                run_boxels  -> wsl bash -c "cd .../Semantic_Boxels &&
                               source wsl_env/bin/activate && DISPLAY=:0
                               LIBGL_ALWAYS_SOFTWARE=1 PYTHONPATH=
                               .../pddlstream_lib python3
                               test_full_pipeline.py $args"
                run_eval    -> wsl bash -c "cd .../Semantic_Boxels &&
                               source wsl_env/bin/activate && PYTHONPATH=
                               .../pddlstream_lib python3 eval_runner.py
                               $args"
                plot_eval   -> wsl bash -c "cd .../Semantic_Boxels &&
                               source wsl_env/bin/activate && python3
                               eval_plotter.py $args"
              (cursor-agent shim also lives in $PROFILE but is editor
              tooling, not part of the boxels pipeline — noted in the
              README parenthetical for completeness.)

              Trigger: 2026-05-08 WSL session — first eval sweep
              (sweep_2026-05-08_14-28-22_scalability, 90 cells, 31/90
              failures-or-timeouts) ran fine via run_eval, but
              plot_eval produced "=== ... (matplotlib not available)
              ==="  text tables instead of PNGs because wsl_env was
              missing matplotlib (eval_plotter.py L26-32 try/except
              import sets HAVE_MPL=False, _print_text_table fallback at
              L94-104).  Diagnosis chain that the README's "Common
              pitfalls" subsection now documents:
                (1) Initial fix attempt targeted ~/anaconda3 — wrong
                    Python entirely; the shims activate wsl_env, not
                    base.
                (2) PowerShell quoting traps surfaced too:
                    plot_eval (Get-ChildItem ...).FullName + 'aggregated.csv'
                    splits into two args; "$(...)\aggregated.csv"
                    eats the FullName backslashes because PowerShell's
                    double-quote interpolation interprets \U, \H, \g,
                    \S as escape sequences before the string crosses
                    into bash.  README documents the wslpath /
                    /mnt/c/... workaround.
                (3) Real fix: pip install matplotlib INTO wsl_env
                    (matplotlib-3.10.9 + 8 deps).  Re-running
                    plot_eval on the same aggregated.csv produced
                    planning_time / success_rate / plan_count PNGs
                    next to the CSV as designed.

              README change: new section between "Quick Start" and
              "Documentation" — alias table, one-time wsl_env
              bootstrap line (now includes matplotlib so this won't
              re-bite a fresh checkout), single-scene + sweep + plot
              command examples, "Common pitfalls" subsection covering
              the three traps above.  No code change.

              Cross-ref: WSL VIRTUAL ENVIRONMENT — DO NOT DELETE
              policy block (L888-891) updated implicitly — the README's
              bootstrap line now includes matplotlib, which the policy
              block's recreate command does not yet.  Defer touching
              the policy block to the #63 final doc sweep so the
              policy section stays a one-pass edit.

Updated:      2026-05-08 (later) — #10 [DONE 2026-05-08].  Uniform-
              voxelization baseline landed in commit afdaadd as a
              free-space-only swap (OBJECT + SHADOW boxel pipelines
              untouched).  Strict static uniform 3D lattice mirrors
              FreeSpaceGenerator's signature; positions are computed
              once at scene init and never change, only per-cell
              labels flip as objects are revealed/moved (consulted
              TAMPURA — Curtis et al. 2024 — find_dice/env.py: voxel
              labels live in Python, planner sees per-OBJECT atoms;
              same pattern as our existing _build_init re-emit).
              Fix shape, 5 files:
                - uniform_grid.py (NEW): UniformGridGenerator,
                  cell_size kwarg (default 0.05 m ~ one cube footprint),
                  workspace bounds match FreeSpaceGenerator (free_-
                  space.py:47-48), AABB-AABB rejection identical to
                  the octree path, FP-imprecision epsilon (1e-9) on
                  the floor() so evenly-divisible workspaces don't
                  silently lose a row of cells.
                - boxel_env.py: instantiate UniformGridGenerator next
                  to FreeSpaceGenerator; use_uniform_grid flag (default
                  False) gates the dispatch in generate_free_space();
                  set_uniform_cell_size() recreates the lattice for
                  matrix sweeps.  reboxelize.py is automatically
                  baseline-aware via the same single dispatch seam.
                - run_logger.py: --baseline {semantic,uniform}
                  (default semantic) + --uniform-cell-size FLOAT
                  (default 0.05).
                - test_full_pipeline.py: thread baseline +
                  uniform_cell_size through run_config, main()
                  kwargs, and the post-construction env config
                  (env.use_uniform_grid + set_uniform_cell_size if
                  non-default).
                - eval_runner.py: baseline axis added to
                  SCALABILITY_MATRIX (90 -> 180 cells, semantic +
                  uniform), --baseline pipe-through in cell_to_argv,
                  cell_tag suffix _semantic / _uniform, baseline +
                  uniform_cell_size in PRIMARY_COLUMNS for stable
                  CSV ordering.
              eval_plotter.py needs no changes — its --baseline-csv
              overlay hook (eval_plotter.py:157) already consumes a
              second aggregated.csv; running the sweep + filtering
              rows by baseline produces the comparison inputs.
              Sanity checks: py_compile across all 5 files;
              UniformGridGenerator on a (0.6 x 0.6 x 0.5) workspace at
              0.05 m -> 1440 cells (12 x 12 x 10) post-FP-fix
              (was 1210 = 11 x 11 x 10 with naive floor); obstacle
              rejection drops 120 cells for a 0.2 m^3 obstacle; eval
              dry-run prints 180 cells with _semantic/_uniform tags;
              parse_pipeline_args round-trip on the new flags
              returns the expected values.  WSL end-to-end run
              deferred to next session (PyBullet only spins up under
              WSL on this machine).  Open count: 7 -> 6.  TIER 1 now
              empty (eval prerequisites complete, #9 + #10 both DONE).
Added:        2026-05-09 (later) — #66 (uniform/tampura baselines —
              place fails when object > cell; fix split A on main,
              C on audit-64-tampura-binary-grid).  Diagnosed from
              the 2026-05-09 stalled run (logs/run_2026-05-09_10-48-
              13/): default uniform cell size 0.05 m is smaller than
              default occluder AABB (5-7 cm wide, 12-16 cm tall),
              so `(boxel_fits ?occluder ?cell)` is unsatisfiable on
              every uniform cell, the PDDL place action's
              precondition can't ground, and PDDLStream loops at
              increasing complexity searching for a plan that
              cannot exist.  Two-branch fix:
                Plan A (main, free-space-only-swap baseline):
                  auto-tune cell size to fit the largest visible
                  AABB + 1 cm headroom.  ~30 LOC patch in
                  test_full_pipeline.main; no domain change.
                  Side-effect: cuts cell count ~8x, also
                  resolves the grounding-cost half of the stall.
                Plan C (audit-64-tampura-binary-grid, TAMPURA-
                  faithful baseline): replace boxel-indexed place
                  with continuous SE(3)-pose place via a new
                  sample-place-pose stream.  Domain rewrite +
                  streams + planner init changes; the actual
                  TAMPURA approach (find_dice/env.py uses
                  continuous poses).
              Plan B (multi-cell place over a cluster of cells)
              ruled out 2026-05-09 — neither faithful to TAMPURA
              nor cheap.  Branch consolidation: baseline-freespace-
              only-swap deleted (it was a stale snapshot of main);
              main IS the free-space-only-swap baseline going
              forward.  ONE shared #66 audit body across main and
              audit-64 — edit on main, merge forward.  Open count:
              7 -> 8.
Added:        2026-05-10 — #67 (Phase 1 / Phase 4 hidden-target
              criterion disagreement — silent run abort).  TIER 1
              correctness blocker, surfaced during the
              eval_results/sweep_2026-05-10_uc-false/ smoke trace.
              Reproducer: `python test_full_pipeline.py --scene
              scalability --n-occluders 1 --n-targets 3 --n-hidden
              1 --seed 2 --goal holding --baseline semantic`
              (also reproduces under --baseline uniform — bug is
              baseline-independent).  GUI replay 2026-05-10
              confirmed: Phase 1 reports "Hidden-target guarantee
              OK: 1/3 hidden" via camera-ray occlusion;
              Phase 4 reports "No targets hidden — all visible
              from camera" via AABB-containment in a shadow boxel;
              the two criteria disagree because shadow boxels are
              axis-aligned approximations of the camera-to-occluder
              frustum, so a target near a shadow edge can be
              camera-occluded but lie outside the shadow AABB.
              When this happens, Phase 4 falls into "all visible",
              picks a random target whose OBJECT boxel was never
              registered (Phase 2 only emits OBJECT boxels for
              camera-visible objects), prints "WARNING: Target X
              has no boxel in registry", calls env.close() and
              return False.  No planner call is made;
              report_run_outcome() is never invoked;
              eval_runner writes a stub timing_summary.json with
              exit_reason=no_summary.  In-flight sweep
              eval_results/sweep_2026-05-10_uc-false/ at 67/180
              cells: 11 of 25 failures (~44 % of failures, ~16 %
              of all cells) match this signature on both
              baselines identically — matched-pair failure, so
              semantic-vs-uniform mean comparisons are NOT
              corrupted, but absolute success rates are
              under-reported by ~16 % at the n_occluders=1..3
              prefix.  Two-commit fix per user direction
              2026-05-10: (1) tighten hidden placement so
              hidden = inside-shadow-AABB by construction (bias
              toward occluder footprint inset, audit #29 raycast
              gate retained as final check), (2) drop Phase 4's
              AABB test in favour of camera-ray occlusion as the
              single source of truth.  Independent of #66 (Plan A
              landed) / #50 / #62.  Open count: 8 -> 9.
Updated:      2026-05-09 (later) — #66 [Plan A DONE on main].  Auto-tune
              uniform cell size to fit the largest object AABB +
              0.01 m headroom in test_full_pipeline.main, applied
              inside the existing `if baseline == 'uniform':` block
              (test_full_pipeline.py:445-481).  Acts as a safety
              guard: if --uniform-cell-size is already >= the auto
              value, keep it; otherwise bump up.  Smoke-test
              correction vs the audit body's "~0.10 m" estimate:
              `test_boxel_fits` (streams.py:369) checks all 3 axes,
              so cell size is forced to fit the OCCLUDER HEIGHT
              (12-16 cm), not the XY footprint — auto_cell ≈ 0.17 m
              for the default scene (not 0.10 m).  Lattice drops
              4x4x2 = 32 cells (was 16x16x10 = 2560 at 0.05 m on
              the 0.8x0.8x0.5 _SAFE_TABLE workspace), ~20 free
              after obstacle rejection.  ~100x cell reduction (not
              ~8x as the audit body predicted) but plenty for
              grounding place.  Risk-2 (annotate_free_space_surface
              on_z check) verified clean: bottom-layer cell's
              min_corner[2] = table_z exactly at any cell size, so
              `on_z = b.min_corner[2] <= table_z + 0.01` always
              passes for the bottom layer.  Plan C still pending on
              audit-64-tampura-binary-grid; #66 stays open.  Open
              count: 8 -> 8 (Plan A is half of #66).
Added:        2026-05-09 — #65 (rename: boxel -> hanixel terminology
              overhaul).  User decision 2026-05-09: "boxel" retired
              in favour of HANIXEL = HANI + XEL, where
                HANI = Heterogeneous Axis-aligned Named Interval
                       (each letter a literal property of the data
                       structure: H = polymorphic semantic label
                       OBJECT/SHADOW/FREE_SPACE; A = AABB
                       representation in every mode; N = string
                       `id` handle; I = 3-D interval = AABB in the
                       formal geometry sense)
                XEL  = inherited voxel-suffix; same construction as
                       vo(lumetric) + (pi)xel and te(xture) + (pi)xel.
              Mechanical rename — no correctness, performance, or
              behavioural change.  Single dedicated branch
              (rename-boxel-to-hanixel) off main, case-preserved
              sed across *.py / *.pddl / forward-looking *.md, file
              renames (boxel_*.py -> hanixel_*.py), PDDL predicates
              (Boxel -> Hanixel; obj_at_boxel -> obj_at_hanixel;
              boxel_fits -> hanixel_fits) mirrored in domain +
              _build_init + goal-construction.  Changelog history
              and archive/* keep "boxel" wording (audit trail).
              Repo name + thesis title separate decisions.  TIER 3.
              Open count: 6 -> 7.
Updated:      2026-05-10 — #67 [DONE 2026-05-10].  Single-commit
              resolution: dropped the audit body's two-commit plan
              (placement tightening + Phase 4 criterion swap) in
              favour of just the Phase 4 swap, after user pushback —
              making Phase 4 use camera-ray occlusion (matching
              Phase 1 / oracle_detect_objects) eliminates the
              disagreement at its source, so placement does not need
              tightening to make the AABB proxy succeed.
              test_full_pipeline.py: oracle_detect_objects() runs
              once at Phase 4 entry; hidden_targets_set drives the
              holding-goal target pick and the find-and-tray-stack
              visible/hidden classification; AABB containment is
              demoted to a best-effort target_to_shadow lookup used
              only for the oracle log line and is tolerated to be
              empty per-target (new "boundary case" log message).
              Reproducer (occ1 tgt3 hid1 seed2 holding semantic)
              post-fix: Phase 4 prints "Target: green_object" with
              the new boundary-case ORACLE log line, run reaches the
              planner, executes 8 plans, then fails LOUDLY with
              audit #21 (cascading-occlusion give-up after 3
              still_blocked sense attempts) — strict improvement
              over the previous silent abort with
              exit_reason=no_summary.  The downstream cascading-
              occlusion problem is known-deferred audit #47
              (re-ground blocker atoms) territory, surfaced now
              because no_summary aborts no longer hide it.
              Open count: 9 -> 8.
Added:        2026-05-10 (later) — #68 (scene randomizer — vary
              the (occluder, hidden-target) pair count per run).
              User direction 2026-05-10: "next top prio issue:
              we need to do a scene randomizor.  So every time
              there's some variable number of occluder-hidden
              object pairs.  The task would be to let the AI do
              the work of writing the scene randomizer and then
              ask the user to test it out and wait for response."
              Today scalability_scene takes fixed
              --n-occluders / --n-targets / --n-hidden counts;
              the eval matrix sweeps these axes deterministically
              across cells but every CELL produces the same
              count.  New issue captures the randomizer that
              picks the pair count itself per run (within a CLI-
              configurable range) so consecutive GUI runs and
              per-cell scenes differ in N pairs as well as in
              placement.  TIER 0 — top priority per user.
              Workflow note in body: AI implements; user runs;
              AI waits for response before declaring done.
              Open count: 8 -> 9.
Added:        2026-05-10 (later) — FOR LATER bullet for "Parallel
              FastDownward — split search space across CPU threads
              (possibly GPU)."  User-raised idea, super-low
              priority, post-thesis research direction.  Captured
              with the hard problem flagged ("find a meaningful
              way to split the search space"), the off-the-shelf
              option (FD portfolio mode) acknowledged, and a few
              more research-y angles (goal-fact partitioning,
              per-thread h-bound bands, cooperative beam search
              with shared incumbent) sketched so a future
              attempt has a starting point.  Cross-references
              #50/#62 (Python-side perf hunt — same goal,
              different layer).  Not numbered; lives as a bullet
              in the FOR LATER backlog.
Updated:      2026-05-10 (later) — FOR LATER "Parallel FastDownward"
              bullet rewritten with prior-work findings from a
              web search the same day.  Substantial CPU prior
              work exists (HDA* / Distributed FD by Kishimoto-
              Fukunaga-Botea ICAPS 2009 — 30-60x on 128 cores,
              live code at github.com/jinnaiyuu/distributed-
              fast-downward; ArvandHerd IPC 2011 winner —
              4 Arvand + 1 LAMA configs; FD Stone Soup; Springer
              2021 multi-core BFS; arXiv 2407.21485 generalized
              planning).  GPU is a dead-end for classical PDDL
              specifically (FD pointer-heavy data structures);
              PyBullet GPU pipeline is dormant; Isaac/MuJoCo MJX/
              Genesis are the GPU-sim escape hatches but require
              a full simulator swap.  Bullet now also explains
              WHY it stays FOR LATER not promoted (the wall-clock
              complaint is upstream of FD search per #50/#62).
Added:        2026-05-10 (later) — #69 (PyBullet rayTestBatch —
              pass numThreads on the 5 existing call sites).
              TIER 3, low effort, measurable win.  Promoted from
              the FOR LATER bullet to a numbered issue because
              (unlike the speculative FD/GPU angle) this is just
              an unused PyBullet feature with a 1-kwarg patch.
              Five call sites identified by grep: boxel_env.py
              (oracle_detect_objects, compute_shadow_blockers),
              shadow_calculator.py, execution.py x2 (sense_shadow
              _raycasting — 147 rays per call, biggest single
              batch).  Default numThreads=0 (Bullet auto-picks).
              Thread-safety footgun documented in Care.  Open
              count: 9 -> 10.
Slimmed:      2026-05-10 (later) — moved 3 inline bodies to
              archive/CODEBASE_AUDIT_RESOLVED.txt under a new
              "2026-05-10 SLIM ADDITIONS" section:
                #9  [DONE 2026-05-08 (later)] eval framework +
                    matrix runner (commit e8237b6).
                #10 [DONE 2026-05-08 (later)] uniform-vox
                    baseline (commit afdaadd).
                #67 [DONE 2026-05-10] Phase 1/4 hidden-target
                    criterion swap (commit 4c9e4d5).
              Bodies preserved verbatim including the
              "Outcome [DONE ...]" implementation notes #9 and #10
              grew during the 2026-05-08 push.  Open audit shrinks
              from 12 inline issue bodies to 9 (one per still-open
              issue: #50, #57, #59, #61, #62, #63, #65, #66, #68).
              SUMMARY block already references closed issues by
              short summary, not by body, so no SUMMARY edits
              needed beyond the changelog reference here.
Added:        2026-05-12 — #70 (random-pairs scene — unreachable
              placements hang objects in air).  TIER 0 per user
              direction.  Post-merge defect against #68: random-
              pairs delegates to scalability_scene, which builds
              its SceneConfig without constrain_to_reach=True
              (unlike stack_scene); _random_xy_positions samples
              in the wide _TABLE_PLACE_* window with no reach-
              disk filter.  Robot base sits at (-0.4, 0.0),
              reach radius 0.65 m -> reachable x ceiling drops
              from 0.25 (at y=0) to 0.11 (at y=±0.4); the
              placement window goes out to x=0.5.  Roughly
              half the placement window lies outside the reach
              disk, so the majority of multi-object spawns
              produce at least one unreachable cube.  Pick IK
              still succeeds via
              the small slack beyond the comfort-reach disk,
              the grasp constraint binds, post-pick lift then
              wedges the cube in mid-air with the arm fully
              extended ("object hung in air" artefact reported
              by user GUI run).  Open count: 10 -> 11.
Added:        2026-05-12 (later) — #71 (refresh OBJECT boxel
              AABBs on every sense — shadow regions untouched).
              Currently the registry's OBJECT AABBs are written
              once at scene creation (or once at discovery for
              non-target objects found via sense_contains_
              nontarget) and never refreshed.  Cubes that shift
              between actions (settle, get bumped, jiggle on a
              stack) drift away from the registry's stored
              AABB; the planner then reasons over stale
              geometry and may emit false-free or stale-on
              facts.  User direction: refresh BB only, not the
              shadow region behind it (shadow recomputation is
              expensive and out of scope for this fix).
              Open count: 11 -> 12.
Added:        2026-05-12 (later) — #72 (shadow AABB carves only
              along dominant axis — lateral overhang beside the
              occluder).  Calculate_shadow_boxel builds an AABB
              enclosing both the cube AABB and the divergent ray
              hit points, then carves the occluder out along
              ONE axis (the dominant component of mean(hits) -
              obj_center).  Perpendicular axes inherit the wide
              hit-point bounds all the way back to the cube's
              back face, so the SHADOW boxel "sticks out" beside
              the cube on the camera-facing side (~7-10 cm of
              non-shadow region absorbed into the AABB).  Free-
              space generator and boxel_fits then carve cells
              that the camera can actually see.  User-reported
              visually 2026-05-12 (random-pairs GUI run, seed
              779694423).  Open count: 12 -> 13.

Resolved issues:     archive/CODEBASE_AUDIT_RESOLVED.txt
Deferred/background: archive/CODEBASE_AUDIT_DEFERRED.txt

================================================================================
CODEBASE POLICY
================================================================================

GIT COMMITS IN POWERSHELL — DO NOT USE BASH HEREDOCS
       The environment uses Windows PowerShell. Do NOT use bash-style
       heredocs (`cat <<'EOF'`) for git commit messages, as they will hang
       or fail. Use standard `git commit -m "message"` instead.

GIT PUSH — BOTH REMOTES (GITHUB + GITLAB)
       After pushing meaningful work, push the same branch to BOTH hosts
       so they stay in sync:
         git push github <branch>
         git push origin <branch>
       Remotes: `github` → github.com (hanihabobo-dot/pybullet);
                `origin` → git.rwth-aachen.de (hani.alassiri.alhabboub/pybullet).
       Do not treat a single-remote push as "done" unless the other host
       is intentionally out of scope for that change.

COMMENTED-OUT CODE — DO NOT DELETE
       Commented-out code is preserved intentionally as inline
       documentation, quick-enable toggles, and reference for
       re-implementation.
       Rule: NEVER delete without explicit approval.  Move to archive/.

DEAD CODE — PRESERVED FOR FUTURE USE
       Dead methods/imports documented in old #87–#92 (resolved archive)
       are NOT scheduled for deletion.  No runtime cost; may be needed
       for evaluation framework, real perception, etc.

WIKI — UPDATE AFTER EVERY CHANGE
       Project wiki: pybullet.wiki.git (GitHub wiki repo, not main repo).
       Local clone: ../pybullet.wiki/
       Update after any change to architecture, data structures,
       algorithms, PDDL domain, execution pipeline, or design decisions.

ARCHIVE — TRACKED AND PUSHED
       archive/ contains superseded implementations and audit history.
       Tracked by git.  Must NOT contain personal information.

PYBULLET — PLANNING VS. GUI (audit #46 supersedes old #60 full-plan lock)
       PDDLStream planning runs against a separate DIRECT PyBullet client
       (boxel_env.plan_client_id); the GUI window stays interactive during
       long planner.plan() calls.  RenderingLock may still wrap brief IK
       solves on the GUI client during execution (execute_pick / place /
       stack), so a short flicker is possible per action — not a multi-
       minute freeze.  If WSLg/X11 still drops under heavy load, restart
       WSL (wsl --shutdown from PowerShell, then reopen terminal).

WSL VIRTUAL ENVIRONMENT — DO NOT DELETE
       wsl_env/ is the WSL Python venv (gitignored, local only).
       Deleting it breaks the entire pipeline.
       Recreate: python3 -m venv wsl_env && pip install numpy pybullet

BOXEL_FITS / STATIC INIT FACTS — REBUILT EACH REPLAN (old #102)
       Predicates such as (boxel_fits ?o ?b) are precomputed in
       pddlstream_planner._build_init() from the current BoxelRegistry
       snapshot.  Not a PDDLStream test stream — an earlier implementation
       used one (test-boxel-fits), but adaptive search re-evaluated it
       heavily across skeletons and dominated runtime despite a cheap
       per-call check.  stream.pddl no longer declares that stream; domain
       and predicate are unchanged.  As of 2026-04-15, free-space boxels
       ARE re-merged and re-split between replans (reboxelize_free_space
       after sense); the registry is mutated in place before each
       planner.plan() call, and shadow removal / new object creation after
       sense also update the registry.  The planner is stateless between
       calls — all context is passed in each plan() call.

META-ISSUE — OLD #95 SYSTEMIC PDDL STATE DRIFT (dissolved)
       The 4 drift channels between PDDL symbolic state and PyBullet
       physical state are tracked individually: (a) robot config drift
       after pick/place — closed by audit #1 [RESOLVED, archive];
       (b) hidden move in sense — audit #3 [RESOLVED, archive];
       (c) stale shadow geometry after occluder relocation — audit #4
       [RESOLVED as accepted simplification, archive 2026-05-03]; (d) stale
       blocks_view_at facts — old #73 [DONE, archive].  See THESIS_NOTES.md
       for accepted geometric / symbolic gaps (including stale shadow AABBs).


################################################################################
#                                                                              #
#  OPEN ISSUES — #50, #57, #59, #61, #62, #63, #65, #66                         #
#                                                                              #
################################################################################


================================================================================
ROADMAP — PRE-EVALUATION VS. EVALUATION (updated 2026-05-08)
================================================================================
Pre-evaluation (intended order before the eval sweep):
  #31 [DONE 2026-05-07] (comment-quality sweep, absorbs #32)
    → #41 [DONE 2026-05-07] (on_table) + #48 [DONE 2026-05-07] (clear on_relations[X])
    → #36 [DONE 2026-05-07] (hardcoded place/stack lift)
    → #49 [DONE 2026-05-07] (tray goal)
    → #53 [DONE 2026-05-07] (TIER 0 diagnostic triage — 1-cube tray-stack)
    → #54 [DONE 2026-05-07] (TIER 0 follow-up triage — visible-only tray-stack)
    → #55 [DONE 2026-05-08] (find-and-tray-stack 2-cube goal — lazy-
        collision hack + held-cube AABB fallback; Path C archived on
        branch audit-55-pose-aware-stack)
    → #60 [DONE 2026-05-08] — post-action lift now reaches plan_motion
        via runtime sync into plan_client (fix ii) + execution-time
        motion replan from the lifted pose (fix i).  Diagnostic logs
        from commit 23687af (#60 step a) confirmed candidates (i) and
        (ii) firing universally; targeted Option B fix landed in
        commit c08dc65.  Pre-evaluation chain complete.
    → after #60: #50 (planner perf) is the only pre-eval-adjacent
      open item, parallelised with the eval sweep.
  (#40 landed 2026-05-05 — physics verifier for stack goals; will
   move to archive on next slim.  #47 [DEFERRED OUT OF SCOPE 2026-05-06]
   — user decision; the archived #21 log-only band-aid is the
   accepted level of correctness, real fix abandoned.
   #30 [WONTFIX 2026-05-06] — split-pick mitigation scrapped per
   supervisor; ~2× holding-goal slowdown accepted.)

Evaluation prerequisites (runner + uniform baseline for thesis comparison):
  #9  [DONE 2026-05-08 (later)] metric hooks + batch experiment runner +
      scalability matrix + plots (absorbs #12).  eval_runner.py +
      eval_plotter.py landed; subprocess-per-cell, scalability preset
      (n_occluders 1..6 × n_targets 1..3 × 5 seeds = 90 cells),
      aggregated.csv + jsonl, matplotlib-with-text-fallback plotter.
      Master --seed now plumbs through random.seed() + np.random.seed()
      at process start so two runs at the same seed are bit-identical
      end-to-end (target choice, PDDL init shuffle, IK joint sampling,
      RRT goal bias, placement-offset shuffle, path-smoothing pivots).
      FastDownward's internal tie-breaking RNG (random_seed=-1 in
      vendored PDDLStream) is NOT overridden — same noise hits every
      approach at the same seed, so approach-vs-baseline averaging
      remains valid.
  #10 [DONE 2026-05-08 (later)] uniform voxelization baseline.
      Free-space-only swap: UniformGridGenerator (uniform_grid.py)
      replaces FreeSpaceGenerator under --baseline uniform; OBJECT +
      SHADOW boxels untouched, planner contract preserved.  Strict
      static lattice — positions fixed at scene init, only per-cell
      labels flip as objects are revealed/moved.  TAMPURA-style
      Python-side label maintenance via the existing _build_init
      re-emit on every planner.plan() call (no PDDL domain changes).
      eval_runner.py SCALABILITY_MATRIX gains the baseline axis
      (90 -> 180 cells, semantic + uniform); eval_plotter.py
      --baseline-csv hook already consumes the comparison input.
      Commit afdaadd; py_compile clean.

Evaluation phase (Results chapter — user-scoped sweep):
  (no remaining items — see Closed/relocated below.)
  #11 [WONTFIX 2026-05-06] — fixed-semantic-regions baseline killed;
       semantic-vs-uniform comparison via #10 alone carries the thesis
       claim, and #49's tray entity already exemplifies a fixed
       labelled region.
  #12 [MERGED INTO #9 2026-05-06] — scalability matrix folded into
       the eval runner.
  #13 [WONTFIX 2026-05-06] — recursive free-space discovery is dead
       code under our fixed-camera + no-concave-geometry setup; cited
       as future work in THESIS_NOTES §20.

Post-evaluation (final doc sweep — last commit before submission):
  #63  README + wiki final doc sweep — gated on #9 / #10 plus any
       other open issue that lands between now and submission.
       Doc-state catch-up; per CODEBASE POLICY L823-828 the wiki
       is updated continuously, this is the safety-net pass.
       Explicitly the last thing we change per user 2026-05-08.

Parallel / non-blocking:
  #50  planner perf investigation — can run alongside the eval sweep; any wins
       feed back into #9's scalability plots.  Subsumes the per-hotspot
       questions formerly tracked as #34 (ray×free-boxel precompute) — those
       land here as profiler hits, not as separate audit items.
  #57  registry should own cube geometry (audit #55 follow-up; replace the
       PyBullet AABB fallback in compute_stack_kin_solution).
  #59  scaled-up find-and-stack — N hidden targets stacked physically-
       sensibly via (compat_stack ?o ?on_obj) PDDL predicate (very low
       priority per user direction 2026-05-08).
  #61  smart console filter — strip residual startup/shutdown garbage
       and add Claude-Code-style thinking-animation affordance during
       long planner.plan() / RRT / stream-sample calls.  UX polish on
       the existing smart_filter pipeline; no correctness impact.
  #62  replan-while-holding still slow after #58 — sub-hotspot of #50
       localised to the held-cube replan path.  User-flagged
       2026-05-08 immediately after #58 landed: "its faster but
       still slow."  Profile first; close as MERGED INTO #50 if the
       dominant cost turns out to be branch-agnostic.
  #65  rename: boxel -> hanixel (HANI = Heterogeneous Axis-aligned
       Named Interval; XEL = voxel-suffix).  User decision
       2026-05-09.  Mechanical case-preserved sweep across *.py,
       *.pddl, README, forward-looking audit body — keep changelog
       history + archive/* as-is for the trail.  Single dedicated
       branch `rename-boxel-to-hanixel` off main.  Land after any
       in-flight branch (audit-64-tampura-binary-grid currently
       open) merges or rebases, to avoid identifier-collision
       merge noise.  TIER 3 — cosmetic only.
  #66  uniform/tampura baselines — place fails when object > cell.
       Default occluders (5-7 cm wide, 12-16 cm tall) don't fit
       any uniform 5 cm cell; (boxel_fits ?occluder ?cell)
       unsatisfiable; place can't ground; PDDLStream loops.
       Split fix: Plan A (auto-tune cell size to largest visible
       AABB + 1 cm) lands on `main` — ~30 LOC, no domain change,
       also cuts grounding cost ~8x.  Plan C (continuous-pose
       place via new sample-place-pose stream) lands on
       `audit-64-tampura-binary-grid` — domain rewrite, the
       actual TAMPURA approach.  Single shared audit body
       across both branches.  TIER 1 — correctness blocker for
       uniform.

Closed / relocated since last summary:
  #4  archive/CODEBASE_AUDIT_RESOLVED.txt (accepted stale shadow AABBs)
  #6  removed — multi-boxel place not pursued
  #42 archive/CODEBASE_AUDIT_DEFERRED.txt (collisions logged, no replan;
       supervisor reconfirmed 2026-05-06 — "no need to track knocked-down
       objects")
  #40 [DONE 2026-05-05] — physics verifier landed; archived 2026-05-06 slim
  #30 [WONTFIX 2026-05-06] — split-pick scrapped per supervisor; archived
       2026-05-06 slim
  #56 [DONE then REVERTED 2026-05-08] — CLI flag for post-action lift;
       plumbing reverted because mechanism was broken (now closed via
       #60).  Archived 2026-05-08 (later) slim.
  #58 [DONE 2026-05-08] — preserve grasp across replans; held_obj kwarg
       threaded through planner.plan().  Archived 2026-05-08 (later) slim.
  #60 [DONE 2026-05-08] — post-action lift now reaches plan_motion (Option
       B: sync + replan).  Archived 2026-05-08 (later) slim.

================================================================================
#50. PLANNER PERFORMANCE INVESTIGATION — REDUNDANCY HUNT
================================================================================
Priority: TIER 2 — Affects every eval-run wall-clock time;
          user-flagged 2026-05-06 ("planner is too slow").
Where:    pddlstream_planner.py (_build_init, planner.plan, stream
          stitching), streams.py (test/gen streams; per-call cost),
          execution.py (between-replan re-grounding paths), and the
          adaptive search inner loop (FastDownward grounder).
Depends:  None (independent investigation).  Pairs naturally with
          #9 / #12 — any wins should show up in the scalability plots.
Effort:   1 week of focused profiling, then variable per-redundancy fix.

What:  User raised at 2026-05-06 sync:
         "Personally I think the planner is too slow and want to find
          out ways to make it faster or at least discover and remove
          some big redundancies."

       This is broader than #30 (which targeted one specific
       conditional-effect cost in the pick action and is now
       [WONTFIX]).  #50 is a profiling-driven sweep — find ALL the
       big redundancies, regardless of whether they live in the
       grounder, the streams, or the Python init-fact construction.

       Known suspects (no measurement yet — confirm with profiler
       before fixing):
       (a) _build_init re-emits ALL static facts every replan.  Many
           of them (boxel_fits, semantics, geometry-derived
           predicates) are invariant under sense / pick / place and
           could be cached across replans of the same run.  Cf.
           archive note "BOXEL_FITS / STATIC INIT FACTS — REBUILT
           EACH REPLAN" — that note explained WHY the test-stream
           variant was abandoned, but did not rule out caching the
           Python-side computation.
       (b) _compute_placement_view_blocks runs every _build_init at
           O(S × 25 × F) ray-AABB tests (#34 audit).  Cache by
           (shadow geometry, free-boxel layout) hash; invalidate only
           when reboxelize_free_space mutates the registry.
       (c) The placement sampler in streams.py may be re-sampling
           geometrically equivalent poses; check if pose-level
           dedup would help.
       (d) `test_*` streams that fire many times per skeleton against
           Python-side data that doesn't change between calls — fold
           the test result into init facts and drop the stream.
       (e) Any per-action Python loop in execute_* that scales with
           registry size (look for O(N^2) over object/shadow lists).
       (f) FastDownward grounder cost: how much grounded-state size
           is inflated by predicates that have only a handful of
           true ground atoms at runtime?  candidate for
           negative-precondition / typing tightening.

Fix:   APPROACH (no single commit — investigation first):
       (a) Land a profiling pass: cProfile around planner.plan() and
           around _build_init separately; aggregate over a fixed seed
           sweep on the default scene and on scalability_scene (small
           and large).  Output a Python-side flame graph + a
           FastDownward translator-time / search-time / total-time
           breakdown (the translator already prints these; capture
           them into timing_summary.json).
       (b) For each hotspot ≥ 10% of total time, decide:
             - Cache (input is deterministic and stable across replans)
             - Vectorise / numpy-ify (pure-Python ray loops, etc.)
             - Drop / replace (dead code path or redundant fact)
       (c) Land each redundancy fix as its own commit with a before-
           after timing entry in the commit message + a one-line
           note in THESIS_NOTES.md "evaluation methodology" if it
           changes the per-call time used in the Results chapter.
       (d) Re-run the seed sweep after each fix to make sure the
           change does not regress success rate.

Care: Do not start optimising before profiling.  The planner has many
       plausible-looking suspects (init build, streams, grounder) and
       only one of them is likely to dominate at any given scene size.
       Premature optimisation will make the code harder to read for
       little wall-clock gain.

Related: #9 (experiment runner — needed to gather timing samples),
         #12 (scalability eval — wins land here visibly),
         #30 [WONTFIX] (the per-action conditional-effect cost is one
              specific instance of the broader question — out of
              scope per supervisor; revisit only if profiling shows
              it dominates after other wins),
         #34 (placement-view-blocks ray-AABB cost — concrete suspect),
         FOR LATER bullets (incremental boxel recomputation, dense
              lattice — both touch planner cost).


================================================================================
#57. REGISTRY SHOULD OWN CUBE GEOMETRY (REPLACE PyBullet AABB FALLBACK)
================================================================================
Priority: TIER 3 — Code quality.  No correctness bug — current behaviour
          is right; just an architectural cleanup.
Where:    streams.py compute_stack_kin_solution — currently reads
          p.getAABB(held_body_id) when no registry boxel exists
          (commit 8281d73, audit #55 fix).
          boxel_data.py BoxelRegistry — add object_geometry dict.
          test_full_pipeline.py — populate object_geometry at scene
          init from env.objects[name].size for every cube (visible
          AND hidden), since cube dimensions are URDF-determined and
          unknown-position is independent of unknown-size.
Depends:  None.
Effort:   <0.5 day.

What:  Audit #55's held-cube AABB fallback (commit 8281d73) reads
       p.getAABB(body_id) on the plan-client to recover a hidden
       cube's z-extent when the cube has no OBJECT-type registry
       boxel pre-sense.  The data taken is pure geometry (size, not
       position), so this is NOT an oracle leak — but bypassing the
       registry for object metadata is architecturally suboptimal:
       the registry is supposed to be the "what the planner knows"
       boundary, and one stream now reaches around it.

       The clean fix: separate KNOWN-from-URDF metadata (size, mass,
       grasp affordances) from OBSERVABLE-via-sense state (position).
       Cube sizes are URDF-determined and the same regardless of
       observability — they belong in the registry from scene init.

Fix:   ONE COMMIT:
       (a) BoxelRegistry: add object_geometry: Dict[str, np.ndarray]
           mapping object name -> half-extents (or full extents).
       (b) test_full_pipeline.py scene-init: populate
           registry.object_geometry from env.objects for every cube
           (visible + hidden + tray).
       (c) streams.compute_stack_kin_solution: replace the
           p.getAABB(body_id) fallback branch with a registry
           lookup; drop the PyBullet read entirely from this stream.
       (d) (optional) compute_kin_solution: same treatment — read
           grasp Z height from registry.object_geometry instead of
           hardcoding.

       Behaviour-preserving.  Existing successful runs (logs/run_-
       2026-05-08_10-49-01) should produce identical timing /
       success metrics.

Related: #55 [DONE 2026-05-08; archived] — introduced the AABB
              fallback this issue replaces.
         #9 — eval runner; benefits from registry as the single
              metadata source.


================================================================================
#59. SCALED-UP FIND-AND-STACK — N HIDDEN TARGETS, SIZE-COMPAT STACKING
================================================================================
Priority: TIER 3 — Very low priority (user direction 2026-05-08:
          "very very low prio").  Demo evolution; not on the
          critical thesis path.  Builds on #55 rather than scaling
          DOWN to a simpler goal.
Where:    boxel_env.py default_scene
                          — optionally mix target sizes (e.g. 30 / 40 /
                            50 mm) so the size-compat predicate has
                            real work to do.  Uniform-size targets
                            keep the predicate trivially-satisfied.
          pddl/domain_pddlstream.pddl
                          — add (compat_stack ?o ?on_obj) to the
                            :action stack precondition list.
          pddlstream_planner.py _build_init
                          — emit (compat_stack a b) for every ordered
                            pair where horizontal_half_extent(a) +
                            safety_margin <= horizontal_half_extent(b).
                            Same one-pass-per-replan pattern as
                            boxel_fits / clear.
          test_full_pipeline.py
                          — extend the find-and-tray-stack branch
                            (or add a new --goal find-and-stack-N)
                            to accept N hidden targets and build a
                            tower in size-decreasing order.
          run_logger.py   — relax --n-hidden upper cap if currently
                            limited; surface the new goal kind.
Depends:  - #55 [DONE; archived] — find-and-tray-stack pipeline
                                    reused as-is for the multi-cube case.
          - FOR LATER size-compat bullet (CODEBASE_AUDIT.txt FOR
            LATER section) — this issue absorbs option (i) of that
            bullet (symbolic predicate path).
Effort:   1-2 days (predicate plumbing + scene mix + smoke test).

What:  #55 demonstrates 1-visible + 1-hidden tray stack.  Scale up:
       2-3 hidden targets, all stacked physically-sensibly.
       "Sensibly" means the planner cannot plan big-on-small towers
       that the physics step then fails to stabilise — a (compat_stack
       ?o ?on_obj) static predicate gates :action stack so only
       smaller-on-larger placements are permitted.

       Currently safe by setup: every find-and-tray-stack run uses
       40 mm uniform targets (default_scene targets), so size-compat
       is mooted in practice.  Promoting to a numbered issue captures
       the dependency BEFORE we mix sizes; otherwise the gap surfaces
       silently the first time a richer scene composition is tried.

       User direction 2026-05-08: "find two hidden targets and stack
       them in a phiyical sensble way. or find three and do that.
       that would be nice. we alreday have find and stack. we dont
       need to make it simpler."  Hence this scales UP from #55,
       does NOT replace #55 with a simpler goal.

Fix:   ONE COMMIT (or split into domain change + scene mix):
       (a) Domain: add (compat_stack ?o ?on_obj) predicate; add to
           :action stack precondition list.
       (b) _build_init: precompute (compat_stack a b) for every
           ordered pair where horizontal_half_extent(a) +
           safety_margin <= horizontal_half_extent(b).  Source the
           half-extents from the registry once #57 lands; until
           then, env.objects[name].size is the fallback.
       (c) Optional default_scene size mix: e.g. 30 / 40 / 50 mm
           target cubes so the predicate gates real planning
           choices, not just no-ops.  Without the mix, every (a, b)
           pair is compat and the predicate is informational only.
       (d) Goal builder: build_find_and_stack_goal(targets) that
           sorts targets by descending size and chains
           (on smaller bigger).  Tray base optional; works on any
           support entity.

       Smoke test: --goal find-and-stack-N --n-hidden 2 (or 3) on
       default_scene with mixed sizes.  Expect plan to discover all
       hidden cubes via sense, then stack them smallest-on-top.

Related: #55 [DONE 2026-05-08; archived] — find-and-tray-stack base
              pipeline that this issue extends.
         #57 (open) — registry should own cube geometry; once
              landed, half-extents come from registry.object_geometry
              instead of env.objects[name].size.
         #58 (open) — preserve grasp across replans; longer towers
              fire more replans, so #58 amplifies the wins from #59.
         #50 (open) — planner perf; (compat_stack) adds O(N²)
              static facts but those ground once per replan, so
              the cost should not dominate.
         FOR LATER size-compat bullet — this issue claims option (i)
              (symbolic predicate); option (ii) (geometric sampler
              filter) remains a parallel future option.


================================================================================
#61. SMART CONSOLE FILTER — STRIP STARTUP/SHUTDOWN GARBAGE + ADD THINKING ANIMATIONS
================================================================================
Priority: TIER 3 — UX / code-quality polish.  No correctness impact;
          improves the developer-watching-a-run experience.  User-flagged
          2026-05-08: "the terminal output is still not smart enough.
          in the begining and the end there still garbage that no one
          cares about and it would be cute to have thinking animations
          or stuff like that so that it really looks like claude."
Where:    smart_filter.py — _DROP_PATTERNS (~L88-120) covers PyBullet
          GL/thread banner + a few PDDLStream lines, but startup and
          shutdown garbage still leaks; SmartConsoleFilter (the line-
          buffered drop / dedupe / aggregate / transform / pass-through
          pipeline described in the module docstring L1-26) currently
          has no time-based "thinking" affordance during long
          planner.plan() / RRT / stream-sample calls.
          run_logger.py — RunLogger wires SmartConsoleFilter onto stdout
          when verbosity='smart' (the default; see L8-14, L44-58, the
          _LEVEL_MAP / VALID_VERBOSITIES tuple); any spinner / animation
          channel needs to coexist with the _TeeStream stdout->log split
          (L63-77) so the log file stays clean and complete.
          test_full_pipeline.py — emits the run banner / phase markers
          / timing summaries that smart-mode reformats; surrounding
          startup imports + atexit shutdown sequence is what currently
          leaks at the edges of the "Phase 1 ... -> SUCCESS / FAIL"
          envelope.
Depends:  None.  Independent of #50 (planner perf — different concern;
          this is presentation-layer only).  No interaction with #9
          (eval runner; batch runs would use --log-level normal or
          quiet anyway).
Effort:   1-2 days.  Roughly half on extending DROP / aggregation rules
          for the residual leak categories, half on the spinner /
          progress affordance + ANSI cursor handling.

What:  smart_filter.py already implements the Claude-Code-style filter
       pipeline (drop / dedupe / aggregate / transform / pass-through —
       see module docstring L1-26 and run_logger.py L8-14) and
       run_logger.py defaults verbosity='smart'.  Two gaps remain that
       the user flagged:

       (1) STARTUP / SHUTDOWN LEAK.  _DROP_PATTERNS at smart_filter.py
           L88-120 covers PyBullet's GL banner ("GL_VENDOR", "Direct
           GLX", "startThreads creating", etc.) and the visible
           thread-shutdown sequence ("stopping threads", "Thread
           TERMINATED", "destroy ... semaphore"), but garbage still
           appears at the run's edges.  Likely culprits not currently
           in the drop list:
             - Python-level import warnings (deprecation, futures, etc.)
               that hit stderr before the pipeline banner prints.
             - PDDLStream / FastDownward subprocess startup chatter
               that doesn't match the existing "Iteration:" / "Attempt:"
               aggregation rules.
             - boxel_env / numpy / PyBullet "starting / loaded /
               connected" status lines emitted before
               "--- Phase 1: Environment Setup ---" prints.
             - atexit / __del__ tear-down output (stray stderr after
               the SUCCESS / FAIL summary, thread-join tracebacks).
           Concrete reproduction: run --goal find-and-tray-stack
           --log-level smart on default scene; capture the lines
           emitted BEFORE "Phase 1: Environment Setup" and AFTER the
           timing_summary.json line — those are the leak set.

       (2) NO "THINKING" ANIMATION.  Long blocking calls (planner.plan()
           — 9-30 s typical per replan per logs/run_2026-05-08_-
           10-49-01; RRT inside plan_motion; stream sample loops) emit
           nothing on the console in smart mode (the per-iteration
           output is correctly aggregated to a single summary by
           _ITER / _ATTEMPT / _PLAN_NONE / _NO_PLAN at L128-137).
           From the user's seat the terminal looks frozen.  Claude
           Code's UX trick is a compact spinner + rotating verb
           ("Thinking…", "Planning…", "Sensing…") so the user knows
           work is happening; smart_filter has the metadata (it sees
           the dropped per-iteration lines internally before
           aggregating) but doesn't render an affordance.

What's already done (don't redo):
       - Drop / dedupe / aggregate / transform / pass-through pipeline
         with ANSI styling (smart_filter._Style L58-79).
       - PyBullet GL/thread banner drop (_DROP_PATTERNS L88-120).
       - Per-iteration / per-attempt PDDLStream rollup
         (_ITER / _ATTEMPT / _PLAN_NONE / _NO_PLAN, L128-137).
       - Action narrative reformat (_EXECUTING / _MOVING_TO /
         _PICKING / _PLACING / _STACKING / _SENSING and matching
         "***" success markers, L144-159).
       - Shadow-blocker repetition collapse (_SHADOW_BLOCKERS_HDR /
         _SHADOW_BLOCKER_ROW, L140-142).
       - File-log tee preserves raw output for post-mortem
         (_TeeStream in run_logger.py L63-77) — any presentation-
         layer change here must NOT touch the log file.

Fix:   ONE OR TWO COMMITS — extend the existing filter, no architectural
       change.  Suggested split: (a) leak suppression first, (b) spinner
       affordance second so each lands as a self-contained UX win.

       (a) Leak suppression.
           - Run --log-level smart on default + scalability scenes;
             diff console output against the
             "==..." banner -> "Phase 1 ..." -> "SUCCESS / FAILED"
             envelope to enumerate the exact leak lines at both edges.
           - Add the leak set to _DROP_PATTERNS (or a new STARTUP_DROP
             / SHUTDOWN_DROP regex if the lines are stateful — e.g.
             "drop everything until first --- Phase line" / "drop
             everything after the timing_summary.json line").
           - Suppress python import warnings reaching stderr (warnings
             module; respect log-level so verbose mode keeps them).
           - Add a smoke test in tests/ that spawns the pipeline and
             asserts the first console line matches "^=+$" (banner)
             and the last matches the SUCCESS / FAIL summary, no
             trailing tracebacks.

       (b) Thinking-animation affordance.
           - Extend SmartConsoleFilter with a TimerSpinner that renders
             "  ⠋ Planning… 12s" on a single line (carriage-return,
             no newline) when no narrative output has been emitted for
             > N seconds (default ~1 s) and a long-running phase is
             active.  Erase + reprint on each tick (\r + ANSI clear
             EOL); finalise to a static "  ✓ Planned in 13.2s" line on
             phase exit.
           - Verb selected from the active phase: "Planning" during
             planner.plan() (already detected via _PLAN_HEADER /
             _TIMING at L135-137), "Sensing" during sense_shadow_-
             raycasting (_SENSING already matched), "Executing"
             otherwise.
           - Disable when stdout is non-TTY or NO_COLOR is set
             (mirror _supports_color L42-55).
           - Do NOT write spinner frames to the log file — the
             _TeeStream split (run_logger.py L63-77) has to skip the
             carriage-return updates; only the final static line
             ("Planned in 13.2s") goes through to the log.

       Out of scope:
           - Changing the verbosity default away from 'smart' (it's
             already the default, see VALID_VERBOSITIES + _LEVEL_MAP
             at run_logger.py L46-60).
           - Adding new presentation modes ('pretty', 'json', etc.).
           - Reformatting timing_summary.json.

Smoke test: --goal holding and --goal find-and-tray-stack on default
       scene, --log-level smart.  Expect: (i) first console line is
       the "==..." banner, no PyBullet/PDDLStream prelude; (ii) during
       each planner.plan() the spinner renders + updates; (iii) last
       console line is the SUCCESS / FAIL summary, no atexit
       traceback/teardown noise.  Raw log file under logs/run_<ts>/
       still contains every byte of stdout for post-mortem (verify
       byte count against a --log-level normal run on the same seed).

Related: #9   — eval runner; runs in batch (likely --log-level
              normal or quiet); spinner only fires for interactive
              smart mode so no impact on batch logs.
         #50  — planner perf; reduces the time the spinner spends
              rotating but is independent of the affordance.


================================================================================
#62. REPLAN-WHILE-HOLDING STILL SLOW AFTER #58 — HELD-CUBE REPLAN HOTSPOT
================================================================================
Priority: TIER 2 — Per-replan wall-clock cost on the held branch;
          user-flagged 2026-05-08 immediately after #58 landed.  Sub-
          hotspot of #50 (planner perf investigation) localised to the
          held-cube replan path.  Quote: "the planner still takes too
          much time to replan while holding object.  its faster but
          still slow."
Where:    pddlstream_planner.py — _build_init L505-515 (the
          (holding ?o) init-fact path added by #58 commit 690f86d),
          plan() L569+ (no certificate / static-fact caching across
          replans within a run).
          streams.py — sample_grasp generator (re-fires every replan
          to certify a fresh (Grasp ?g) + (valid_grasp ?o ?g) on the
          same held cube), compute_stack_kin_solution / compute_kin_-
          solution (held-cube IK runs against the unchanged grasp
          transform on every candidate skeleton).
          test_full_pipeline.py — held_obj plumbing through
          planner.plan() (audit #58, commit 690f86d) is the call
          site this issue tracks.
Depends:  None directly.  Pairs with #50 — share the same profiling
          pass; this issue is the (holding ?o)-stratified view of the
          same measurements.  If the dominant cost on the held branch
          turns out to be branch-agnostic (e.g. boxel_fits emission),
          close as MERGED INTO #50 rather than land duplicate fixes.
Effort:   <1 week — profile first, fix what dominates.  One or two
          targeted commits if hypothesis (a) or (b) below dominates;
          longer if (c) requires a static-fact cache keyed on
          registry-snapshot identity across replans.

What:  Audit #58 [DONE 2026-05-08] preserved the grasp across replans
       by emitting ('holding', held_obj) instead of ('handempty',)
       when held_obj is set.  This eliminated the ~4-action re-grasp
       cycle previously observed at the PLAN #1 -> PLAN #2 transition
       in logs/run_2026-05-08_10-49-01.  User confirmed the speedup
       ("its faster") and immediately flagged residual slowness on
       the same code path ("but still slow").

       Hypotheses to confirm with profiler before fixing:

       (a) SAMPLE-GRASP RE-CERTIFICATION.  When _build_init emits
           ('holding', held_obj), the certificate (Grasp ?g) +
           (valid_grasp ?o ?g) does NOT carry over from the prior
           plan — it must be re-grounded from streams.sample_grasp
           every replan.  But the held cube hasn't moved between the
           prior place/stack failure and the current replan; the
           same grasp pose that worked at execution time should
           still be valid.  Fix candidate: thread the prior plan's
           grasp atom through planner.plan() the same way held_obj
           is, and emit (Grasp <g>) + (valid_grasp <o> <g>) directly
           in _build_init when given.  Saves one stream call (and
           any IK validation it triggers) per replan.

       (b) COMPUTE-STACK-KIN / COMPUTE-KIN RE-IK.  On the held
           branch, every place/stack candidate skeleton fires the IK
           loop against the held cube's pose-in-EE transform.  That
           transform is fixed by the active grasp; the IK seed
           (current_config) is the only variable across replans.
           Profile whether IK count grows superlinearly with replan
           index or candidate-skeleton count; if so, cache by
           (target_boxel_id, grasp_id) hash inside
           streams.compute_*_kin_solution.

       (c) _BUILD_INIT FULL REBUILD.  Broader #50 concern — every
           replan re-emits all static facts (boxel_fits, semantics,
           geometry-derived predicates).  The holding branch is more
           sensitive because (holding ?o) grounds more per-skeleton
           state than (handempty), so the FastDownward grounder pays
           more per replan than on holding-goal starts.  Fix
           candidate: cache the static-fact subset keyed on
           (registry snapshot hash, held_obj, grasp_id) and reuse
           across replans within a run.  This overlaps with #50 (a)
           — land it there if profiling confirms it dominates, and
           merge this issue.

What's already done (don't redo):
       - #58 commit 690f86d — held_obj kwarg + (holding ?o) init
         fact path landed.
       - #58 commit b248c76 — execute_pick post-action lift
         equalised at 10 cm with place/stack (drops the explicit
         5 cm override in favour of _apply_post_action_lift's
         default).
       - #50 broad framing remains: "find ALL the big redundancies,
         regardless of whether they live in the grounder, the
         streams, or the Python init-fact construction."

Fix:   APPROACH (no single commit — measure first):

       (1) Add a profiling slice that splits planner.plan() wall-
           time by replan index AND by (handempty) vs (holding ?o)
           initial state.  Reuse #50's planned cProfile pass;
           aggregate over a fixed seed sweep on --goal find-and-
           tray-stack (default scene; held branch fires for
           plan #2+) and --goal holding (held branch fires only
           after a sense break).  Output the translator-time /
           search-time / total-time breakdown #50 already plans to
           capture, but stratified by the (holding ?o) flag so the
           held branch's dominant cost is visible separately.

       (2) Confirm or rule out hypotheses (a) / (b) / (c) from the
           profile.  Fix whichever dominates first; re-run the
           profile to confirm wall-clock drop.  Do not speculatively
           cache before measurement — premature caching may regress
           correctness if registry or grasp state mutates between
           replans in a way the cache key misses.

       (3) Re-run the seed sweep after each fix to confirm success
           rate is unchanged (same gate as #50).

Care: This is a sub-hotspot of #50 — if the profile shows the held
       branch's dominant cost is also the dominant cost on the
       (handempty) branch, close this as MERGED INTO #50 rather than
       landing duplicate fixes.  Keep #62 distinct only if a held-
       specific cache or certificate-threading fix lands that
       wouldn't apply on (handempty) starts.

Related: #50  — broader planner perf investigation; shares the
              profiling pass and the cache-vs-vectorise-vs-drop
              decision tree.
         #58  [DONE 2026-05-08] — landed the (holding ?o) init fact
              path and held_obj plumbing this issue localises to.
         #61  — smart console filter / spinner; reduces the
              perceived freeze during long planner.plan() calls but
              doesn't change wall-clock cost.


================================================================================
#63. README + WIKI FINAL DOC SWEEP — POST-EVAL FREEZE-AND-WRITE
================================================================================
Priority: TIER 3 — Documentation; gated on all code work being done.
          User-flagged 2026-05-08: "we need to update the read me and
          wiki.  this is should be done post eval.  most likly the
          last thing we change."  Doc state should not chase a moving
          target — land this AFTER #9 / #10 (eval) and any other open
          issue that alters behaviour, predicates, streams, or
          pipeline shape.
Where:    README.md (project root, currently 29 lines — quick-start
          + flags table + wiki link + license).
          ../pybullet.wiki/ — local clone of the GitHub wiki repo
          per CODEBASE POLICY L823-828.  11 pages: Architecture_-
          Overview, Core_Data_Structures, Design_Decisions,
          Execution_Pipeline, Home, Known_Issues_and_Roadmap,
          PDDL_Domain_Reference, Planning_System, Robot_Control_-
          and_Streams, Scene_Environment, Spatial_Reasoning.
Depends:  #9 + #10 (eval runner + uniform-vox baseline) — eval
          results drive Known_Issues_and_Roadmap + Design_Decisions
          + the README story.  Any other open issue (#50, #57, #59,
          #61, #62) that lands between now and submission extends
          the diff this sweep has to absorb.
Effort:   1-2 days (one focused pass per page).

What:  CODEBASE POLICY (L823-828) already requires per-change wiki
       updates, so the wiki is never far out of date.  But by
       thesis-submission time several pages will have accumulated
       drift from the 2026-05-07/08 audit chain (#41 on_table, #49
       tray, #55 lazy-collision, #58 held_obj, #60 lift sync) plus
       whatever lands during eval.  This issue is the final once-
       over: walk the audit changelog since the last full sweep,
       diff each wiki page against current code, push the result.

       Likely drift hotspots at landing time:
       - PDDL_Domain_Reference.md — predicates / action signatures
         vs. pddl/domain_pddlstream.pddl + pddl/stream.pddl
         (on_table, is_tray, any size-compat predicate from #59).
       - Execution_Pipeline.md — post-action lift (#36) +
         runtime-pose sync (#60) integrated into the dispatch loop
         description.
       - Planning_System.md — held_obj plumbing (#58) +
         (holding ?o) init-fact path; replan walkthrough.
       - Architecture_Overview.md / Core_Data_Structures.md —
         registry ownership story if #57 lands (geometry source).
       - Known_Issues_and_Roadmap.md — rewrite to match final
         open / resolved / deferred split; cross-link to thesis
         Results chapter.
       - Design_Decisions.md — sync with THESIS_NOTES.md (the
         canonical source); any gaps accepted during eval land
         here.
       - README.md — flags table missing --goal, --n-hidden,
         --n-occluders, --seed; quick-start may benefit from an
         eval-runner pointer once #9 lands.
       - Cross-page link health — internal wiki anchors and
         README -> wiki links.

What's already done (don't redo):
       - Wiki scaffolding + 11 pages exist and are touched per
         feature commit (CODEBASE POLICY L823-828).
       - THESIS_NOTES.md is canonical for accepted simplifications;
         this issue propagates THESIS_NOTES decisions into the
         public-facing wiki, it does NOT relitigate them.
       - README structure (quick-start, flags table, wiki link)
         is correct — only contents drift.

Fix:   ONE PASS, RUN LAST.

       (a) Walk audit changelog (this file + archive/RESOLVED) for
           every Updated / Added / Slimmed / Restructured entry
           since the last full wiki sweep; bullet which wiki page
           each lands in.
       (b) Page-by-page diff against current code: regenerate code
           blocks from actual files, predicate / action / stream
           tables from pddl/, sequence ordering against execute_-
           pick / place / stack + dispatch loop, link health.
       (c) Refresh README — add missing flags (--goal, --n-hidden,
           --n-occluders, --seed), update quick-start if #9 lands
           a recommended eval invocation, verify wiki link target.
       (d) Push wiki to both remotes if mirrored (CODEBASE POLICY
           L802-810; verify whether pybullet.wiki has a github +
           origin counterpart).
       (e) Audit entry at landing: "WIKI / README final sweep —
           frozen at <commit>."  No further edits unless filed
           as a downstream issue.

Care: Do NOT start before #9 / #10 land — starting earlier means
       redoing the same pages when eval reveals something needing
       a doc note.  Per-commit wiki touch-ups by other open issues
       remain the primary update mechanism; this final sweep is
       the safety net.

Related: All other open issues at landing time — this is the
              catch-all final pass.  Sister to THESIS_NOTES.md
              hygiene (THESIS_NOTES is internal; README/wiki is
              public — kept separate by audience).


================================================================================
#65. RENAME: BOXEL -> HANIXEL — TERMINOLOGY OVERHAUL (mechanical, large-diff)
================================================================================
Priority: TIER 3 — Code/doc cosmetic.  No correctness, performance,
          or behavioural impact.  Sweep of one identifier; touches
          most source files but every change is a literal word
          substitution.
Where:    Sweeping rename across:
          - Source: ~150 occurrences of `boxel`/`Boxel`/`BoxelData`
            etc. in *.py (boxel_data.py, boxel_env.py, boxel_types.py,
            pddlstream_planner.py, streams.py, visualization.py,
            execution.py, uniform_grid.py, reboxelize.py,
            cell_merger.py, free_space.py, shadow_calculator.py,
            test_full_pipeline.py, eval_runner.py, eval_plotter.py,
            run_logger.py, plus tests/ and any utility scripts).
          - File names: boxel_data.py -> hanixel_data.py;
            boxel_env.py -> hanixel_env.py; boxel_types.py ->
            hanixel_types.py.
          - PDDL: predicate `(Boxel ?b)` -> `(Hanixel ?b)`;
            `(obj_at_boxel ?o ?b)` -> `(obj_at_hanixel ?o ?b)`;
            `(boxel_fits ?o ?b)` -> `(hanixel_fits ?o ?b)`.
            Mirror in pddl/domain_pddlstream.pddl + pddl/stream.pddl
            + pddlstream_planner._build_init() tuple emission +
            goal-construction code in test_full_pipeline.py.
          - Docs: README.md, the FORWARD-LOOKING / LIVE issue
            bodies in CODEBASE_AUDIT.txt (NOT the historical
            changelog — keep "boxel" wording there as audit trail),
            THESIS_NOTES.md, wiki (11 pages), in-line docstrings.
          - Data files: boxel_data.json (artefact written by
            BoxelRegistry.save_to_json) -> hanixel_data.json;
            search-and-replace any references in run logs / test
            fixtures.
          - Repo name (Semantic_Boxels) — separate decision,
            optional, can be left as-is.
          - Thesis title and chapter text — out of scope here;
            handled on the writing side.
Depends:  None — purely mechanical.  No code logic changes.
          However: land AFTER any in-flight branch merges or
          rebases (audit-64-tampura-binary-grid is currently
          open) to avoid identifier-collision merge noise across
          every renamed identifier.
Effort:   ~1 hour mechanical pass + ~30 min smoke run.  Single-day
          execution.

What:  "Boxel" originated as a portmanteau of box + (vo)xel —
       fine etymologically but the team has soured on the word.
       User raised 2026-05-09 after looking at TAMPURA's voxel-grid
       code (Curtis et al. 2024) and reflecting on the project
       vocabulary.

       Replacement chosen 2026-05-09: HANIXEL.
       - HANI = "Heterogeneous Axis-aligned Named Interval" —
         each letter is a literal property of the data structure:
           H = Heterogeneous (the semantic label can be OBJECT /
               SHADOW / FREE_SPACE — exactly the polymorphic
               distinguishing feature of the project)
           A = Axis-aligned (AABB representation, true in every
               mode: semantic octree, semantic merged, uniform
               lattice)
           N = Named (every entry carries a string `id` — the
               planner's handle, e.g. `red_object`,
               `shadow_of_red_object`, `free_011`)
           I = Interval (rigorous geometry term: a 3-D interval
               = AABB; defines what the thing geometrically is)
       - XEL = inherited voxel-suffix; same construction as
         vo(lumetric) + (pi)xel and te(xture) + (pi)xel.

Fix:   ONE BRANCH — `rename-boxel-to-hanixel`, off `main`.
       (a) Mechanical case-preserved sed: boxel->hanixel,
           Boxel->Hanixel, BOXEL->HANIXEL across *.py, *.pddl,
           README.md, and the LIVE issue-body sections of
           CODEBASE_AUDIT.txt — but EXCLUDING the changelog
           history block at the top of CODEBASE_AUDIT.txt and
           ALL of archive/CODEBASE_AUDIT_RESOLVED.txt /
           archive/CODEBASE_AUDIT_DEFERRED.txt.  Those record
           historical wording and stay verbatim.
       (b) File renames: boxel_*.py -> hanixel_*.py; update
           every import and from-import to match.
       (c) Data-artefact rename: any place that writes or reads
           "boxel_data.json" updates to "hanixel_data.json"
           (run_logger.py, BoxelRegistry.save_to_json caller,
           any test fixtures).  Old artefacts in logs/ stay
           with their historical names.
       (d) py_compile across all touched .py files; smoke run
           (--scene default --baseline semantic --no-gui --seed 0)
           to verify nothing broke.
       (e) Two commits: one mechanical rename + one manual
           cleanup (e.g. README "Semantic Boxels" -> "Semantic
           Hanixels" for the public-facing prose; audit body
           rebrand in TOC banner + SUMMARY block; visualizer
           class-docstring polish).
       (f) Audit DONE entry on this issue at landing.

Care: Don't touch the changelog history at the top of this file —
       every "Added:"/"Updated:"/"Slimmed:"/"Restructured:" entry
       above mentions "boxel" because that was the term at the
       time.  Rewriting history would lose the audit trail.  Same
       for archive/CODEBASE_AUDIT_RESOLVED.txt and
       archive/CODEBASE_AUDIT_DEFERRED.txt.  Only the
       FORWARD-LOOKING / LIVE issue bodies and the SUMMARY block
       get the rename.

       Coordinate with audit-64-tampura-binary-grid: that branch
       is currently sitting on the GUI uniform_mode flag work plus
       its own #64 audit body.  Either merge it to main first
       (preferred — clean main = clean rename diff) or rebase it
       on top of #65 after #65 lands (every commit on the branch
       gets a noisy text-rename hit).

Related: #63 (README + wiki final doc sweep) — picks up the
              hanixel terminology naturally if it lands first.
              If #63 hasn't started yet at #65 landing time,
              #63 inherits the rename for free.
         #64 (open on audit-64-tampura-binary-grid) — see "Care"
              above; merge order matters for diff cleanliness.
         Repo name (Semantic_Boxels on disk + both remotes) —
              separate decision, can stay as-is to avoid breaking
              upstream URLs.
         Thesis title ("Semantic Boxels for TAMP" -> "Semantic
              Hanixels for TAMP") — your call on the writing side.


================================================================================
#66. UNIFORM/TAMPURA BASELINES — PLACE FAILS WHEN OBJECT > CELL (split fix)
================================================================================
Status:   Plan A [DONE on main 2026-05-09 (later)] — auto-tune cell
          size landed in test_full_pipeline.main.  Plan C [PENDING on
          audit-64-tampura-binary-grid] — continuous-pose placement
          (the actual TAMPURA-faithful approach).
Priority: TIER 1 — correctness blocker for the uniform baseline.
          Without this, --baseline uniform with default cell size
          0.05 m cannot ground occluder-relocation place actions
          (default occluders are 5-7 cm wide, 12-16 cm tall; no
          uniform cell satisfies boxel_fits).  The 2026-05-09
          stalled run (logs/run_2026-05-09_10-48-13/) was a direct
          consequence: PDDLStream looped at increasing complexity
          searching for a plan that cannot exist.
Where:    Two-branch fix.  Plan A on `main` (the free-space-only-
          swap baseline), Plan C on `audit-64-tampura-binary-grid`
          (the TAMPURA-faithful baseline).  ONE shared issue body
          across both branches: edit on main, merge forward.
          Plan A:  test_full_pipeline.main (auto-tune cell size).
          Plan C:  pddl/domain_pddlstream.pddl + pddl/stream.pddl
                   + streams.py (sample_place_pose) +
                   pddlstream_planner._build_init + execution.py
                   place handler.
Depends:  None — independent of in-flight work.

What:  Under --baseline uniform, free cells are 5 cm cubes
       (default --uniform-cell-size 0.05).  test_boxel_fits
       (streams.py:350-371) compares full AABB extents of the
       OBJECT vs the candidate destination cell.  Default scene
       occluders are 5-7 cm wide and 12-16 cm tall (boxel_env.py:
       296-317), so for any 6-7 cm occluder the comparison
       0.05 >= 0.07 fails on every uniform cell.  Result: zero
       (boxel_fits occluder ?cell) atoms emit; the PDDL place
       action's precondition is unsatisfiable; any plan that
       requires relocating an occluder fails to ground.

       Quantified from the 2026-05-09 stalled run log:
       - 2480 cells emitted under uniform 0.05 m
       - 2472 boxel_fits atoms (~25% of 4 visible obj × 2470
         free cells = ~9880 candidate pairs)
       - The passing 25% are the 4 cm targets fitting in 5 cm
         cells; the failing 75% are 6-7 cm occluders
       - PDDLStream stalled at iter-1 (Cost: inf, Search Time
         0.010 s) because no skeleton can satisfy place

       Three options were considered:
         (A) Auto-tune cell size to fit the largest visible AABB
             (+1 cm headroom for PyBullet contact margin).
             ~30 LOC patch in test_full_pipeline.main; no domain
             change; cuts cell count ~8x as a side-effect (also
             fixes the grounding-cost half of the stall).  Cell
             size becomes scene-dependent: typical default scene
             -> ~0.10 m; smaller-target scenes -> ~0.05 m stays.
             Lands on `main` (the free-space-only-swap baseline).
         (B) Multi-cell place — symbolic place over a CLUSTER of
             cells covering the object footprint.  Requires PDDL
             action change + new "anchor cell" semantics.  RULED
             OUT 2026-05-09: combinatorial blowup (planner picks
             a SET of cells); not TAMPURA-faithful; not cheap.
         (C) Continuous-pose place — replace boxel-indexed place
             with TAMPURA-style continuous SE(3)-pose placement.
             Stream samples (x, y, z) within the free volume,
             returns a Pose certificate; place action takes ?p
             (Pose) instead of ?b (Boxel).  Major domain rewrite;
             the actual TAMPURA approach (Curtis et al. 2024 —
             find_dice/env.py uses continuous poses).  Lands on
             `audit-64-tampura-binary-grid`.

Fix:   SPLIT ACROSS TWO BRANCHES, ONE SHARED AUDIT BODY (this entry).

       On `main` (the free-space-only-swap baseline)
       ---------------------------------------------
       Plan A — auto-tune cell size.  Single commit:

       In test_full_pipeline.main, after env.use_uniform_grid is
       set, compute auto_cell = max(visible AABB extent across all
       scene objects) + 0.01 m.  If auto_cell > the user-supplied
       --uniform-cell-size, call env.set_uniform_cell_size(
       auto_cell) and log the bump.  Preserves user override
       (passing --uniform-cell-size 0.20 stays at 0.20 — only the
       default 0.05 is auto-bumped).  Smoke-run the previously-
       stuck scene (--scene default --baseline uniform --seed 0)
       to verify it succeeds.

       On `audit-64-tampura-binary-grid` (the TAMPURA-faithful baseline)
       -----------------------------------------------------------------
       Plan C — continuous-pose placement.  Multi-commit, in order:
       (i)   pddl/domain_pddlstream.pddl: place action takes ?p
             (Pose) instead of ?b (Boxel); pose-based precondition
             stream replaces boxel_fits.
       (ii)  pddl/stream.pddl: new sample-place-pose stream that
             yields valid free-volume poses per object (uniform
             rejection sample within table_x_range x table_y_range
             x table_z, AABB-checked against current OBJECT/SHADOW
             set).
       (iii) streams.py: implement sample_place_pose; modify
             compute_kin_solution to accept a Pose argument for the
             place case (pose.position + grasp_offset = EE target).
       (iv)  pddlstream_planner._build_init: drop boxel_fits and
             obj_at_boxel emission for free cells under uniform
             mode (free cells become a visualisation aid only;
             placement is continuous).
       (v)   execution.py place handler: read ?p (Pose) from the
             action params instead of ?b (Boxel).
       (vi)  Audit DONE entries for both A and C at landing.

       Ordering note: A landing on main + C landing on audit-64
       are independent; either can land first.  When audit-64
       eventually merges back to main (post-thesis or per
       supervisor direction), the planner has BOTH placement modes
       — boxel-indexed for semantic baseline, pose-indexed for
       tampura baseline.

Care: The ONE-SHARED-AUDIT-BODY constraint means this issue body
       appears IDENTICALLY on main and audit-64-tampura-binary-grid.
       Update from main and merge forward to audit-64; avoid
       divergent edits to the issue body.  The audit changelog at
       the top of CODEBASE_AUDIT.txt MAY differ between branches —
       that's history, and main/audit-64 have different DONE tracks
       for #64 etc.

       Plan A is the smaller of the two and should land first to
       unblock --baseline uniform smoke runs and the eval matrix.
       Plan C is the larger commit and the genuine TAMPURA-
       faithful contribution.

Related: #50 — planner perf investigation.  Plan A's cell-size
              bump cuts grounding cost ~8x as a side effect; #50
              loses one of its hotspots after Plan A lands.
         #62 — replan-while-holding still slow.  Same grounding-
              cost lineage as #50; Plan A indirectly mitigates.
         #64 [DONE on audit-64-tampura-binary-grid] — TAMPURA-
              faithful uniform-baseline GUI framing (visualizer
              uniform_mode flag).  Plan C is the natural
              continuation: visualizer change was cosmetic; this
              is the placement-semantics change.
         #65 — boxel -> hanixel rename.  Coordinate landing
              order (per #65 Care): merge audit-64 back to main
              first, OR rebase audit-64 after the rename, to
              avoid identifier-collision merge noise.


================================================================================
#68. SCENE RANDOMIZER — VARY THE (OCCLUDER, HIDDEN-TARGET) PAIR COUNT PER RUN
================================================================================
Status:   OPEN.  Raised 2026-05-10 (later) by user as "next top
          prio issue".  No commits yet.
Priority: TIER 0 — top priority per user direction.  Promotes
          ahead of the previously-empty TIER 0 slot and ahead of
          #66 Plan C (TIER 1, audit-64-tampura-binary-grid)
          because the scene-variability gap blocks the kind of
          qualitative evidence the user wants to see in single GUI
          runs ("every run looks meaningfully different") and
          would also broaden the eval matrix without adding
          another explicit axis.
Where:    Primary:
            boxel_env.py — scalability_scene() and
              _hidden_xy_positions() / _random_xy_positions()
              (audit #29 placement helpers).  The randomizer
              either (a) lives here as a new factory
              (e.g. random_pairs_scene(min_pairs, max_pairs,
              seed, ...)), or (b) extends scalability_scene with
              a "count is itself random" mode toggled by passing
              None for the explicit counts.
          CLI surface:
            run_logger.py::parse_args — new flag(s) to drive the
              randomizer.  Candidate designs in the Fix section.
          Eval integration:
            eval_runner.py::SCALABILITY_MATRIX / DEFAULT_MATRIX —
              optional new matrix preset that uses the randomizer
              instead of cartesian-product over fixed counts.
              MAY be deferred to a follow-up commit; the GUI-run
              experience is the primary deliverable.
          Tests:
            none currently — manual GUI verification per the
              workflow note in Care.
Depends:  None.  Independent of in-flight #66 Plan C and #50/#62
          perf work.  Does NOT depend on #65 (boxel -> hanixel
          rename) — touches scene-generation surface area that #65
          will mechanically rename later.

What:  Today's scene-generation surface produces the SAME pair
       count on every invocation:

         scalability_scene(n_occluders, n_targets, seed, n_hidden)

       takes the counts as required CLI inputs (run_logger.py
       --n-occluders / --n-targets / --n-hidden), and the
       eval_runner matrix sweeps those axes DETERMINISTICALLY
       across cells: cell occ3_tgt4_hid2 always has exactly 3
       occluders and 4 targets with 2 hidden, regardless of seed.
       Only the per-object XY positions vary with --seed.

       This means:
       (a) Two consecutive `run_boxels --scene scalability` GUI
           runs produce the SAME silhouette count and qualitative
           difficulty.  Without changing CLI args, the user sees
           the same scene shape every time — only object
           positions differ.  This is fine for unit-test-style
           regression seeds, but undermines any "look at this
           variety of scenes" demonstration.
       (b) The eval matrix conflates "scenes vary structurally"
           with "scenes vary in placement".  The thesis Results
           plots currently sweep the count axis as a deliberate
           independent variable, but there is no within-axis
           randomization at the count level — every n_occluders=3
           cell is structurally identical to every other
           n_occluders=3 cell.

       The user wants a randomizer where the COUNT itself is a
       random variable per run, drawn within a CLI-configurable
       range, so that every invocation produces a structurally
       distinct (occluder, hidden-target) pair budget.  "Pair"
       here means: one occluder casting a shadow that has at
       least one hidden target inside its frustum (audit #29
       guarantees this at spawn time via raycast verification).

Fix:   USER DIRECTION 2026-05-10:
       "Let the AI do the work of writing the scene randomizer
        and then ask the user to test it out and wait for
        response."

       Implementation outline (AI proposes, user validates the
       choice via GUI smoke before any commit lands):

       (1) Decide the API.  Two candidates, AI picks one and
           justifies inline in the commit message:

           (A) New scene preset.  --scene random-pairs.
               scalability_scene gets a sibling
               random_pairs_scene(min_pairs, max_pairs, seed,
               extra_distractors=0) in boxel_env.py.  CLI gets
               --n-pairs-range MIN-MAX (default e.g. "1-5").
               Pros: zero impact on existing scalability matrix;
               new flag is self-documenting; eval_runner can
               leave SCALABILITY_MATRIX untouched and add a new
               RANDOM_MATRIX preset later.
               Cons: scene plumbing in boxel_env.py grows by
               another factory; CLI surface widens.

           (B) Extend scalability_scene with optional ranges.
               --n-occluders / --n-targets / --n-hidden start
               accepting either a single int (today) or a
               MIN-MAX range string ("1-5") that is sampled per
               run from the seeded RNG.  No new --scene preset;
               no new factory.
               Pros: reuses existing scene preset; minimal CLI
               surface delta.
               Cons: argparse type goes from int to a custom
               parser; the eval matrix must distinguish "axis
               value 3" from "axis value 1-5" semantically.

           AI default recommendation: (A) for clean separation of
           concerns and zero risk to existing eval cells.  User
           may direct (B) instead.

       (2) Hook the new factory / mode into the existing
           hidden-target placement guarantee (audit #29
           raycast verification).  The randomizer must NOT
           bypass the guarantee: every spawned hidden target
           must still pass camera-ray occlusion at spawn time.
           Reuse _hidden_xy_positions() with the sampled count.

       (3) Hook into the audit #67 fix (Phase 4 uses camera-ray
           occlusion).  Since #67 already removed the AABB-
           containment dependence, any scene the randomizer
           produces should round-trip through Phase 4 cleanly.
           No changes to test_full_pipeline.py expected.

       (4) GUI smoke verification (USER STEP).  Before declaring
           DONE, AI runs the new flag in a single
           `run_boxels --scene <new>` invocation, captures the
           Phase 1/4 logs and the spawn count, and posts the
           output to the user.  User runs the same command on
           their end at least 2-3 times, confirms the count
           varies between runs and that each run completes
           through to a planner call (no silent aborts).  AI
           waits for explicit user OK before committing the
           [DONE 2026-05-XX] entry.

       (5) Optional follow-up: add a new eval_runner matrix
           preset (e.g. "random") that uses the randomizer
           instead of fixed counts.  Bundle in same commit if
           the surface change is small; defer to a follow-up
           commit otherwise.

Care:  WORKFLOW (per user direction 2026-05-10):

       This issue is explicitly framed as: AI implements, USER
       tests the output via a real GUI run, AI waits for the
       user's response before declaring the issue DONE.  Do NOT
       self-mark this issue [DONE] based on a `--no-gui` smoke
       run from the AI's side alone — the failure mode is
       exactly the kind of "looks fine in headless, looks weird
       in GUI" drift that benefits from human verification.

       Concretely:
       (i)   AI writes the implementation, runs `python -c
             "import boxel_env; ..."` import-smoke, and runs ONE
             headless invocation to confirm Phase 1/4 logs look
             sensible.
       (ii)  AI posts the diff summary + the headless-run logs
             to the user as part of the implementation reply.
             AI does NOT push or commit yet.
       (iii) User runs `run_boxels --scene <new-preset>` (or
             the equivalent new flag) 2-3 times in the GUI,
             confirms scenes differ structurally between runs.
       (iv)  User responds with "looks good" / "fix X" / etc.
       (v)   AI either commits + adds [DONE] entry (on user OK)
             or iterates (on user feedback).

       This explicit hand-off is preserved in this Care block
       so future audit slim passes don't lose the workflow
       expectation.  Other audit issues (e.g. #67 single-commit
       fix on AI direction) are NOT bound to this workflow —
       this is a per-issue user request.

Related: #29 [archived] — hidden-target placement raycast
              verification.  The randomizer must call this
              guarantee on every sampled count; do not bypass.
         #67 [DONE 2026-05-10] — Phase 4 camera-ray occlusion
              fix.  The randomizer benefits from the post-#67
              clean Phase 4 path and should not need any
              test_full_pipeline.py change.
         #9 [DONE 2026-05-08] — eval runner.  Optional follow-up
              matrix preset using the randomizer; out of scope
              for the primary commit.
         #59 — scaled-up find-and-stack with N hidden targets
              (very low priority).  The randomizer is a
              prerequisite for any "random N targets, all
              hidden" find-and-tray-stack scaling demo, but
              implementing #59 itself stays deferred.


================================================================================
#69. PYBULLET rayTestBatch — PASS numThreads ON THE 5 EXISTING CALL SITES
================================================================================
Status:   OPEN.  Raised 2026-05-10 (later) after web search for
          PyBullet parallelism support (logs in chat history).
Priority: TIER 3 — low priority, low effort, measurable win.
          Promoted from the FOR LATER "Parallel FastDownward"
          bullet to a numbered issue because (unlike the
          speculative FD/GPU angle) this is just an unused
          PyBullet feature with a 1-kwarg patch.
Where:    Five rayTestBatch call sites, all currently single-
          threaded:
            boxel_env.py:1074  oracle_detect_objects
                               — 8 rays (camera→AABB corners)
                               × N visible objects per scene
            boxel_env.py:1475  compute_shadow_blockers
                               — 5×5 ray grid per shadow
            shadow_calculator.py:111  ShadowCalculator
                               — back-corner rays per occluder
            execution.py:113   sense_shadow_raycasting
                               — 7×7 grid × 3 Z levels
                               = 147 rays per sense action
            execution.py:212   sense_shadow_raycasting
                               (second batch in same handler)
Depends:  None.  Independent of #50/#62 perf hunts (different
          layer — those are Python-side stream/init redundancy).

What:  PyBullet's rayTestBatch accepts a numThreads kwarg
       (added upstream in bullet3 PR #1760).  Rays are dispatched
       across threads from a shared work queue (greedy schedule —
       per-ray cost varies a lot, so pre-distribution is worse).
       None of the 5 call sites in this codebase pass it, so we
       single-thread every raycast.

       Raycasting is on the planner-call hot path:
         - oracle_detect_objects fires once per Phase 1 visibility
           check and once per Phase 4 oracle re-check.
         - compute_shadow_blockers fires once at scene setup and
           per replan (registry rebuild).
         - sense_shadow_raycasting fires once per sense action;
           147 rays per call is the largest single batch.

       Independent measurement of the wall-clock share comes
       from timing_summary.json (#9 instrumentation), but the
       147-ray sense batch alone is large enough to benefit from
       4-8 threads on a modern laptop CPU.  Phase 1 scenes with
       many visible objects multiply by N for that one call too.

Fix:   ONE-LINE PATCH PER CALL SITE.

       (1) Add numThreads kwarg to all 5 rayTestBatch invocations.
           Default to 0 (= "Bullet picks max available threads",
           per upstream docs) so behavior auto-scales with the
           host's core count.  Allow override via a single
           SetPhysicsEngineParameter-style runtime knob if the
           eval matrix wants to compare 1-thread vs N-thread
           cells (worth doing; it doubles the matrix axis cost
           but produces a clean "yes-this-actually-helps" plot
           for the THESIS_NOTES side).

       (2) Verify the thread-safety constraint: rayTestBatch
           cannot run concurrently with stepSimulation on the
           same physics client.  Our code paths are sequential
           (one phase at a time), so no concurrent risk — but
           document it in a comment near each call site so a
           future refactor doesn't introduce one.

       (3) Smoke run on default scene + scalability scene with
           the new flag; confirm ray hit results are identical
           between numThreads=1 and numThreads=N (greedy
           scheduling preserves results, only ordering of
           hit-collection changes within the batch — we already
           consume the result as a list indexed by ray id, so
           order-independence holds).

       (4) Optional — re-run a small eval cell (smoke matrix)
           and compare timing_summary.json planning_time_s
           before/after.  If the win is measurable, mention in
           THESIS_NOTES §17 (or a new §22) as a "free wall-clock
           reduction independent of the discretization-strategy
           comparison being measured."

Care:  - PyBullet's GPU rigid-body pipeline is dormant (Bullet
         3.x has an OpenCL pipeline but it was never wired into
         PyBullet — see FOR LATER bullet for the FastDownward /
         PyBullet GPU summary).  Don't conflate this issue with
         "go GPU" — this is purely CPU thread parallelism within
         a single rayTestBatch call.
       - Thread-safety footgun (forum-confirmed): "you cannot be
         raycasting from one thread while you are stepping the
         world in another".  Currently fine — every call site
         is sequential w.r.t. stepSimulation — but the Care
         note above prevents accidental future regressions.
       - This is NOT a substitute for #50/#62 (Python-side perf
         hunt).  Different layer entirely.  Land both.

Related: #50/#62 — Python-side perf hunt; raycast multithreading
              is a different layer of the same wall-clock budget.
         #67 [DONE 2026-05-10] — Phase 4 camera-ray occlusion
              swap.  Phase 4's oracle_detect_objects call was
              one of the audit-#67-touched code paths; it is
              also a numThreads candidate (same boxel_env.py:1074
              call site).
         #9 [DONE 2026-05-08 (later)] — timing_summary.json
              instrumentation.  Without #9 the before/after
              measurement above wouldn't be possible.
         FOR LATER bullet "Parallel FastDownward / GPU sim" —
              this issue is the realistic CPU-side spinoff;
              the bullet covers the speculative angles
              (FD parallelisation, GPU sim swap to Isaac /
              MuJoCo MJX / Genesis) that stay out of scope.


================================================================================
FOR LATER
================================================================================
 backlog (not scheduled — cross-reference open/resolved/deferred audits):

- Incremental boxel recomputation after occluder relocation (optimisation of
  reboxelize_free_space / octree pass).  Shadow AABB staleness is an accepted
  thesis gap; see archive/CODEBASE_AUDIT_RESOLVED.txt (#4) and THESIS_NOTES.md.

- Physics-based pushing instead of pick-and-place for heavy objects.  Push
  logic remains commented out in-repo (historical experiment).

- Pick–observe–replace (clear corridor, sense, place object back).  Raised
  2026-03-22; not modeled in PDDL.  Related reactive behaviour lives in
  archive resolved #21 (still_blocked / search policy; log-only band-aid
  landed 2026-05-05) — the principled re-grounding fix (#47) was
  deferred out of scope 2026-05-06, so this bullet is purely conceptual.

- "Approach 2": PDDL-level merge/split with active representative boxels.
  Superseded by opaque re-boxelize + replan (Approach 1).  Full semantic merge
  condition: deferred #2 in archive/CODEBASE_AUDIT_DEFERRED.txt.

- Dense camera→AABB visibility lattice (64-ray variant): deferred #14 — branch
  audit-14-dense-lattice-attempt; precision vs planner grounding cost.

- Multi-boxel `place` (old open #6): removed from scope 2026-05-03 — not pursued.

- Hard-gate `:action stack` with a `(stack_allowed)` precondition (only set
  when the goal demands stacking) — fallback if the action-cost bias
  (stack=2, others=1) is insufficient to suppress place-rescue stacks.

- Tighten stack physics check to a safe-inset XY test: centre within
  (support AABB shrunk by half the cube footprint) instead of plain
  center-within-AABB.  Audit #40 introduced the loose check; refinement
  only matters if eval (#12) shows edge-perching false-positives.

- Stack size-compatibility check (raised 2026-05-07 during #52 root-cause
  analysis; #52 dropped 2026-05-08).  Domain :action stack and
  streams.compute_stack_kin_solution currently let any cube stack on any
  other — the planner happily plans big-on-small towers that physics
  fails to stabilise.  Two possible fixes: (i) symbolic
  (compat_stack ?o ?on_obj) static predicate precomputed in _build_init
  from boxel half-extents — CLAIMED BY #59 (open, very low priority);
  (ii) geometric filter inside the placement / stack-kin sampler (skip
  seeds where held footprint exceeds support footprint by more than X%)
  — still parallel future option.  Currently mooted by 40 mm uniform
  targets in default_scene; surfaces if any goal mode mixes sizes.

- Trajectory-clipping edge case for stack actions (audit #55 lazy-
  collision hack residual, raised 2026-05-08).  Commit 9a64aa1 adds
  the support cube body to compute_stack_kin_solution's
  ignored_body_ids so plan_motion can route through where the support
  sits at planning time.  Side effect: the planned trajectory is
  unaware of where the support will be at EXECUTION time (its
  destination on the tray / a free boxel / a previously-stacked cube /
  etc.).  If the destination lies on the planned arm route, the
  actual arm motion could collide with the support at runtime.  Not
  exercised by current goal modes — find-and-tray-stack lands the
  support at the back-left tray corner well outside typical arm
  routes between table-spread cubes; --goal stack uses initial poses
  for all supports (destination = source so no clip risk); --goal
  holding has no stack.  Future fix when needed: branch
  audit-55-pose-aware-stack (Path C) threads correct destination
  poses through compute-stack-kin and resolves this — works but
  costs ~7x baseline planning time due to PDDLStream's exogenous-
  axiom compilation.  Alternative: extend RobotConfig with a
  separate endpoint-only-ignored set so plan_motion path-checks
  honour the support body while endpoint-checks don't.

- POMDP / contingent baseline: large effort; only if time after eval sweep
  (#9–#13).  Thesis-critical fixes are now bounded to #40 [DONE] +
  #36 [DONE] + #49 per ROADMAP; #21 / #47 (still_blocked correctness) are
  closed at the log-only-band-aid level (#47 deferred out of scope
  2026-05-06).

- LW1 / TAMPURA empirical comparison.  Supervisor (sync 2026-05-06):
  "Comparison with lw1 and tampura only after finishing the writing of the
  thesis if I have at least a week of free time."  Conditional on:
  (i) thesis writing done end-to-end, and (ii) ≥ 1 week of free time
  remaining before submission.  Otherwise cite as related work without
  empirical comparison.  Likely scope: harness LW1 / TAMPURA on the same
  scenes used for #12 scalability, reuse #9 metric hooks, run the same
  seeds — no domain re-encoding by us if their published encodings cover
  the task; otherwise drop the comparison rather than shoehorn.

- Parallel FastDownward / GPU robotics sim — post-thesis research
  direction.  Idea raised 2026-05-10 (later) by user; web-searched
  same day for prior work, findings below.  Bullet refined from
  "speculative" to "mostly-solved on CPU; dead-end on GPU for FD
  specifically; sim-side GPU exists but means a different simulator."

  ON THE FASTDOWNWARD SIDE — substantial prior work exists:
    - HDA* / Distributed Fast Downward (Kishimoto, Fukunaga, Botea —
      ICAPS 2009; arXiv 1201.3204) — Hash-Distributed A* as a drop-in
      replacement for FD's sequential A*.  MPI-based; works on shared
      memory, distributed, or mixed.  30-60x speedup on 128 cores.
      Uses Abstract Zobrist Hashing to assign each state to a unique
      process for local duplicate detection.  Live code:
      github.com/jinnaiyuu/distributed-fast-downward (research code,
      not tracking modern FD; targets OPTIMAL A*, not the satisficing
      greedy best-first that PDDLStream's adaptive algorithm uses —
      so a drop-in port to our pipeline is non-trivial).
    - ArvandHerd (IPC 2011 winner, parallel track) — 4 Arvand configs
      + 1 LAMA config running on separate processors with minimal
      cross-process comms.  This is exactly the "FD portfolio mode"
      angle; it works because the "find a meaningful split" problem
      is hard and config-portfolio is the practical workaround.
    - Fast Downward Stone Soup (Helmert et al., ICAPS 2011) and
      Fast Downward Remix — sequential portfolio configurators
      (15 of 38 FD configs, varying time slices).
    - Springer 2021 "Parallel best-first search algorithms for
      planning problems on multi-core processors" — async work-pool
      design with abstraction-based duplicate-detection partitioning;
      maps directly to a "split by CPU thread count" framing.
    - arXiv 2407.21485 (2024) — Parallel Strategies for Best-First
      Generalized Planning; recent active research.

  ON THE GPU SIDE — essentially dead-end for classical PDDL planning:
    - FD's data structures are pointer-heavy and not trivially
      vectorisable; the literature has very few wins.  The most
      likely win is heuristic batching (e.g. pattern-database
      lookups) — narrow, not a general parallelisation.
    - LLM-generated heuristics on GPU (arXiv 2501.18784, 2025) is
      the more promising angle: don't parallelise the search,
      parallelise the heuristic computation.

  ON THE SIMULATOR SIDE — PyBullet GPU is dormant; alternatives
  exist but mean swapping simulator entirely:
    - Bullet 3.x has an OpenCL GPU rigid-body pipeline (Erwin
      Coumans, 2012) — 100k bodies real-time on a Radeon 7970.
      Never wired into PyBullet; maintainer position on GitHub
      issue #3105 is "may be enabled in the future."  Code dormant
      since AMD stopped funding the work in ~2013.
    - For GPU-accelerated robotics sim today: NVIDIA Isaac Sim /
      Isaac Lab (thousands of envs in parallel, Omniverse-based);
      MuJoCo MJX (JAX); Genesis.  All require a full simulator
      swap — out of thesis scope.
    - Within PyBullet itself, the realistic CPU-parallelism win
      is rayTestBatch numThreads — see #69 for the 5-call-site
      one-line patch.

  WHY THIS STAYS FOR LATER (not promoted to a numbered issue):
    - Even if HDA*/MPI was wired into the planner stack, our
      wall-clock perf complaint is upstream of FD search itself
      (see #50/#62 — Python-side stream/init redundancy).  An
      infinitely-fast FD would shrink an already-small slice.
    - The realistic CPU-side win that doesn't require any
      simulator/planner swap is captured concretely as #69
      (rayTestBatch numThreads).
    - GPU is research-grade work measured in months, not days,
      and the literature is thin enough that it's a dissertation
      topic in its own right — not a pre-submission tweak.

  Cross-references: #50/#62 (Python-side perf hunt — same goal,
  different layer); #69 (rayTestBatch numThreads — the realistic
  spinoff).


================================================================================
#70. RANDOM-PAIRS SCENE — UNREACHABLE PLACEMENTS HANG OBJECTS IN AIR
================================================================================
Status:   OPEN.  Raised 2026-05-12 by user after GUI run on a
          random-pairs scene where an occluder/target spawned far
          from the Panda base.  The arm stretched to a borderline-
          reachable IK solution; on lift, the grasp constraint
          anchored the cube in mid-air (visible "hung object"
          artefact in the GUI screenshot the user attached to
          the request).
Priority: TIER 0 — top priority per user direction 2026-05-12.
          Promotes ahead of the previously-empty TIER 0 slot
          (since #68 [DONE 2026-05-10 (later)]).  Blocks
          qualitative use of --scene random-pairs in any thesis
          figure / video demo and silently inflates holding /
          find-and-tray-stack success on borderline cells (the
          plan "succeeds" with a hung cube).
Where:    Root cause:
            boxel_env.py:271-341 (scalability_scene) returns a
              SceneConfig with default constrain_to_reach=False
              (line 336 — no kwarg passed).  Compare stack_scene
              (boxel_env.py:263-268) which explicitly sets
              constrain_to_reach=True.
            boxel_env.py:344-380 (random_pairs_scene) inherits
              this by delegating to scalability_scene unchanged.
          Placement layer:
            boxel_env.py:818-885 (_random_xy_positions) samples
              from _TABLE_PLACE_X_RANGE = (-0.20, 0.60) and
              _TABLE_PLACE_Y_RANGE = (-0.50, 0.50) when
              constrain_to_reach=False (lines 847-848).  The
              reach-disk filter at line 868 is dead code on this
              path.
            boxel_env.py:967+ (_hidden_xy_positions) anchors
              hidden targets to occluder XYs and inherits the
              same wide bounds via lines 1032-1033.  If the
              occluder lands far, the hidden target lands far
              too.
          Constants involved:
            _PANDA_REACH_RADIUS = 0.65          (line 794)
            _SAFE_TABLE_X_RANGE = (-0.1, 0.70)  (line 800)
            _SAFE_TABLE_Y_RANGE = (-0.40, 0.40) (line 801)
            _TABLE_PLACE_X_RANGE = (-0.20, 0.60) (line 815)
            _TABLE_PLACE_Y_RANGE = (-0.50, 0.50) (line 816)
            Geometry: with base at (-0.4, 0.0) and reach 0.65 m,
            the reachable x ceiling is -0.4 + sqrt(0.65^2 - y^2)
            = 0.25 at y=0, 0.18 at y=±0.2, 0.11 at y=±0.4.
            The placement window's x range goes to 0.50 (line
            815) — i.e. ~half the window is outside the reach
            disk for typical y.  This is not a corner-case
            defect: every multi-object spawn is expected to
            land at least one cube outside reach, and user
            GUI runs confirm "almost all" of a 5-cube scene
            land outside reach in practice.
Depends:  None.  Independent of #50/#62 (planner perf), #66 Plan C
          (TAMPURA-style placement), and #65 (boxel -> hanixel
          rename).  Lands directly on main.

What:  The random-pairs randomizer (#68 [DONE 2026-05-10 (later)])
       can spawn occluders, visible targets, and (transitively)
       hidden targets outside the Panda's reach disk.  Concretely:

       (1) scalability_scene constructs its SceneConfig without
           constrain_to_reach=True.  random_pairs_scene reuses
           that config unchanged.
       (2) _create_objects -> _random_xy_positions samples in
           the WIDE _TABLE_PLACE_* bounds, not the SAFE_TABLE_*
           + reach-disk path.
       (3) Pick IK on the spawned object still finds a stretched
           solution because the robot has a few extra cm of
           reach beyond _PANDA_REACH_RADIUS (the constant is a
           comfortable-reach disk, not a hard kinematic limit).
       (4) execute_pick succeeds, the grasp constraint binds,
           but the post-pick lift cannot satisfy both "keep the
           cube at grasp pose" and "respect joint limits" — the
           constraint takes priority, leaving the cube floating
           where the end-effector last reached and the arm in
           an awkward fully-extended stance.  In the GUI this
           reads as "object hung in the air."

       This is functionally identical in spirit to audit #36
       (post-action lift workaround for shotty motion planning,
       supervisor: "not cheating, a workaround"), but the
       failure mode here is upstream: the scene generator
       shouldn't be producing borderline-reachable spawns in
       the first place.  #36 is the safety net; #70 closes the
       hole.

Fix:   Plumb constrain_to_reach into random-pairs at scene
       construction time so every spawned object sits inside
       _SAFE_TABLE_* AND inside the 0.65 m reach disk.

       Two implementation options (AI picks one and justifies
       inline in the commit message; user may direct otherwise):

       (A) Targeted fix — flip random_pairs_scene only.
           random_pairs_scene wraps the SceneConfig returned
           by scalability_scene and overrides
           constrain_to_reach=True (one-line dataclasses.replace
           or in-place mutation).  No change to scalability_scene
           itself.
           Pros: zero risk to existing scalability matrix cells
                 (eval_runner currently invokes scalability_scene
                 directly via --scene scalability with explicit
                 counts — those cells have been validated as
                 reachable empirically for occ<=4 / tgt<=5).
                 Tightest scope.
           Cons: scalability_scene continues to silently allow
                 the same regression if called with larger counts;
                 the scalability matrix could hit this in the
                 future when n_occluders or n_targets scales.

       (B) Wider fix — make scalability_scene reach-bound by
           default.
           scalability_scene gets constrain_to_reach=True hard-
           wired into its SceneConfig return; random_pairs_scene
           inherits via the existing delegation.
           Pros: closes the latent #70 in the scalability matrix
                 too.  Removes the silent-fail mode entirely.
           Cons: re-runs the existing SCALABILITY_MATRIX cells
                 under tighter bounds — could shift placements
                 enough to invalidate already-recorded seeds.
                 Needs a sweep regression check before merging.

       AI default recommendation: (A).  The eval matrix has
       been measured under the current wide bounds; the
       qualitative defect is only observed with random-pairs;
       the targeted fix isolates the regression from the
       existing evaluation surface.  If the user prefers (B),
       bundle a fresh SCALABILITY_MATRIX smoke regression in
       the same commit.

       Hardening (cheap, on either path):
       (i)  After spawn, in _create_objects / _create_targets,
            assert max(|xy - robot_base|) <= _PANDA_REACH_RADIUS
            for every pickable.  Raise a clear placement error
            rather than letting it surface as a hung-cube in
            the GUI.  Keep the existing 400-attempt rejection
            budget; on exhaustion, raise so the retry layer in
            test_full_pipeline.main() re-seeds (mirrors the
            audit #29 hidden-target post-spawn check).
       (ii) Optional: tighten _TABLE_PLACE_*_RANGE to match
            _SAFE_TABLE_*_RANGE for the random-pairs path.
            Skip if the assert in (i) is enough.

Care:  Verification protocol:
       (i)   AI implements either (A) or (B), runs ONE headless
             `--scene random-pairs --seed 0/1/2` to confirm no
             spawn exceeds reach.
       (ii)  AI captures spawn XY logs + max-reach delta and
             posts to user.
       (iii) User runs `run_boxels --scene random-pairs` 2-3
             times in GUI, confirms no object hangs in air
             after pick, no arm-stretch artefact.
       (iv)  Commit + [DONE 2026-05-XX] on user OK.

       This issue inherits the AI-implements-USER-verifies
       workflow from #68 (per-issue user request, not a global
       policy).

Related: #68 [DONE 2026-05-10 (later)] — random-pairs scene
              generator.  This is the post-merge defect report
              against that feature.
         #29 [archived] — hidden-target raycast verification.
              Provides the model for the post-spawn assert in
              hardening (i).
         #36 — post-action lift workaround.  Downstream safety
              net for the same class of "borderline IK +
              constraint" defect; #70 closes the upstream hole.
         #49 [archived] — tray entity (find-and-tray-stack).
              The find-and-tray-stack goal mode exposes this
              defect more often because both the source cube
              and the tray must be reachable.


================================================================================
#71. REFRESH OBJECT BOXEL AABBs ON EVERY SENSE — SHADOW REGIONS UNTOUCHED
================================================================================
Status:   OPEN.  Raised 2026-05-12 (later) by user.  Symptom not
          yet captured in a single GUI screenshot, but the failure
          mode is implied by the find-and-tray-stack runs that
          show cubes drifting from their initial spawn pose
          across multi-action plans (settle, bump from arm
          swing, jiggle on a stack) while the registry's OBJECT
          AABB stays frozen at the spawn-time value.
Priority: TIER 1 — correctness drift.  Promotable to TIER 0 if
          user-observed false-success cases multiply (planner
          uses stale OBJECT AABB to certify a place / stack at
          a pose that physics no longer admits).  Not promoted
          today because #70 (placement reachability) and #66
          Plan C (TAMPURA placement) sit above it on the
          critical path.
Where:    execution.py:handle_sense_action (around line 880-1000
          for the sense_shadow dispatch; the refresh hook lands
          at the end of every outcome branch, BEFORE returning
          ActionResult).  Concretely:
            - sense_outcome == "found_target": refresh, return
            - sense_outcome == "clear_but_empty": refresh, return
            - sense_outcome == "contains_nontarget": the
              existing per-discovery getAABB at line 962 stays;
              after the discovery loop ends, do a full-registry
              refresh so existing OBJECTs that did NOT trigger
              the discovery path also get a fresh AABB.
          Registry walk:
            boxel_data.py:101+ (BoxelRegistry) — use
              get_boxels_by_type(BoxelType.OBJECT) to iterate.
          Object body lookup:
            env.objects[obj_name].object_id supplies the
              PyBullet body id for p.getAABB.
Depends:  None.  Independent of #70 (placement reachability),
          #66 Plan C (TAMPURA), and #50/#62 (planner perf).
          Lands directly on main.

What:  OBJECT boxel AABBs are written at exactly two points in
       the current pipeline:
         (1) Scene creation — _create_objects / _create_targets
             populate registry with p.getAABB(body_id) once,
             immediately after spawn.
         (2) sense_contains_nontarget — when a non-target body
             is discovered inside a shadow, execution.py:962
             pulls a fresh AABB for THAT body only, before
             creating the new OBJECT entry.
       There is no third update.  In particular:
         - After place / stack / pick: the held cube's pose
           changes, but the registry's OBJECT AABB for that
           cube is left at its pre-action value (audit #36's
           hardcoded post-action lift mitigates the visible
           gap, but doesn't refresh the registry).
         - After sense for any other body: a near-miss arm
           swing can nudge a neighbouring cube; physics
           updates the body, the registry does not.
         - After a stack settles: gravity drops the top cube
           a few mm onto its support; the registry's OBJECT
           AABB still claims the cube is at its handoff pose.
       Net effect: the planner emits free / on / clear /
       boxel_fits facts grounded in stale geometry.  The
       physical-failure verifier (#40 [DONE]) catches the
       worst false-success at goal time, but every replan
       between sense actions is reasoning over potentially-
       stale OBJECT boxels.

       USER DIRECTION (explicit): refresh ONLY the OBJECT
       boxel's AABB.  Do NOT recompute the shadow region
       cast by that object.  Shadow recomputation is the
       expensive path (shadow_calculator.py traces camera
       rays through every revised AABB) and is out of scope
       for this fix.  Document the resulting inconsistency
       in the commit message: OBJECT AABB will be fresh,
       SHADOW boxel keyed to that object may now be slightly
       mis-aligned with the live silhouette.  Acceptable
       trade per user.

Fix:   Add a helper in boxel_env.py or execution.py:

         def refresh_object_aabbs(env, registry, viz=None):
             """Refresh every OBJECT boxel's AABB from live
             PyBullet.  Shadow boxels are NOT recomputed
             (audit #71 — user-explicit scope cut)."""
             for obj_boxel in registry.get_boxels_by_type(
                     BoxelType.OBJECT):
                 obj_info = env.objects.get(obj_boxel.id)
                 if obj_info is None:
                     continue
                 bid = obj_info.object_id
                 aabb_min, aabb_max = p.getAABB(bid)
                 obj_boxel.aabb_min = np.array(aabb_min)
                 obj_boxel.aabb_max = np.array(aabb_max)
                 if viz is not None:
                     viz.refresh_object_viz(obj_boxel.id)

       Call it at the end of every sense_outcome branch in
       handle_sense_action, right before the return.

       Optional follow-up (defer to a separate commit unless
       trivially small):
       (i)  Same helper called at the end of execute_place /
            execute_stack.  Out of scope for the primary
            commit per user-explicit "on every sense" scope.
       (ii) A second commit closes the SHADOW-staleness gap
            by re-running shadow_calculator on the refreshed
            AABBs.  Separately tracked because it doubles
            the sense-action cost; promote only if eval shows
            stale-shadow drift hurting success rates.

Care:  Verification:
       (i)   Smoke test: log OBJECT boxel AABB deltas before/
             after each sense.  Confirm deltas appear for
             cubes near the active arm path and are zero
             for cubes the arm did not touch.
       (ii)  Regression: run the existing eval matrix with the
             refresh enabled.  Success rates should not
             regress; if they improve on holding / find-and-
             tray-stack, that's the audit #40 false-success
             tail closing.
       (iii) Performance: p.getAABB is cheap (~10 us each);
             a 10-object scene adds ~0.1 ms per sense.  Not
             expected to register on planner perf budget.

Related: #29 [archived] — hidden-target placement raycast
              verification.  Spawn-time AABB capture is the
              "once" channel that #71 extends to "every
              sense".
         #36 — post-action lift workaround.  Mitigates the
              visible drift but does not refresh registry.
         #40 [DONE 2026-05-05] — physical-failure verifier
              on (on a b) writes and at goal.  Catches the
              worst false-successes #71 would otherwise let
              through; #71 reduces how often the verifier
              has to fire.
         #57 — registry should own cube geometry (replace
              PyBullet AABB fallback).  Long-term direction;
              #71 is the short-term refresh inside the
              current "registry mirrors PyBullet" model.


================================================================================
#72. SHADOW AABB CARVED ONLY ALONG DOMINANT AXIS — LATERAL OVERHANG
================================================================================
Status:   OPEN.  Raised 2026-05-12 (later) by user after GUI
          run on random-pairs scene (seed 779694423).  User
          reported "shadow calculation is wrong" — visible as
          shadow wireframes that extend laterally past the
          occluder on the camera-facing side, into regions the
          camera can directly see.
Repro:    Exact run that surfaced the defect (preserve for
          before/after comparison when the fix lands):
            python run_logger.py \
              --scene random-pairs \
              --n-occluders 3 \
              --seed 779694423 \
              --goal find-and-tray-stack \
              --stack-height 2 \
              --baseline semantic \
              --gui
          Derived counts (random-pairs from seed): n_targets=3,
          n_hidden=2, n_extra_distractors=1.  Camera:
          eye=(0.1, -0.8, 0.7), target=(0.1, 0.0, 0.5).  Scene
          objects (spawn order, body ids 4-9): red, green,
          orange, blue, cyan, purple.  Occluder shadows
          visible: shadow_of_{green, orange, purple}_object.
          Original run log:
            logs/run_2026-05-12_14-02-30/run_2026-05-12_14-02-30.log
          (the run also surfaced #73 — plan_motion stuck on
          stale held=[9] for 385 s before the user killed it;
          unrelated bug, same scene.)
Priority: TIER 1 — correctness drift.  Same tier as #71 because
          the consequences are similar: planner reasons over
          geometry that does not match physics.  In this case
          the SHADOW boxel claims to occlude volume that is
          actually visible, so free-space generator + boxel_fits
          carve out non-shadow cells.  Downstream effect: place /
          motion plans avoid passable regions, contributing to
          the kind of long sampling stalls seen in the #73
          held-set bug.
Where:    shadow_calculator.py:147-178 (calculate_shadow_boxel,
          AABB-construction + subtract-object block).  Concretely:
            - Lines 147-156: full_min/full_max enclose both the
              object's AABB and ALL ray hit points.  Hit points
              spread laterally because rays from the 4 back
              corners diverge outward from the camera through
              each corner.
            - Lines 168-178: only ONE face is trimmed back to
              the object's back face — the face along the
              dominant axis of shadow_dir.  The two perpendicular
              axes keep the union of (object range) and (hit-
              point range), so the shadow AABB extends past the
              object's lateral footprint all the way from the
              object's back face out to the hit-point cluster.
          Visual evidence:
            User GUI run 2026-05-12 — wireframes labelled
            "shadow_of_<colour>_object" extend ~7-10 cm beside
            the corresponding occluder on the camera-facing side
            (-X for the run shown, because camera is offset to
            +X relative to the occluder cluster).
          Geometric evidence:
            For a 5 cm cube at world (0, 0, 0.55), camera at
            (0.1, -0.8, 0.7), the four back-corner rays land at:
              (-0.043, 0.143, 0.5)
              ( 0.014, 0.143, 0.5)
              (-0.100, 0.519, 0.5)
              (-0.020, 0.521, 0.5)
            full_min/full_max gives shadow X range [-0.100,
            0.025]; the object's X range is [-0.025, 0.025].
            The strip X in [-0.100, -0.025] at any Y is
            classified as shadow but is camera-visible.
Depends:  None.  Independent of #70 (placement reachability),
          #71 (OBJECT AABB refresh), #66 Plan C (TAMPURA), and
          #50/#62 (planner perf).  Lands directly on main.

What:  The shadow AABB is constructed in three steps:
       (1) Cast rays from the 4 back corners (those with
           positive dot-product to cam_to_obj) outward through
           each corner.  Rays diverge laterally because they
           originate at distinct corners.
       (2) Build the bounding box of (object AABB ∪ hit points).
           This box is wider than the object in BOTH lateral
           axes because the hits land at wider X (and slightly
           wider/higher Z, since top-back-corner rays hit
           further than bottom-back-corner rays).
       (3) Carve the object out of the shadow ALONG ONE AXIS
           only (the dominant component of shadow_dir):
             if shadow_dir[dom_axis] > 0:
                 s_min[dom_axis] = max(s_min, o_max[dom_axis])
             else:
                 s_max[dom_axis] = min(s_max, o_min[dom_axis])
           This trims the shadow's near face back to the
           object's back face — correct for the dominant axis.
           But the perpendicular axes keep the union bounds.

       Geometrically, the true shadow is a frustum from the
       camera through the occluder, and any axis-aligned box
       containing it must overestimate.  The current code
       OVERESTIMATES along both perpendicular axes ALL THE
       WAY BACK to the occluder, instead of tapering toward
       the occluder's own lateral footprint at the back face.

       Visible failure: the shadow boxel "wraps around" the
       occluder on the camera-facing lateral sides.  In the
       reported run (seed 779694423, three occluders), each
       shadow boxel includes 7-10 cm of clearly-visible
       table beside its caster.

       Downstream planner consequences:
         - free_space.py / uniform_grid.py subtract SHADOW
           AABBs from the workspace -> free cells are deleted
           in the overhang strip beside each occluder.  The
           robot is forbidden from placing in cells that are
           actually free.
         - pddlstream_planner.py boxel_fits checks SHADOW
           AABBs as "occluded" for visibility planning -> the
           camera "cannot see" cells that it actually can.
         - In multi-occluder scenes (random-pairs N>=3), the
           overhang strips of adjacent occluders can merge,
           creating contiguous "occluded" regions that the
           planner believes to be one big shadow when in
           fact two separate cones exist.

Fix:   Three fix shapes, AI picks one and justifies inline.
       User direction on which option to take pending.

       (A) Per-slab AABBs along the dominant axis.
           Split the shadow into N slabs perpendicular to
           dom_axis (e.g. N=4).  Each slab gets its own
           tight lateral bounds from the rays' actual
           lateral extent at that depth (interpolate between
           object back-face bounds at slab nearest the
           occluder, and full hit-point bounds at slab
           furthest).  Output: N SHADOW boxels per occluder
           instead of 1.
           Pros: cleanest geometric tightening; works in
                 the existing AABB-only planner; degrades
                 gracefully (N=1 -> current behaviour).
           Cons: registry has N x more shadow entries per
                 occluder; PDDL fact emission grows
                 accordingly (audit #50 perf concern).
           Recommended: N=2 (one cube-bordering slab + one
                 far slab); equivalent to option C below.

       (B) OBB or frustum shadow boxels.
           Replace SHADOW AABBs with oriented bounding
           boxes (OBBs) or actual frustums.  Tighter, but
           breaks the AABB-only assumption baked into
           boxel_fits, _check_aabb_intersection, the free-
           space generator, and the PDDL stream certificates.
           Pros: geometrically exact.
           Cons: pervasive type change across the planner
                 stack.  Out of scope for this fix.

       (C) Two-slab carve.
           Equivalent to A with N=2.  After the dom_axis
           carve, ALSO clamp the perpendicular axes to the
           OBJECT's perpendicular range from the object's
           back face up to halfway to the far hit-point
           cluster, then expand to the full hit-point range
           only beyond that midpoint.  In practice this
           means:
             - SHADOW boxel 1: cube-bordering slab, lateral
               bounds = object's lateral range
             - SHADOW boxel 2: far slab, lateral bounds =
               hit-point range
           Pros: only one extra SHADOW boxel per occluder;
                 closes the visible defect; same AABB
                 representation as today.
           Cons: registry has 2x shadow entries (manageable);
                 the midpoint heuristic is arbitrary (a
                 perspective-correct split would compute the
                 frustum cross-section at the midpoint
                 depth, which is also cheap).

       AI default recommendation: (C).  One extra SHADOW
       boxel per occluder is acceptable overhead; closes
       the user-visible defect; no PDDL stream surface
       changes.  Implement the midpoint split with a
       perspective-correct cross-section (cheap closed form
       given camera_pos, occluder AABB, and slab depth) so
       there's no arbitrary half-and-half heuristic.

       Verification:
       (i)   Headless smoke: log shadow AABBs before/after
             for the seed 779694423 random-pairs run.  Expect
             lateral overhang to drop from ~7-10 cm to <= 2 cm
             on each occluder.
       (ii)  GUI: user reruns the same seed (see Repro
             block above); visually confirm the shadow
             wireframes no longer extend past the occluders'
             lateral footprint near the back face.
       (iii) Regression: existing scalability matrix should
             not regress on success rate (the fix removes
             false-positive shadow cells; if any cell starts
             succeeding that previously failed because of
             that, that's the fix working).

Care:  AABB-only planner assumption.
       Boxel_fits, free-space generator, _check_aabb_
       intersection, and PDDL certificates all assume SHADOW
       boxels are AABBs.  Fix (C) preserves this; fix (B)
       would require a wider refactor.  Stay in (A)/(C)
       unless user explicitly directs (B).

       Shadow ID continuity.
       Today each occluder produces ONE SHADOW boxel with id
       "shadow_of_<object>".  Splitting into 2 boxels needs
       a naming scheme: "shadow_of_<object>__near" /
       "__far", or numeric suffix.  Match the existing
       fragment-naming convention in _subtract_aabb (line
       208) so reboxelize / belief / sense don't need new
       lookup paths.

       sense_shadow_raycasting (execution.py:44).
       The sense action takes one shadow_boxel and casts
       rays.  With two slabs, the sense action would need to
       either (a) walk both slabs in one call, or (b) treat
       them as two PDDL "regions" that the planner senses
       separately.  (a) is cheaper and preserves current
       semantics; (b) is more honest but doubles sense
       counts.  Default to (a) — concatenate the two slabs'
       interiors before raycasting.

Related: #4  [archived, accepted] — post-action re-
              boxelization staleness.  #72 is a different
              flavour of "SHADOW boxel inaccuracy"; #4 was
              about staleness, this is about original
              construction.
         #71 — refresh OBJECT boxel AABBs on every sense.
              Companion correctness drift.  Together #71
              + #72 close the two main "registry geometry
              vs reality" gaps; #57 is the long-term
              consolidation.
         #50 — planner performance investigation.  Doubling
              SHADOW boxel count is a real cost — flag this
              fix as a candidate for #50's cost-budget
              consideration before landing.
         #73 — plan_motion stream evaluated with stale
              held=[purple] after stack(purple, tray).
              Surfaced in the same seed-779694423 run; the
              overhang-shadow free-space carve-out makes
              the held-set bug bite harder by removing
              passable cells the motion sampler would
              otherwise have used.


================================================================================
SUMMARY — OPEN ISSUES BY PRIORITY (updated 2026-05-12 (later))
================================================================================

Aligns with the ROADMAP banner above (pre-eval → eval prerequisites → eval).
Issue numbers are chronological (audit discovery order), NOT priority order —
follow the tier / roadmap ordering, not the lowest open number.
Each issue is ideally one commit; parallelise where dependencies allow.

The pre-evaluation correctness chain ended 2026-05-08 with #60 [DONE].
Eval (#9, #10) was the user's critical path to the thesis Results
chapter and was promoted from TIER 3 to TIER 1 in the 2026-05-08 (later)
restructure; both landed 2026-05-08.  #67 [DONE 2026-05-10] closed
the silent-abort failure mode that was inflating eval no_summary
counts.  TIER 0 repopulates with #68 (scene randomizer) per user
direction 2026-05-10 (later).  #70 (random-pairs unreachable
placements) added 2026-05-12 as a TIER 0 post-merge defect
report against #68 — placement-bound regression that hangs
picked cubes in mid-air; closes ahead of the #68 polish item.

TIER 0 — TOP PRIORITY:
  #70  Random-pairs scene — unreachable placements hang objects
       in air.  scalability_scene returns SceneConfig with
       constrain_to_reach=False (no kwarg passed at line 336),
       so random_pairs_scene inherits the wide _TABLE_PLACE_*
       sampling window with no reach-disk filter.  Robot base
       at (-0.4, 0.0), reach 0.65 m, placement-window x ceiling
       0.50 — roughly HALF the window is unreachable; user GUI
       runs report "almost all" of a 5-cube scene land outside
       reach.  Pick IK still solves on the slack, grasp
       constraint binds, post-pick lift wedges the cube in
       mid-air with the arm fully extended.  AI implements
       (option A or B in the body); user verifies in GUI;
       AI waits for OK before [DONE].
  #68  Scene randomizer — vary the (occluder, hidden-target) pair
       count per run (within a CLI-configurable range), not just
       per-object placement.  Today scalability_scene takes fixed
       --n-occluders / --n-targets / --n-hidden counts so two
       consecutive GUI runs produce structurally identical scenes
       (only XY positions differ).  AI implements; user runs in
       GUI 2-3 times for verification; AI waits for user OK
       before committing [DONE].  Workflow expectation captured
       in the Care block.

TIER 1 — UNIFORM/TAMPURA BASELINE PLACEMENT (correctness blocker):
  #66  Place fails when object > cell.  Default uniform 5 cm
       cells can't fit 6-7 cm occluders -> boxel_fits empty ->
       PDDLStream stalls.  Split fix: Plan A [DONE on main
       2026-05-09 (later)] (auto-tune cell size); Plan C
       [PENDING on audit-64-tampura-binary-grid] (continuous-
       pose place, real TAMPURA).  Single shared issue body
       across both branches.
  #71  Refresh OBJECT boxel AABBs on every sense.  Currently
       written once at spawn (or at discovery for nontarget
       bodies); cubes that shift between actions drift away
       from the stored AABB and the planner reasons over
       stale geometry.  User direction: refresh BB only, not
       the shadow region behind it (out of scope).  ~0.1 ms
       overhead per sense.  Promotable to TIER 0 if false-
       success cases multiply.
  #72  Shadow AABB carved only along dominant axis -> lateral
       overhang beside the occluder.  full_min/full_max
       encloses object AABB ∪ ray hit points; only the
       dominant-axis face is trimmed back to the object.
       Perpendicular axes keep the wide hit-point bounds all
       the way back to the cube, so 7-10 cm of camera-visible
       table beside each occluder is incorrectly classified
       as shadow.  Free-space + boxel_fits then carve cells
       the camera can see.  Fix C (two-slab carve) recommended;
       one extra SHADOW boxel per occluder, no PDDL stream
       refactor.  Repro seed 779694423 stored in body.

TIER 2 — PERF (planner wall-clock):
  #50  Planner perf investigation — profile, cache static init, kill
       redundancies (user-flagged "planner is too slow")
  #62  Replan-while-holding still slow after #58 — sub-hotspot of #50
       on the held-cube path (user-flagged 2026-05-08; profile first,
       merge into #50 if dominant cost is branch-agnostic)

TIER 3 — CODE QUALITY / LOW-PRIO:
  #57  Registry should own cube geometry (audit #55 follow-up;
       replace PyBullet AABB fallback in compute_stack_kin_solution)
  #59  Scaled-up find-and-stack — N hidden targets + size-compat
       PDDL predicate (very low priority per user 2026-05-08).
       #68 (scene randomizer) is a prerequisite for any "random N
       hidden targets" find-and-tray-stack demo.
  #61  Smart console filter — strip residual startup/shutdown garbage
       and add Claude-Code-style thinking-animation affordance during
       long planner.plan() / RRT / stream-sample calls
       (UX polish; user-flagged 2026-05-08)
  #63  README + wiki final doc sweep — gated on remaining open issues
       (now: #50, #57, #59, #61, #62, #65, #66 Plan C, #68).
       Explicitly the last commit before thesis submission per user
       2026-05-08.  Periodic refresh sweep landed 2026-05-10
       (commits 56d6001 README + de218bb / 169800b wiki); #63 itself
       remains open as the final pre-submission pass.
  #65  Rename: boxel -> hanixel.  HANI = Heterogeneous Axis-aligned
       Named Interval; XEL inherited from voxel.  User decision
       2026-05-09.  Mechanical case-preserved sweep on its own
       branch (rename-boxel-to-hanixel).  Land after audit-64-
       tampura-binary-grid merges/rebases to avoid rename noise.
  #69  PyBullet rayTestBatch — pass numThreads on the 5 existing
       call sites (boxel_env.py x2, shadow_calculator.py,
       execution.py x2 — 147 rays per sense action is the
       largest single batch).  Unused upstream PyBullet feature;
       1-kwarg patch.  Spinoff from the FOR LATER "Parallel
       FastDownward" bullet — captures the realistic CPU-side
       win that doesn't require simulator/planner swap.

Total open: 13 items (#50, #57, #59, #61, #62, #63, #65, #66 Plan C, #68, #69, #70, #71, #72).

Closed since previous summary:
  #67  [DONE 2026-05-10] — Phase 4 hidden-target criterion swap
       (commit 4c9e4d5).  Single-commit resolution: dropped the
       audit body's two-commit plan in favour of just the Phase 4
       swap, after user pushback — making Phase 4 use camera-ray
       occlusion (matching Phase 1 / oracle_detect_objects)
       eliminates the disagreement at its source.  Eval no_summary
       cells drop to zero (modulo unrelated #21 / #47 cascading-
       occlusion give-ups now surfaced).
  #10  [DONE 2026-05-08 (later)] — uniform voxelization baseline
       (commit afdaadd).  Free-space-only swap: UniformGridGenerator
       (uniform_grid.py) replaces FreeSpaceGenerator under
       --baseline uniform; OBJECT + SHADOW boxels untouched; PDDL
       domain unchanged (TAMPURA-style Python-side label maintenance
       via the existing _build_init re-emit).  SCALABILITY_MATRIX
       gains the baseline axis (90 -> 180 cells, semantic + uniform).
       eval_plotter.py --baseline-csv hook already consumes the
       comparison input.

Deferred   → archive/CODEBASE_AUDIT_DEFERRED.txt:
             #2, #14, #42 (logged collisions do not trigger replan — accepted;
                  supervisor reconfirmed 2026-05-06: "no need to track
                  knocked-down objects"),
             #47 (DEFERRED OUT OF SCOPE 2026-05-06 — archived in 2026-05-06 slim),
             #51 (DEFERRED 2026-05-07 — archived in 2026-05-07 slim; fix preserved
                  on branch audit-51-shadow-blocker-fix on both remotes).

Resolved/subsumed → archive/CODEBASE_AUDIT_RESOLVED.txt:
             #1, #3, #4 (accepted shadow AABB remainder), #5, #7, #8, #11
             [WONTFIX], #12 [MERGED INTO #9], #13 [WONTFIX], #15–#20,
             #21 (log-only band-aid 2026-05-05; real fix #47 deferred
             2026-05-06), #22–#29, #30 [WONTFIX], #31 [DONE 2026-05-07],
             #32 [MERGED INTO #31], #33, #34 [WONTFIX], #35, #36 [DONE
             2026-05-07], #37, #38, #39, #40 [DONE 2026-05-05], #41
             [DONE 2026-05-07], #43, #44, #45, #46, #48 [DONE 2026-05-07],
             #49 [DONE 2026-05-07], #53 [DONE 2026-05-07],
             #54 [DONE 2026-05-07], #55 [DONE 2026-05-08],
             #56 [DONE 2026-05-08], #58 [DONE 2026-05-08],
             #60 [DONE 2026-05-08].
