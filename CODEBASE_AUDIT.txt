================================================================================
CODEBASE AUDIT — OPEN ISSUES
================================================================================
Date: 2026-02-25 (original, merged from 3 passes). Slimmed repeatedly (2026-05-12,
-19, -20) — resolved bodies + the RESOLVED archive moved to git history /
archive/CODEBASE_AUDIT_DEFERRED.txt; closed-issue one-liners kept in the SUMMARY block.
Slimmed 2026-05-25: open issue bodies condensed; POLICY + FOR LATER kept verbatim.
Deferred/background/resolved: archive/CODEBASE_AUDIT_DEFERRED.txt

================================================================================
CODEBASE POLICY
================================================================================

GIT COMMITS IN POWERSHELL — DO NOT USE BASH HEREDOCS
       The environment uses Windows PowerShell. Do NOT use bash-style
       heredocs (`cat <<'EOF'`) for git commit messages, as they will hang
       or fail. Use standard `git commit -m "message"` instead.

GIT PUSH — BOTH REMOTES (GITHUB + GITLAB)
       After pushing meaningful work, push the same branch to BOTH hosts
       so they stay in sync:
         git push github <branch>
         git push origin <branch>
       Remotes: `github` → github.com (hanihabobo-dot/pybullet);
                `origin` → git.rwth-aachen.de (hani.alassiri.alhabboub/pybullet).
       Do not treat a single-remote push as "done" unless the other host
       is intentionally out of scope for that change.

COMMENTED-OUT CODE — DO NOT DELETE
       Commented-out code is preserved intentionally as inline
       documentation, quick-enable toggles, and reference for
       re-implementation.
       Rule: NEVER delete without explicit approval.  Move to archive/.

DEAD CODE — PRESERVED FOR FUTURE USE
       Dead methods/imports documented in old #87–#92 (resolved archive)
       are NOT scheduled for deletion.  No runtime cost; may be needed
       for evaluation framework, real perception, etc.

WIKI — UPDATE AFTER EVERY CHANGE
       Project wiki: pybullet.wiki.git (GitHub wiki repo, not main repo).
       Local clone: ../pybullet.wiki/
       Update after any change to architecture, data structures,
       algorithms, PDDL domain, execution pipeline, or design decisions.

ARCHIVE — TRACKED AND PUSHED
       archive/ contains superseded implementations and audit history.
       Tracked by git.  Must NOT contain personal information.

PYBULLET — PLANNING VS. GUI (audit #46 supersedes old #60 full-plan lock)
       PDDLStream planning runs against a separate DIRECT PyBullet client
       (boxel_env.plan_client_id); the GUI window stays interactive during
       long planner.plan() calls.  RenderingLock may still wrap brief IK
       solves on the GUI client during execution (execute_pick / place /
       stack), so a short flicker is possible per action — not a multi-
       minute freeze.  If WSLg/X11 still drops under heavy load, restart
       WSL (wsl --shutdown from PowerShell, then reopen terminal).

WSL VIRTUAL ENVIRONMENT — DO NOT DELETE
       wsl_env/ is the WSL Python venv (gitignored, local only).
       Deleting it breaks the entire pipeline.
       Recreate: python3 -m venv wsl_env && pip install numpy pybullet

RUNNING THE PIPELINE — POWERSHELL / WSL WRAPPERS
       Machine-specific run instructions, moved out of README.md on
       2026-05-18 per user direction (README and wiki must stay
       machine-agnostic).  Kept here because this is the only record
       of the launch wrappers — they live in the user's PowerShell
       profile, not in the repo.

       The repo runs in the wsl_env/ WSL venv (see entry above) with
       PDDLStream vendored as a sibling checkout, ../pddlstream_lib.
       Three wrapper functions in $PROFILE
       (Microsoft.PowerShell_profile.ps1) launch it from PowerShell.
       <repo> below = /mnt/c/Users/HaniAlassiriAlhabbou/git/Semantic_Boxels
         run_boxels  ->  cd <repo> && source wsl_env/bin/activate &&
                         DISPLAY=:0 LIBGL_ALWAYS_SOFTWARE=1
                         PYTHONPATH=<repo>/../pddlstream_lib
                         python3 test_full_pipeline.py $args
         run_eval    ->  cd <repo> && source wsl_env/bin/activate &&
                         PYTHONPATH=<repo>/../pddlstream_lib
                         python3 eval_runner.py $args
         plot_eval   ->  cd <repo> && source wsl_env/bin/activate &&
                         python3 eval_plotter.py $args

       One-time wsl_env bootstrap (wsl_env/ is gitignored):
         wsl bash -lc "cd <repo> && python3 -m venv wsl_env &&
           source wsl_env/bin/activate &&
           pip install pybullet numpy matplotlib"
       matplotlib is required by eval_plotter.py — without it the
       plotter prints text tables instead of PNGs (look for the
       "(matplotlib not available)" marker in stdout).

       plot_eval: pass the /mnt/c/... WSL form of the aggregated.csv
       path.  PowerShell mangles backslashes when a Windows path has
       escape-like substrings (\U, \H, ...).  Three PNGs land next
       to the CSV.

       PowerShell pitfalls:
       - "[plotter] not found: C:Users..." (no backslashes) — a
         Windows path lost its backslashes inside a double-quoted
         "$(...)"; use the /mnt/c/... path or Join-Path as one arg.
       - "(matplotlib not available)" — wsl_env/ lacks matplotlib;
         re-run the bootstrap pip install.
       - "bash: ...conda.sh: No such file or directory" — the shims
         activate the wsl_env venv, not anaconda; install into the
         venv, not base.

BOXEL_FITS / STATIC INIT FACTS — REBUILT EACH REPLAN (old #102)
       Predicates such as (boxel_fits ?o ?b) are precomputed in
       pddlstream_planner._build_init() from the current BoxelRegistry
       snapshot.  Not a PDDLStream test stream — an earlier implementation
       used one (test-boxel-fits), but adaptive search re-evaluated it
       heavily across skeletons and dominated runtime despite a cheap
       per-call check.  stream.pddl no longer declares that stream; domain
       and predicate are unchanged.  As of 2026-04-15, free-space boxels
       ARE re-merged and re-split between replans (reboxelize_free_space
       after sense); the registry is mutated in place before each
       planner.plan() call, and shadow removal / new object creation after
       sense also update the registry.  The planner is stateless between
       calls — all context is passed in each plan() call.

META-ISSUE — OLD #95 SYSTEMIC PDDL STATE DRIFT (dissolved)
       The 4 drift channels between PDDL symbolic state and PyBullet
       physical state are tracked individually: (a) robot config drift
       after pick/place — closed by audit #1 [RESOLVED, archive];
       (b) hidden move in sense — audit #3 [RESOLVED, archive];
       (c) stale shadow geometry after occluder relocation — audit #4
       [RESOLVED as accepted simplification, archive 2026-05-03]; (d) stale
       blocks_view_at facts — old #73 [DONE, archive].  See THESIS_NOTES.md
       for accepted geometric / symbolic gaps (including stale shadow AABBs).

================================================================================
OPEN ISSUES — #104 #105 #106-#114
================================================================================
Issue numbers are DISCOVERY ORDER, not priority — read the TIER label, not the number
(e.g. #63 is T3 doc work, #81 was T0). If walking the list lowest-first, re-sort by TIER.

STATUS (2026-05-20): pre-eval correctness chain, eval framework (#9 runner + #10 uniform
baseline), eval-chapter figures (#73), gripper-handoff chain (#75-#85), README/wiki sweep
(#63) all DONE. Open are T2/T3 follow-ups to the resolution sweep (#98 done w/ RESULT,
#101 writeup) plus the 2026-05-24 read-through spillover (#104-#112). Closed 2026-05-20:
#77 #93 #94 #95 #96 #99. Deferred: #78 #97. Detail in git / DEFERRED archive.

STATUS (2026-05-25): code-only fixes landed on main — #107 (replan cap removed, cabad01),
#108 part-a (resolution-mbs01 / mbs0.1 arm, 67335c1), #111 (TAMPURA caption caveat, 59157ec;
the THESIS_NOTES-text removal was already done by thesis #177). #109 killed (WONTFIX).
DEFERRED TO THE NEXT FULL RE-RUN: #107 eval re-run (replan_limit cells now run to the
wall-clock budget -> shifts headline numbers feeding thesis #177/#190/#200/#203/#209 + #104
figures); #108 part-b (run --matrix resolution-mbs01 --output sweep_anytime --skip-existing,
~900 cells) + part-c (boxel-count-vs-config plotter figure); #111 + #104 plot regen.

================================================================================
FOR LATER
================================================================================
 backlog (not scheduled — cross-reference open/resolved/deferred audits):

- Incremental boxel recomputation after occluder relocation (optimisation of
  reboxelize_free_space / octree pass).  Shadow AABB staleness is an accepted
  thesis gap (resolved #4 2026-05-03; see THESIS_NOTES.md).

- Physics-based pushing instead of pick-and-place for heavy objects.  Push
  logic remains commented out in-repo (historical experiment).

- Pick–observe–replace (clear corridor, sense, place object back).  Raised
  2026-03-22; not modeled in PDDL.  Related reactive behaviour lives in
  archive resolved #21 (still_blocked / search policy; log-only band-aid
  landed 2026-05-05) — the principled re-grounding fix (#47) was
  deferred out of scope 2026-05-06, so this bullet is purely conceptual.

- "Approach 2": PDDL-level merge/split with active representative boxels.
  Superseded by opaque re-boxelize + replan (Approach 1).  Full semantic merge
  condition: deferred #2 in archive/CODEBASE_AUDIT_DEFERRED.txt.

- Dense camera→AABB visibility lattice (64-ray variant): deferred #14 — branch
  audit-14-dense-lattice-attempt; precision vs planner grounding cost.

- Perception-density sweep plot (former #73 TIER D / plot 10).  X: VISIBILITY_-
  GRID_N in {2, 3, 4, 5}; Y: success rate, planning time, # discovered
  occluders, # init facts (4 sub-plots, per baseline).  Gated on the
  audit-14-dense-lattice-attempt flag merge above and a new --visibility-
  grid-n axis on the eval matrix.  Out of thesis scope per user 2026-05-14;
  THESIS_NOTES §14 cites this as the missing-experiment slot if a future
  pass revives it.

- Multi-boxel `place` (old open #6): removed from scope 2026-05-03 — not pursued.

- Hard-gate `:action stack` with a `(stack_allowed)` precondition (only set
  when the goal demands stacking) — fallback if the action-cost bias
  (stack=2, others=1) is insufficient to suppress place-rescue stacks.

- Tighten stack physics check to a safe-inset XY test: centre within
  (support AABB shrunk by half the cube footprint) instead of plain
  center-within-AABB.  Audit #40 introduced the loose check; refinement
  only matters if eval (#12) shows edge-perching false-positives.

- Stack size-compatibility check (raised 2026-05-07 during #52 root-cause
  analysis; #52 dropped 2026-05-08).  Domain :action stack and
  streams.compute_stack_kin_solution currently let any cube stack on any
  other — the planner happily plans big-on-small towers that physics
  fails to stabilise.  Two possible fixes: (i) symbolic
  (compat_stack ?o ?on_obj) static predicate precomputed in _build_init
  from boxel half-extents — CLAIMED BY #59 (open, very low priority);
  (ii) geometric filter inside the placement / stack-kin sampler (skip
  seeds where held footprint exceeds support footprint by more than X%)
  — still parallel future option.  Currently mooted by 40 mm uniform
  targets in default_scene; surfaces if any goal mode mixes sizes.

- Trajectory-clipping edge case for stack actions (audit #55 lazy-
  collision hack residual, raised 2026-05-08).  Commit 9a64aa1 adds
  the support cube body to compute_stack_kin_solution's
  ignored_body_ids so plan_motion can route through where the support
  sits at planning time.  Side effect: the planned trajectory is
  unaware of where the support will be at EXECUTION time (its
  destination on the tray / a free boxel / a previously-stacked cube /
  etc.).  If the destination lies on the planned arm route, the
  actual arm motion could collide with the support at runtime.  Not
  exercised by current goal modes — find-and-tray-stack lands the
  support at the back-left tray corner well outside typical arm
  routes between table-spread cubes; --goal stack uses initial poses
  for all supports (destination = source so no clip risk); --goal
  holding has no stack.  Future fix when needed: branch
  audit-55-pose-aware-stack (Path C) threads correct destination
  poses through compute-stack-kin and resolves this — works but
  costs ~7x baseline planning time due to PDDLStream's exogenous-
  axiom compilation.  Alternative: extend RobotConfig with a
  separate endpoint-only-ignored set so plan_motion path-checks
  honour the support body while endpoint-checks don't.

- Smart / dynamic action-cost modelling for risk-aware planning.  Action
  costs are currently static (stack=2, all others=1; see domain.pddl
  :action-costs and the bias rationale at domain.pddl:73).  Extend this so
  the planner can weigh a RISKY-but-cheap solution against a GOOD-but-
  costlier one — the same lever already used to bias place over stack, but
  applied more broadly (e.g. sensing, placing into shadow-adjacent free
  cells).  Crucially the cost-giver should UPDATE costs between replans
  whenever warranted: e.g. raise the cost of re-sensing a region already
  observed without result, or penalise placements that earlier failed
  (drop_failed / physics_mismatch) or that land on a line of sight the
  robot still needs.  Turns the flat unit-cost search into adaptive
  replanning that learns from the episode's own failure modes.  Conceptual
  for now; pairs with the bounded give-up / still_blocked logic and the
  per-occluder shadow bookkeeping.

- POMDP / contingent baseline: large effort; only if time after eval sweep
  (#9–#13).  Thesis-critical fixes are now bounded to #40 [DONE] +
  #36 [DONE] + #49 per ROADMAP; #21 / #47 (still_blocked correctness) are
  closed at the log-only-band-aid level (#47 deferred out of scope
  2026-05-06).

- LW1 / TAMPURA empirical comparison.  Supervisor (sync 2026-05-06):
  "Comparison with lw1 and tampura only after finishing the writing of the
  thesis if I have at least a week of free time."  Conditional on:
  (i) thesis writing done end-to-end, and (ii) ≥ 1 week of free time
  remaining before submission.  Otherwise cite as related work without
  empirical comparison.  Likely scope: harness LW1 / TAMPURA on the same
  scenes used for #12 scalability, reuse #9 metric hooks, run the same
  seeds — no domain re-encoding by us if their published encodings cover
  the task; otherwise drop the comparison rather than shoehorn.
  RESULT [2026-05-26] -- TAMPURA half done on our HW.  Real find_dice now
  stands up + runs in WSL (see notes/TAMPURA_PLAN.md "HOW TO RUN REAL TAMPURA"
  + T2 PHASE 1/2).  Headless 20-seed sweep (runs/sweep_2026-05-26.json):
  SUCCESS 11/20 = 55% (paper >= 63%); per-episode loop_s (planning + execution)
  239.9 +- 151.1 s all / 166 +- 85 s on successes; wall_s 253.3 +- 150.9 s.
  CORRECTION [2026-05-26, vs paper Table II screenshot]: the 57+-38 s (Task C)
  is NOT planning-only and NOT per-episode -- caption: "PER-STEP planning times
  ... averaged over trials and steps within each trial.  These include execution
  time of the selected controller in simulation".  It is PER-STEP, incl sim
  execution.  Matched: our per-step = loop_s/20 ~ 8 s/step (success) to ~16 s/step
  (fail), ~16-28 s per MEANINGFUL step.  run_planner IS TAMPURA, so this is
  TAMPURA-ON-OUR-HW vs their published their-HW number: per-step we are AT/BELOW
  57+-38 -> NO "3-4x slower", and no evidence their HW is much better.  Don't
  declare a winner (their averaging/task config may differ).  SymK classical
  solve negligible (~0.03 s/step).  Ours-on-their-env is T2 PHASE 3 (planned).  Data also feeds
  the deferred #111 TAMPURA wall-clock plot regen.
  UPDATE [2026-05-27]: source of truth CONFIRMED = arXiv:2403.10454 v2 PDF p.15, Table II +
  caption (author screenshot).  Source of misconception: the paper states something different
  than the PDF (the v2 PDF caption, per-step, is authoritative).  OUR matched per-step (for the
  thesis per-step reconciliation) = wall_clock/plan_count = 13.676/2.315 = 5.9 s/solve
  (holding-semantic success-only, avg 2.315 plans/ep), vs paper 57 +- 38 s/step (~9.7x).  Thesis
  re-derivation tracked as thesis_audit #213 (figure -> local 166 +- 85 per-episode; prose ->
  13.7 vs 166 primary + 5.9 vs 57 per-step).

- Parallel FastDownward / GPU — post-thesis research direction (raised
  2026-05-10).  CPU-side parallel FD has prior work (HDA*, ArvandHerd,
  FD Stone Soup); GPU is a dead-end for classical PDDL planning.  Stays
  unscheduled: our wall-clock cost is upstream of FD search (#50/#62 —
  Python-side stream/init redundancy), and the realistic CPU win is #69
  (rayTestBatch numThreads).  GPU robotics sim means swapping simulator
  entirely (Isaac, MuJoCo MJX, Genesis) — out of thesis scope.

- Planner stack (post-thesis / exploratory): move off Fast Downward toward
  any alternative that is plug-and-play alongside the PyBullet sim (motion
  or task planners normally used in the same ecosystem are fine for a first
  integration).  Goal is a swappable backend without investing further in
  FD-specific parallelisation; see deferred #50 in
  archive/CODEBASE_AUDIT_DEFERRED.txt if work stays on the current
  PDDLStream + FD path.

================================================================================
#104. REFRESH THE PLOTS + DATA EMBEDDED IN THE THESIS PDF TO CURRENT EVAL OUTPUTS
================================================================================
Status/Priority: OPEN, TIER 3 (figure/data sync; no correctness risk; gates a faithful
   build). Filed 2026-05-24. Depends: #100 (merged 4500-cell dataset + resolution figure),
   #98 RESULT, thesis_audit #121/#168.
What:   the PDF's eval figures + quoted numbers have drifted (sweep_anytime now 4500 rows;
   resolution figure/numbers new; partition-comparison #168 being curated).
Fix:    1. inventory every eval-derived plot + number in the thesis. 2. re-export from
   sweep_anytime/(+/resolution/) via eval_plotter.py / render_thesis_figs.py; swap stale
   PNGs in thesis/graphics/. 3. reconcile numbers vs regenerated summary_table.md (holding
   42.3/40.3/45.7; f-a-t-s 39.8/35.3/35.3; stack 61.3 flat). 4. rebuild, confirm consistent.
Care:   plots ADD never overwrite (read each PNG first); doc edits land thesis-side (#121/
   #168); stack rows carry #95 misnaming — slice by stack_height.
Related: #100, #101, #98, thesis_audit #121/#168.

================================================================================
#105. ADD A WUMPUS-WORLD BASELINE FOR SEARCH-UNDER-OCCLUSION COMPARISON
================================================================================
Status/Priority: OPEN, TIER 2 (new eval arm; no correctness risk; stronger comparison
   claim). Filed 2026-05-24. Depends: #10 (uniform scaffold to mirror), #9 (runner/matrix),
   #99 (variant-key plotter). Effort TBD — scope the planner/belief model first.
What:   a geometry-blind lower bound: discretize the workspace into cells, treat hidden
   objects as the "wumpus", maintain occupancy belief over cells, sense/move/replan with NO
   shadow reasoning — isolates the value of semantic shadow reasoning vs same sense/replan loop.
Fix:    1. define cell discretization + belief (mirror uniform grid). 2. implement sense/
   move/replan over cell belief (no shadow_calculator). 3. register as a new variant tag
   (confirm #99 splits it out). 4. run a matrix slice; record in EVAL_SUMMARY.
Care:   distinct variant tag so #99 doesn't average it in; plots ADD.
Related: #10, #9, #99.

################################################################################
#  ISSUES #106-#112 — ADDED 2026-05-24 (thesis read-through batch 2 spillover)
################################################################################
Code/eval/plotter follow-ups from the 2026-05-24 read-through (prose counterparts =
thesis_audit.md #199-#207); several are eval-data findings agent-verified vs sweep_anytime.

================================================================================
#106. FAILURE-MODE TAXONOMY: FOLD all_searched + physics_mismatch INTO planner_failed; INVESTIGATE no_summary
================================================================================
Status/Priority: PART (1) DONE 2026-05-26 (author also folded in drop_failed); PART (2) STILL
   OPEN, TIER 2 (eval classification + plot grouping; changes reporting, not runs). Drives
   thesis #201 + the failure-mode figure. Depends: #99, #9.
What/Fix: (1) [DONE] all_searched + physics_mismatch + drop_failed are planner outcomes ->
   collapsed into planner_failed at REPORT time (eval_plotter.group_failure_modes via
   _PLANNER_FAILED_ALIASES; raw exit codes kept in the data). failure_modes.png regenerated +
   fig:failure-modes caption notes the grouping. (2) [OPEN] no_summary cells (the ~6% denominator
   gap, results.tex:98) still need a real exit reason (scripts/_audit96_scan_no_summary.py, #96).
Related: thesis_audit #201; #96; #99.

================================================================================
#107. REMOVE THE PER-EPISODE REPLAN LIMIT IN EVALUATIONS (run to time budget; keep 3-retry give-ups)
================================================================================
Status: OPEN, TIER 1. Code DONE 2026-05-25 (cabad01: per-episode replan cap removed;
   episodes bounded by runner wall-clock only, then timeout; give-ups kept -> exit #79/#80
   + unreachable-shadow three-strike). DEFERRED to next full re-run: re-run affected cells
   (stack esp., where replan_limit dominated, results.tex:221) -> shifts headline numbers
   feeding thesis #177/#190/#200/#203/#209 + #104 figures; regen plots (ADD); update #106.
Related: thesis_audit #200/#201; #106; #104; #79/#80.

================================================================================
#108. BOXEL-COUNT-VS-CONFIG PLOT + semantic+mbs0.1 ARM (min free-space size barely changes cell count)
================================================================================
Status: OPEN, TIER 2. Part-a DONE 2026-05-25 (67335c1: resolution-mbs01 / mbs0.1 preset).
   Part-c DONE 2026-05-25 (790cd1e: plot_boxel_vs_resolution + scripts/_audit108c_plot.py ->
   boxel_count_vs_resolution.png, total+free-space vs leaf size per goal; used in thesis #101/#209).
   DEFERRED to next full re-run: part-b (run --matrix resolution-mbs01 --output sweep_anytime
   --skip-existing, ~900 cells -> distinct mbs0.1 arm; current plot uses #98/#100 1.5x/2x arms).
   Verified: finer floors 0.05/0.09 == auto, only coarser reduce. Stack rows by stack_height (#95).
Related: thesis_audit #202/#203; #100; #99; #95.

================================================================================
#109. [WONTFIX 2026-05-25]  NEW BASELINES: uniform@smallest-object autocell; ENTITY-MEMBERSHIP action preconditions
================================================================================
Killed per user direction 2026-05-25. The smallest-object uniform baseline and the
entity-membership precondition redesign (drop boxel-center+10cm) are not pursued.
Related: #10; #105; thesis_audit (Baselines, results.tex:100).

================================================================================
#110. LIVE-DEMO NATURAL-LANGUAGE CLI ("pick blue box" -> executes)
================================================================================
Status/Priority: OPEN, TIER 3 (nice-to-have demo, off critical path). Effort medium.
   Where: new interactive entry point over pddlstream_planner.py + executor.
What/Fix: terminal demo — type "pick blue box", robot executes; cover pick(~=holding/find),
   drop, sense, stack, find-and-stack. Lightweight NL/regex parser maps utterance -> object +
   action/goal; ASK back when ambiguous rather than guess. Steps: define grammar (verbs +
   descriptors resolved vs scene); map to goal/action through existing planner/executor;
   ambiguity + help paths.
Care:   thin wrapper; do not entangle with the eval pipeline.
Related: existing action set; thesis Future Work.

================================================================================
#111. REMOVE "THESIS_NOTES" TEXT FROM THE TAMPURA WALL-CLOCK PLOT
================================================================================
Status: OPEN, TIER 3. Caption DONE 2026-05-25 (59157ec: caption notes the 57 s spans all 20
   trials, not success-only; the "THESIS_NOTES"-text removal was already done by thesis #177).
   DEFERRED to next full re-run: regenerate the TAMPURA wall-clock PNG (ADD) once the re-run
   lands. Where: eval_plotter.py plot_tampura_wallclock_comparison.
Related: thesis_audit #177/#190; #94; #99.

================================================================================
#112. (OPTIONAL) WILSON INTERVAL FOR SUCCESS-RATE PLOTS
================================================================================
Status/Priority: OPEN, TIER 3 (optional; current bands usable once captioned, thesis #207).
   Where: eval_plotter.py group_success_rate / plot_metric. Depends: #86, #99.
What/Fix: success-rate plots shade +/-1 SAMPLE STD of per-trial 0/1 flags (clipped [0,1]) —
   not a proper binomial interval. Replace/supplement with a Wilson score interval for the
   ~80-100 Bernoulli trials/point; regenerate plots (ADD); update thesis #207 caption.
Related: thesis_audit #207; #86; #99.

================================================================================
#113. FULL CANONICAL SWEEP RE-RUN -- reproduce all data + add mbs0.01 arm; ensure COMPLETE coverage
================================================================================
Status/Priority: OPEN, TIER 1 (regenerates the canonical eval dataset the whole
   results chapter rests on).  Expensive; run on the eval machine (WSL), gated by
   available time.  Author-requested 2026-05-27.
Where: eval_runner.py presets -- SCALABILITY_VS_TIME (semantic min_boxel_size
   [None, 0.05] + uniform), SCALABILITY_VS_RESOLUTION (#98: random-pairs 0.135/0.18,
   stack 0.09/0.12), RESOLUTION_MBS01 (#108: mbs0.10).  Run into a FRESH
   eval_results/sweep_<ts> dir, then promote to the canonical aggregated.csv and
   regenerate EVERY figure from it.
Depends: the no_summary pre-flight fix (commit 5bd5f55 -- find-and-tray-stack
   random-pairs 0-visible seeds now re-roll instead of aborting); the new sweep must
   show ZERO no_summary cells.

What: the current canonical aggregated.csv (4500 rows, eval_results/sweep_anytime)
   is NOT one clean full sweep -- it was assembled by merging sub-sweeps (#100) and
   has gaps / inconsistencies:
   - the mbs0.10 arm (RESOLUTION_MBS01 / #108 part-a) is NOT in the 4500-row csv
     (group_boxel_vs_resolution shows only auto/0.05/0.135/0.18 for random-pairs and
     auto/0.05/0.09/0.12 for stack);
   - the coarse mbs arms differ per goal (random-pairs 0.135/0.18 vs stack 0.09/0.12),
     so the resolution axis is not uniform across goals;
   - 91 find-and-tray-stack random-pairs cells are no_summary holes (0-visible
     unsatisfiable seeds; root cause + fix in 5bd5f55).
   The author wants ONE actual full sweep that reproduces all data cleanly AND adds a
   mbs0.01 (1 cm) fine-end resolution arm.

Fix:
   - Add 0.01 to the semantic min_boxel_size arms (1 cm: below auto_cell ~6-9 cm AND
     below the 3.5 cm octree min_resolution, so it probes the FINE extreme of the
     resolution axis, complementing the coarse 0.135/0.18; expected still flat under
     the convex free-space merge, which would confirm the plateau spans ~1-18 cm).
   - Run ALL presets (SCALABILITY_VS_TIME + SCALABILITY_VS_RESOLUTION + RESOLUTION_MBS01
     + the new 0.01 arm) in ONE sweep into a FRESH output dir -- do NOT rely on
     --skip-existing merging onto the old partial data.  --timeout 1800 (post-process
     at shorter wall_clock_s thresholds).
   - Acceptance: every (goal x {semantic, semantic+mbs{0.01,0.05,0.10,1.5x,2x}, uniform}
     x 100 corpus seeds x {2,3,4} difficulty) cell PRESENT; ZERO no_summary; promote to
     the canonical aggregated.csv; regenerate every thesis figure and reconcile the
     reported numbers (holding 42% / 13.7 s, boxel counts, failure modes, resolution).
Related: #98, #100, #108 (RESOLUTION_MBS01), #66 (auto_cell); commit 5bd5f55 (no_summary
   fix); thesis_audit #209 (resolution writeup).  Care: plots ADD -- keep the old sweep
   dir; read regenerated PNGs before committing.

#114. [2026-06-06] PHASE 3-B (B-full) DONE -- OUR pipeline on TAMPURA's find_dice.
     Bridge in tampura_bridge/ (never edits their clone); runs IN their .venv, same
     pybullet 3.2.7.  GUI to 3 goals: G1 boxelization+shadows on their scene; G2
     our planner emits the pick and the policy executes a REAL reach->grasp->lift
     on their Panda via OUR IK (panda_grasptarget link 14, arm 0-6, fingers 12/13)
     -- per user, a real pick not a weld; G3 oracle+boxelization run the SAME code
     (foreign-self reuse + GUARDED optional physics_client, inert when None) and
     sense gated to the fixed home config.  IK note: arm joints align (0-6);
     EE/fingers differ (14 / 12,13 vs 11 / [9,10]) so the bridge resolves BY NAME
     -> no shared-streams refactor (eval-safe); IK reaches cup to 0.1 mm.  FINDING:
     their hiding is CONTAINMENT (die under cup) vs our LATERAL shadow.  OPEN
     FOLLOW-ONS (LATER): full die-solve; PLACE/go-home + RRT motion; belief.update
     integration; >=20-seed eval = PHASE 4 (GATED).  Shared-core footprint: 27
     lines / 2 files, inert when physics_client=None.  Log: notes/TAMPURA_PHASE3B_PLAN.md.

#118. [2026-06-07] PHASE 3-C DONE -- OUR pipeline SOLVES TAMPURA's find_dice holding
     goal (the #114 "full die-solve" follow-on).  GUI seed 0: rollout reward = 1.0
     (holding(die) AND at-home), driven by OUR planner + OUR faithful IK on their Panda
     with fully ANIMATED motion (no teleport -- arm interpolates to home; set_sense_config
     snap removed): pick(cup)->place(cup,free)->sense@home->pick(die)->go-home.  PATH B
     (user-chosen): our faithful execution drives their world; the bridge WRITES the
     result into THEIR SceneBelief (tampura_bridge/belief_bridge.py: grasp_body=die alias
     + current_conf=DEFAULT_ARM_POS + moved) so abstract()->get_reward fires
     (find_dice/env.py:306/1004) WITHOUT editing their env (policy no-ops; the rollout's
     no-op deepcopy carries the writes forward).  PART B: dark-blue POMDP voxels
     (visibility_grid.draw_intervals) removed + OUR boxelization drawn/refreshed live
     (boxelize.suppress_pomdp_and_draw; removeAllUserDebugItems + tracked phantom removal).
     Planner: relaxed PLACE + new at_home/go_home in pddl/domain_find_dice.pddl;
     boxel_policy.plan_solve (seed-generic init).  Faithful PLACE + go-home in
     execution_bridge.py.  New probes _belief_smoke.py + _solve_smoke.py.  Captures ADD:
     phase3c_initial_boxels.png, phase3c_die_at_home.png.  Shared pipeline (boxel_env/
     shadow_calculator/streams/robot_utils) UNCHANGED -- eval-safe.  SEED-GENERIC for the
     gated seed-for-seed PHASE 4.  Commits 5659250..3cdec02 (main).  PHASE 4 (>=20-seed
     eval) STILL GATED.

#119. [2026-06-07] find_dice/holding SUCCESS CEILING -- failure-mode taxonomy (why our
     eval success is ~42%, not 100%; THESIS_NOTES §21.5).  IMPORTANT for the comparison:
     the Phase 3-C bridge hits 20/20 ONLY because it executes KINEMATICALLY (no motion
     planning, no collision checks, no physics) on guaranteed-solvable scenes; our REAL
     eval runs the full physical-execution layer, where the failures below occur.  Each
     item: [THESIS_NOTES § | existing audit # | status].  Consolidated record, NOT a
     reopen -- most fixes were deferred/wontfix per prior supervisor decisions.
     (a) Motion-planning fragility: the sampling-based planner fails to find a path for
         pick/place/relocate when the start config is in contact (esp. after place/stack).
         [§19 | #36 hardcoded-lift workaround landed | underlying robustness UNTRACKED].
     (b) Reachability / grasp limits: top-down-only grasp at a fixed 10 cm offset; arm
         cannot reach some configs incl. tall stacks -> no IK / no path -> failure.
         [§4,§12,§13 | #116 stack-degradation KILLED | accepted].
     (c) Collision-blind final approach + no replan on collision: the last 10 cm uses a
         local IK that bypasses planner collision checks; execution collisions are logged,
         not replanned -> physical desync / failure. [§12,§16 | #42 DEFERRED].
     (d) 3-strike give-up on still-blocked shadows: after 3 still_blocked senses the loop
         marks the shadow not_here and exits "all_searched" (eval counts it as failure).
         Real fix = re-ground blocks_view_at from current physics. [§18 | #21 log band-aid
         DONE; #47 real fix DEFERRED OUT OF SCOPE].
     (e) Sliver-visibility oracle miss: the 8-AABB-corner oracle misses partly-visible
         occluders, so the planner never gets the option to relocate them. [§1,§14 | #14
         64-ray fix measured + shelved on branch audit-14-dense-lattice-attempt].
     (f) Optimistic-sense + replan budget: optimistic sense with reactive replanning under
         a per-episode replan cap; exhausting it gives up. [§5 | #107 remove replan limit,
         OPEN T1].
     NOTE: §2 (perfect-gripper weld -> no slip/drop) and §3 (fixed full-table camera -> no
     sensing-pose motion) REMOVE failures rather than cause them -- part of why the
     kinematic bridge number is high.  Caveat for the thesis: bridge 20/20 measures
     planning+perception under idealized execution on solvable scenes; ~42% measures the
     full stack incl. the above.  See #118, notes/THESIS_NOTES.md §§1-21, notes/
     TAMPURA_PLAN.md PHASE 3-C RESULT.

#120. [2026-06-07] [OPEN T0] find_dice BRIDGE IS EXECUTION-UNFAITHFUL -- rebuild it to use
     OUR real streams + motion planning + collision checking + physics, NOT the kinematic
     shortcuts.  WHY: the bridge's 20/20 is an ARTIFACT of these shortcuts -- it cannot
     fail the way the real system does (#119).  This is the heart of a fair comparison.
     Shortcuts to remove (tampura_bridge/ + pddl/domain_find_dice.pddl):
     (a) NO STREAMS.  domain_find_dice.pddl RELAXED pick+place to DROP the grasp/config/
         kin_solution/motion streams; plan_solve grounds from a hand-built symbolic init
         (we currently use ZERO streams).  FIX: use OUR real streams (grasp-sample,
         place-sample, motion/kin) so pick/place ground like our normal pipeline (user:
         "fine to add our streams").
     (b) NO MOTION PLANNING.  execute_faithful_pick/place/go_home (execution_bridge.py) do
         straight joint interpolation via pybullet IK (_move_arm); no RRT / path planning.
         FIX: collision-free motion planning.
     (c) NO COLLISION CHECKING.  the kinematic moves ignore collisions.  FIX: add it.
     (d) NO PHYSICS / DYNAMICS.  grasp = rigid weld (createConstraint); place = teleport-
         to-rest (resetBasePositionAndOrientation).  FIX: real grasp + stepSimulation
         settling.
     (e) ORACLE READS GROUND TRUTH.  boxel_env.oracle_detect_objects enumerates EVERY body
         incl. the hidden die and reads its pybullet AABB (every body has an AABB even when
         hidden -> it "knows" hidden objects exist).  The bridge now uses segment_visible
         (render + segmentation, sees only un-occluded bodies) -- correct; do NOT use the
         ground-truth oracle for visibility.
     (f) SENSE OVER SYMBOLIC, NOT GEOMETRIC, REGIONS.  the bridge senses "region_cup_X" (a
         per-cup placeholder), not a real shadow boxel.  find_dice is CONTAINMENT (die UNDER
         the cup); our lateral-shadow boxels don't contain the die (die_in_shadow=False), so
         the semantic partition does NOT drive the search -- exhaustive relocate+re-sense
         does.  FIX: model CONTAINMENT regions (cup footprint volume) so the die lands in a
         region and the partition drives sensing.  (shadows ARE recomputed each refresh but
         go unused for localization.)
     (g) ARM-PARK BEFORE SENSE IS UNPLANNED.  the bridge calls execute_go_home to move the
         arm clear of the external camera before each sense -- bridge-internal, NOT a PDDL
         action (the planned go_home is only the final step).  Make sensing principled.
     (h) FULL OVERLAY REDRAW.  draw_overlay clears all our items + redraws every refresh;
         could redraw only what changed (perf/cleanup).
     Until fixed, the bridge benchmarks PLANNING + PERCEPTION only, on idealized execution.
     See #118, #119, notes/THESIS_NOTES.md, notes/TAMPURA_PLAN.md PHASE 3-C RESULT.

     PROGRESS [2026-06-08] -- approach (ii): port OUR streams onto THEIR Panda in bridge-side
     COPIES; shared streams.py/robot_utils.py stay byte-pristine for the live eval sweep.
     Built: robot_utils_boxel.py (+RobotModel), streams_boxel.py (robot-agnostic IK/collision/
     motion + solve_pose_ik), tampura_robot.build_robot_model (queries their live Panda: arm
     0-6, fingers 12,13, ee=panda_grasptarget link 14), _streams_smoke.build_streams,
     execution_boxel.faithful_pick.  Streams smoke PASSES; faithful_pick on seed 0 does a
     collision-checked reach -> grasp -> +0.20 m lift that reveals the die (status "ok").
     SCENE ADAPTATIONS (needed to manipulate their LOOK-ONLY scene; runtime-only, their URDF/
     clone untouched -- disclosed for comparison fairness):
       1. their cups AND dice are STATIC (mass=-1; TAMPURA only LOOKS behind cups, never
          picks).  faithful_pick makes the grasped object dynamic (changeDynamics mass=0.1)
          first, else welding the arm to a static body anchors the arm to the world.
       2. their Panda hand frames (panda_hand/handv/tool_tip/camera) carry NO inertial data ->
          PyBullet gives each mass=1 (~5 kg phantom hand) -> unphysical control.
          fix_phantom_masses zeros links [8,9,10,11] (fingers 12,13 + grasptarget 14 untouched).
       3. panda_grasptarget IK needs multi-seed + FK verification: a single-seed null-space
          solve returns joint-limit-valid configs that DON'T reach the target -> solve_pose_ik.
     PICK RE-EXAM [2026-06-08] -- faithfulness pass vs shared execution.execute_pick (pick
     "looked weird" in GUI); 4 bridge-side fixes, commits 805cbac/9ad647c/05dab36/d324349:
       - IK BRANCH-JUMP (the "weird"): approach/contact/lift were 3 independent rest-seeded
         solve_pose_ik picking min-FK-error -> base joint swung ~144deg (2.52 rad) between
         approach and contact for a 10 cm descent.  FIX: solve_pose_ik gains a seed arg (return
         the within-tol config CLOSEST to seed); faithful_pick seeds contact from approach,
         lift from contact -> one branch (swing 2.52 -> 0.16 rad).  Mirrors execute_pick's
         seeded contact IK (#37/#38).
       - SMASHING: close_gripper used a fixed 0.015 target (inside the cup wall, motor grinding
         the contact stop).  FIX: target = object half-width max(0.005, obj_hw) (== execute_pick,
         #81 anti-smash).
       - UNPHYSICAL FORCE: _drive_to used 1500 N.  FIX: 240 N (Panda datasheet, ==
         move_robot_smooth); converges now that phantom masses are zeroed.
       OPEN parity gaps (defer): (i) contact-z lacks execute_pick's table_z+0.035 floor --
       harmless for the tall cup, needed before faithful_pick is used on the SHORT die;
       (ii) post-pick lift 0.20 m vs execute_pick's 0.10 m -- drop to 0.10 once faithful_place
       transports the cup away.
     REMAINING: faithful_place -> faithful_go_home -> wire into BoxelPolicy -> honest run ->
     seeds 0-19 (PHASE 4 >=20-seed canonical eval GATED).

#121. [2026-06-09] [OPEN T2] STACK-UNIFORM FAILS VIA "no plan found", NOT TIMEOUT -- WHY?
     The detailed failure-mode breakdown (failure_modes_sem_uni_detailed.png, 2026-06-09) shows an
     anomaly the author flagged: on the STACK goal, uniform fails MORE than semantic overall, but
     mostly via "no plan found" (planner_failed) with FEWER timeouts than semantic -- uniform stack
     ~48 no-plan / ~7 timeout vs semantic ~2 no-plan / ~30 timeout (90 cells each). Counter-intuitive:
     the BIGGER grounded domain (uniform stack init facts ~12,100 vs semantic ~268, thesis #282) fails
     FAST (planner returns None) rather than exhausting the 1800 s budget.
     INVESTIGATE: (1) success-only mean plan time is stack semantic 17.16 s vs uniform 924.13 s
     (summary_table_aggregate.csv) -- successful uniform stacks ~54x slower; what is the mean/median
     plan time on the FAILED (planner_failed) uniform-stack cells? (2) Does planner.plan return None
     quickly (Fast Downward proves the grounded task unsolvable, or a stream iteration/budget cap
     trips) rather than timing out? (3) Is "no plan found" genuine unsolvability, a grounding blow-up
     that aborts, or stream exhaustion? Then decide whether §5.4 needs a sentence explaining it
     (currently it just states the split).
     AUTHOR HYPOTHESIS 2026-06-09: uniform likely KNOCKS DOWN STACKS more often during execution and
     recovers poorly, so the replan from the scattered state returns no plan (planner_failed) rather
     than timing out -- a likely IMPLEMENTATION ERROR in stacking/recovery, not genuine unsolvability.
     This fits the data: a mid-execution knock-down breaks the plan -> planner_failed (the symbolic
     goal is never reached, so it is NOT physics_mismatch/#122, which needs symbolic success first);
     #121 and #122 may share a manipulation-fragility root. CHECK the failed stack-uniform cells' logs
     for knocked-down-stack states and whether the post-knockdown state has no valid plan.
     DATA: eval_results/sweep_full_2026-05-28 (cells/ per-cell logs + aggregated.csv); plotter
     group_failure_modes (eval_plotter.py). HANDOFF: author will take this in a NEW session via a
     handoff prompt AFTER the thesis-audit issues are finished -- do NOT solve now.

#122. [2026-06-09] [OPEN T2] "FALSE SUCCESS" (physics_mismatch) IS HOLDING-ONLY -- WHY?
     physics_mismatch = the symbolic goal was reached but _verify_goal_physics failed, so the run is
     counted as a FAILURE not a success (success = symbolic_ok AND not physics_failures,
     test_full_pipeline.py:1617-1619). The detailed failure-mode breakdown (2026-06-09) shows it
     occurs ONLY on the HOLDING goal: 36 cells (semantic 7, +mbs0.05 9, +mbs0.135 5, +mbs0.18 4,
     uniform 11), and ZERO on stack / find-and-tray-stack. Odd: a toppled tower on stack should also
     be a physics_mismatch, yet stack never produces one.
     HYPOTHESIS: holding's verifier is _verify_holding (is the TARGET actually grasped?), and the
     target is the just-revealed HIDDEN object behind occluders, so its grasp is less reliable than
     the clean visible-cube grasps in stack/fats; meanwhile stack uses _verify_cube_on, which may be
     more lenient, or stack failures manifest as timeout/no-plan BEFORE the verify step. INVESTIGATE:
     which verifier fires; whether the target grasp slips during the post-pick lift; whether stack
     toppling is silently uncaught (a real false-success hole).
     REPRO SEEDS (goal=holding, exit_reason=physics_mismatch; sweep_full_2026-05-28; mostly n_occ=2):
       0,4,15,19,20,34,36,39,43,44,45,48,59,63,75,87,92  (17 distinct seeds, 36 cells).
       Quick starters (semantic, n_occ=2): seed 0 (4 plans, 20.2 s); seed 34 (auto mbs, 1 plan,
       6.0 s). Uniform outlier: seed 43 (n_occ=2, 1351 s wall-clock). Re-run one with the GUI to
       watch the target grasp fail.
     DATA: eval_results/sweep_full_2026-05-28 (aggregated.csv filter goal=holding &
     exit_reason=physics_mismatch; cells/ per-cell logs). HANDOFF: author will take this in a NEW
     session via a handoff prompt AFTER the thesis-audit issues (with #121); do NOT solve now.
     AUTHOR NOTES 2026-06-10 (capture in full):
       (1) CONTRADICTION to resolve. The manipulation model is described as a rigid constraint weld
           ("cannot slip or be dropped in transit", §6.5 / methods §4.5) -- yet HOLDING fails via
           physics_mismatch. So the failure is NOT a transit drop: either the grasp never properly
           took (the constraint attaches but _verify_holding finds the target not actually held), or
           the "no slip/drop" claim is too strong. Pin down which. (Thesis §4.5 wording softened
           2026-06-10 to "attached ... while it is carried" + "target not actually grasped", but the
           ROOT mechanism is still open.)
       (2) COUNTERINTUITIVE -- and this is the heart of it. The physics failure mode appears ONLY in
           holding, NEVER in stack or find-and-tray-stack. We would EXPECT it in STACK (toppling
           towers), not holding. Strong smell that stack toppling is being SILENTLY UNCAUGHT -- a real
           false-success HOLE: _verify_cube_on too lenient, or the check runs at a moment the tower is
           still standing, or stack failures exit via timeout/no-plan BEFORE the physics check runs.
       (3) So #122 is really TWO questions: "why does holding produce false successes?" AND "why does
           stack NOT?" The ABSENCE of stack false-successes is itself suspicious. CONFIRM the verifier
           actually fires by deliberately toppling a completed stack and checking it is flagged.
       (4) Likely shares a root with #121 (uniform knocks down stacks -> no-plan): a mid-execution
           knock-down on stack breaks the plan -> planner_failed (never reaches the physics check),
           whereas a fake grasp on holding survives to the check -> physics_mismatch. Same manipulation
           fragility, two different exit reasons. Both = likely implementation errors in grasp/stack.

================================================================================
SUMMARY — STATUS (updated 2026-05-24)
================================================================================
Chronological numbers, NOT priority — follow TIER. Closed bodies in git history
(git log --grep="audit: mark #N") + archive/CODEBASE_AUDIT_DEFERRED.txt.

OPEN — 12 items (bodies above):
  #120 T0 — find_dice bridge execution-unfaithful: add real streams + motion planning +
       collision checking + physics/dynamics (the heart of a fair TAMPURA comparison).
  #104 T3 — refresh thesis-PDF plots + numbers to post-#100-merge outputs (thesis #121/#168).
  #105 T2 — Wumpus-World baseline (cell-belief search, no shadow geometry) vs uniform/TAMPURA.
  --- batch 2 (2026-05-24 read-through spillover) ---
  #106 T2 — failure-mode taxonomy: fold all_searched+physics_mismatch into planner_failed;
       investigate no_summary (thesis #201).
  #107 T1 — remove per-episode replan limit (run to time budget; keep 3-retry give-ups);
       will change reported numbers.
  #108 T2 — boxel-count-vs-config plot + semantic+mbs0.1 arm (floor barely changes count;
       thesis #202/#203).
  #110 T3 — live-demo natural-language CLI.
  #111 T3 — remove "THESIS_NOTES" text from the TAMPURA wall-clock plot (thesis #177/#190).
  #112 T3 — (optional) Wilson interval for success-rate plots (thesis #207).
  #113 T1 — FULL canonical sweep re-run: reproduce all data + add mbs0.01 (1 cm) arm; ensure
       complete coverage (no no_summary holes, mbs0.10 included, uniform per-goal arms); regen figs.
  --- batch 3 (2026-06-09 thesis review) ---
  #121 T2 — stack-uniform fails via "no plan found" not timeout (fewer timeouts than semantic);
       investigate failed-cell plan times + whether it's unsolvability vs grounding blow-up.
       HANDOFF (new session, after the thesis-audit issues).
  #122 T2 — "false success" (physics_mismatch) is holding-only (36 cells, 0 on stack/fats);
       investigate why _verify_holding catches fake grasps but stack toppling is never flagged.
       Repro seeds logged. HANDOFF (new session, with #121, after the thesis-audit issues).
  TIER tally: one open T0 (#120); two open T1 (#107, #113).

CLOSED (one-line; full detail in git / archive):
  #117 [DONE 2026-06-04] fig:boxelization camera at its true elevated viewpoint + accurate camera-ray lines of sight; regen PNG.
  #115 [DONE 2026-06-04] camera glyph -> tripod-mounted camera in fig:boxelization; regen PNG (thesis #283; 6bcc034).
  #114 [DONE 2026-06-04] schematic occlusion shadows end at the table-hit, not workspace bounds; regen PNG (thesis #274).
  #116 [KILLED 2026-06-04] stack-degradation experiment dropped; cause taken from supervisor observation
       (arm collides with / can't reach the tall stack, model-free control limit), written into thesis #281.
  #101 [2026-05-25] resolution-axis writeup (thesis aa6ebc6): "Free-space resolution" subsection
       in results.tex + boxel-count-vs-leaf-size figure (#108 part-c, 790cd1e); also thesis #209.
  #98  [2026-05-25] coarse-end resolution sweep (1800 cells, 66a26ed): resolution axis FLAT
       above auto_cell (insensitive across ~4x); auto_cell a robust plateau. Step4->#100, Step5->#101.
  #100 [2026-05-24] merge coarse-end sweep into sweep_anytime + combined 4-point resolution
       plot (#98 Step 4): 1800 SVR cells (mbs 1.5x/2x) merged -> aggregated.csv 4500 rows;
       combined plots in resolution/ subdir (kept separate from #77 figures); green(1.5x)/
       red(2x) variant colours; matrix_resolution.json provenance; EVAL_SUMMARY §2.5+§4.7.
       Finding: resolution axis flat above auto_cell.
  #103 [2026-05-24] mid-air occluders no longer cast shadows (shadow_calculator.py:
       calculate_shadow_boxel skips any object whose footprint sits above the support
       surface). Repro: random-pairs seed=977165494. (Stale shadows of a picked-up occluder
       = separate gap, THESIS_NOTES §8 / audit #71.)
  #102 [2026-05-24] #72 two-slab shadow carve made conditional on intersection
       (shadow_calculator.py): one shadow AABB/occluder by default; #72 near/far carve only
       when the overhang overlaps another shadow/object.
  #77  [2026-05-20] anytime compactness sweep (Step 0-4 done; Step 5 writeup -> thesis #121).
       Full 2700-cell sweep; headline plots regenerated; EVAL_SUMMARY.md.
  #93  [2026-05-20] sweep_anytime full-sweep findings published to EVAL_SUMMARY.md + summary_table.md.
  #95  [2026-05-20] stack-scene cell-tag misnaming: _row_difficulty(r) helper (stack_height
       for stack rows, n_occluders else) routed through summary/group/breakdown plotters;
       per-goal x_label. Surfaces the "stack difficulty grows w/ height" finding (97/74/13%
       at h=2/3/4 semantic).
  #99  [2026-05-20] variant-key plotter: every grouper keys by per-row variant tag (semantic
       / +mbs0.05 / uniform), no longer averaging the two semantic resolution arms.
  #96  [2026-05-20] numpy interpreter pin (main() probe + abs-path sys.executable); 5 cells
       re-ran; Care(3) clean (scripts/_audit96_scan_no_summary.py).
  #94  [2026-05-20] plot_solved_vs_time + TAMPURA annotation + EXIT_REASON_COLOUR.
  #63  [2026-05-19] README + wiki final doc sweep.
  #86  [2026-05-16] eval_plotter sample-std (N-1) fix for error bands.
  #85  [2026-05-15] place-time pad-vs-cube wedge; 15 mm pre-release lift.
  #84  [2026-05-15] cube-on-cube stack mis-landing root-cause diagnostic.
  #83  [2026-05-15] refresh_object_aabbs on dispatcher state-clear.
  #82  [2026-05-15] stuck-on-release diagnostic (per-link + tilt + anomaly).
  #81  [2026-05-15] resetJointState release teleport; gentle close.
  #80  [2026-05-15] 4-signal drop-verify gate; tray-aware support_z.
  #79  [2026-05-15] clear stale grasp_constraint_id on drop-verify failure.
  #76  [2026-05-14] planner no-plan post-failure; register discovered tgt.
  #75  [2026-05-14] execute_place/stack post-release verification gap.
  #73  [2026-05-14] eval-chapter figures (10 plots; ~3x lead over TAMPURA).
  #72  [2026-05-12] shadow AABB lateral-overhang two-slab carve.
  #71  [2026-05-12] refresh OBJECT boxel AABBs on every sense.
  #70  [2026-05-12] random-pairs unreachable-placement reach-constraint fix.
  #69  [2026-05-12] rayTestBatch numThreads=0 on all 6 call sites.
  #68  [2026-05-10] scene randomizer.  #67 [2026-05-10] Phase-4 hidden swap.
  #62  [2026-05-12] replan-while-holding quick-fix.
  #10  [2026-05-08] uniform-voxelization baseline.
  #9   [2026-05-08] eval runner + scalability matrix + plots.
  Older (#1-#60 era): bodies removed in the 2026-05-12 slim — see git log.

DEFERRED (bodies in archive/CODEBASE_AUDIT_DEFERRED.txt):
  #2, #14, #42, #47, #51 (earlier);  #50, #57, #59, #61, #65 (2026-05-12);
  #78, #97 (2026-05-20).
WONTFIX / merged / removed:  #6, #11, #12, #13, #30, #109.
RELOCATED:  #66 -> TAMPURA_PLAN.md (Plan A done on main; Plan C pending on
  branch audit-64-tampura-binary-grid).
