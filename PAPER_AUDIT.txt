================================================================================
PAPER AUDIT — ICAPS 2027 SUBMISSION (OPEN WORKSTREAMS)
================================================================================
Date: 2026-08-15.  Successor to CODEBASE_AUDIT.txt and thesis_audit.md, both
archived 2026-08-15 (archive/CODEBASE_AUDIT_FINAL.txt, archive/THESIS_AUDIT_FINAL.md;
older material in archive/CODEBASE_AUDIT_DEFERRED.txt, archive/THESIS_AUDIT_ARCHIVE.md).

CONTEXT.  The thesis ("Semantic Space Abstractions for Partially Observable
Deterministic Task and Motion Planning", 76 pp, submitted 2026-06-10) was graded
1.3.  Supervisors (Till Hofmann, Daniel Swoboda) did not run the code; they judged
the writing good but noted the work is honest about missing pieces.  Plan: turn it
into an ICAPS 2027 paper (conference June-July 2027, USA; SUBMISSION DEADLINE
DECEMBER 2026).  Division of labour: the supervisors write the paper text, the
author (Hani) does the code.  The missing pieces below come from the supervisor
debrief (2026-08), roughly in execution order.

NUMBERING.  Workstreams are #P1-#P10.  Old issues are cited as CB#N (archived
CODEBASE_AUDIT_FINAL.txt) and TH#N (archived THESIS_AUDIT_FINAL.md).  Work through
this file with the /workflow skill: one issue per turn, before/after preview,
explicit approval, individual commits.  STATUS: no marker = OPEN; [DONE]/[REJECTED]
on the header line when resolved.  [REMARK] items are recorded decisions, not tasks.

================================================================================
POLICY (carried over from CODEBASE_AUDIT.txt, still binding)
================================================================================

SHELL   Windows PowerShell only.  No bash heredocs for commits; plain
        `git commit -m "msg"`.  One-line commit subjects, no body.

REMOTES Push meaningful work to BOTH hosts: `git push github <branch>` (github.com
        hanihabobo-dot/pybullet) and `git push origin <branch>`
        (git.rwth-aachen.de hani.alassiri.alhabboub/pybullet).

CODE    Commented-out code is intentional documentation/toggles: never delete
        without explicit approval, move to archive/ instead.  Dead methods/imports
        are kept on purpose (some are needed for exactly this paper's perception
        work).  Pure-narration comments stay.

WIKI    Project wiki (pybullet.wiki.git, clone at ../pybullet.wiki/) must be
        updated after any change to architecture, data structures, algorithms,
        PDDL domain, execution pipeline, or design decisions.

ARCHIVE archive/ is tracked and pushed; no personal information in it.
        "emails and pdfs/" is private: never git add it; stage paths explicitly.

ENV     wsl_env/ is the WSL venv (gitignored): do not delete.  PDDLStream is a
        sibling checkout ../pddlstream_lib.  Launch wrappers (run_boxels,
        run_eval, plot_eval) live in the user's PowerShell $PROFILE, not the repo.
        Always give the user the --vis 1 GUI command alongside any headless run.

PLANNER PDDLStream plans against a separate DIRECT PyBullet client
        (boxel_env.plan_client_id); the planner is stateless, (boxel_fits ?o ?b)
        init facts are rebuilt from the BoxelRegistry every replan.

PLOTS   New plots add, never replace; old plots must stay regenerable; read new
        PNGs visually before committing.

GIT     Multi-commit refactors go on a branch off main; single-commit fixes on
        main.  No worktrees.  Pre-staged files in the index ride along with the
        next commit; do not unbundle.

================================================================================
PROJECT PRIMER (distilled from the archived audits; read this first)
================================================================================

WHAT THE SYSTEM IS.  A tabletop TAMP pipeline (PDDLStream + Fast Downward +
PyBullet, Franka Panda) for partially observable deterministic (POD) planning.
The contribution is the representation: an adaptive semantic partition of the
workspace into "Boxels" (object AABBs, occlusion-shadow volumes cast from a fixed
camera, octree-partitioned free space with face-merge), which makes occlusion
explicit symbolic state (Know-If fluents, blocks_view_at) so a classical planner
can search for hidden objects via optimistic determinization + replanning.
Baseline comparison: uniform voxel grid (~10x more cells/facts, worse success);
architectural comparison: TAMPURA (Curtis et al. 2024, arXiv:2403.10454).

KEY FILES (root):  boxel_env.py (env, scenes, camera, oracle, boxelization),
shadow_calculator.py, free_space.py, cell_merger.py, uniform_grid.py (baseline),
pddlstream_planner.py (+ pddl/), streams.py (grasp/kin/motion streams),
execution.py (pick/place/stack/sense execution), robot_utils.py, belief.py,
reboxelize.py, test_full_pipeline.py (main loop + eval verifiers), eval_runner.py,
eval_plotter.py, run_logger.py.  tampura_bridge/ runs OUR pipeline inside
TAMPURA's find_dice env (see #P4).  A src/ package refactor is planned but gated
(multi-commit branch, do only when the tree is otherwise quiet).

EVAL DATA.  Canonical headline dataset: eval_results/sweep_full_2026-05-28
(post CB#107 replan-cap removal; 810 cells = 3 goals x 3 variants x 30 seeds x 3
difficulty; 1800 s budget).  Thesis headline numbers (tab:headline): success
find-and-tray-stack 75.6/72.2/34.4 %, holding 65.6/66.7/47.8 %, stack
64.4/64.4/35.6 % (semantic / +mbs0.05 / uniform); stack-by-height semantic
100/80/13.3 % at h=2/3/4 (uniform 90/16.7/0).  NOTE: EVAL_SUMMARY.md documents
the OLDER sweep_anytime dataset (4500-row aggregated.csv, pre-cap-removal
numbers 42.3 % holding etc.) and is stale relative to the thesis; do not quote
it for the paper without saying which sweep it is.  A fresh full canonical sweep
(CB#113) is planned and should only run AFTER #P1-#P3 land, since those change
the system under test.

TAMPURA COMPARISON STATE.  TAMPURA source lives in WSL:
\\wsl.localhost\Ubuntu\root\tampura-work  (tampura/ core lib + tampura_environments/
incl. find_dice/ and panda_utils/; their .venv is python 3.11 / pybullet 3.2.7).
Local 20-seed TAMPURA find_dice re-run (runs/sweep_2026-05-26.json in WSL):
11/20 = 55 % success, 166 +/- 85 s per successful episode (published-implied
>= 63 %; published per-step 57 +/- 38 s, ours ~11 s/step).  The thesis frames this
comparison as INDIRECT and architectural (different task, codebase; local re-run
possibly disadvantages TAMPURA, TH#306).  Their hiding mechanic is CONTAINMENT
(die pose-aliased under a cup) vs our LATERAL shadows; their spawns are
hand-tuned reachable (x in [0.3,0.7], y in [-0.4,0.4], camera-visibility checked,
scene regenerated until the die is genuinely hidden); their controllers are
kinematic (motion via bundled BiRRT; grasp library: saved / antipodal / top).

KNOWN FAILURE TAXONOMY (thesis §5): timed out 24 %, no plan found (dominant on
stack-uniform), search exhausted (holding-only; 13/18 from axis-aligned shadow
under-coverage, TH#335), false success (physics check catches toppled tower /
fake grasp; holding-only, which is itself suspicious, CB#122).

BRANCHES THAT MATTER:  audit-14-dense-lattice-attempt (64-ray visibility lattice,
works, ~3x planning cost), audit-51-shadow-blocker-fix (principled blocker
re-grounding, reverted), audit-55-pose-aware-stack (destination-aware stack
kinematics, works, ~7x planning cost), audit-64-tampura-binary-grid.

NOTES FOLDER.  notes/*.md (THESIS_NOTES.md, TAMPURA_PLAN.md, ...) were removed
from the working tree in 2026-08 cleanup; recover via git history if a reference
below points there.

================================================================================
WORKSTREAMS
================================================================================

================================================================================
#P1. [OPEN T0]  REMOVE THE CONSTRAINT-WELD GRASP AND ALL ORACLE READS; BUILD A
     REAL PERCEPTION SYSTEM AND A REAL POSE SAMPLER
================================================================================
Supervisor wording: no workarounds and cuts any more.  The ONLY thing that may
stay is the hardcoded lift after a pick or place action (collision-avoidance aid).

WHAT MUST GO (full inventory, from the 2026-08-15 code scout):

  Grasp / release cheats:
  (a) execute_pick weld: p.createConstraint(JOINT_FIXED) attaches the object to
      the EE after gripper close (execution.py:775-780; doc at :655 calls it an
      accepted simplification).  The archived audits are explicit that this is
      "a telekinetic attachment that bypasses contact physics" (deferred #59) and
      that close_gripper is "purely cosmetic", the constraint provides ALL grip
      stability (deferred #81, commit 8774e6f).
  (b) release teleport: open_gripper uses p.resetJointState straight to max
      finger opening because POSITION_CONTROL could not physically free objects
      (deferred #81, commit 65146ad).  A real release must work through the motor.
  (c) bridge mirror of the weld: tampura_bridge/execution_boxel.py:94-107 (_weld),
      plus _held_offset ground-truth read (:82-91).  Whatever replaces (a) must be
      mirrored there (see #P4).
  (d) legacy tampura_bridge/execution_bridge.py: full pose-teleport pick/place
      (resetBasePositionAndOrientation at :34, :152).  Dead code (boxel_policy
      imports execution_boxel, not this).  Move to archive/ per policy.
      [(c)+(d) OBE 2026-08-20: the whole tampura_bridge/ tree was discarded to
      archive/tampura_bridge_v1/ — see the #P4 decision note.  No bridge mirror
      exists to maintain until the bridge restart.]

  Oracle / ground-truth reads feeding planning or perception:
  (e) boxel_env.oracle_detect_objects (boxel_env.py:1620-1682): reads exact poses
      via p.getBasePositionAndOrientation (:1650) and decides visibility by
      8-AABB-corner rayTestBatch matched against true body ids (:1659-1675).
      Called unconditionally from get_camera_observation (:1542-1589, both GUI
      and DIRECT paths); its output feeds the BoxelRegistry that planner and
      executor consume (test_full_pipeline.py:586-599).  THE central target.
  (f) RGB-D is already rendered but discarded: getCameraImage at :1560-1572;
      _depth_to_point_cloud exists (:1597-1618) but is commented out at
      :1573-1578 ("Re-enable if a real perception pipeline replaces the oracle").
  (g) sense_shadow_raycasting (execution.py:63-162): the PDDL sense action casts
      147 rays and classifies hits by comparing hit_obj_id against the KNOWN
      target/occluder body ids (:142-149).  compute_shadow_blockers (:165) same
      pattern.  A real sense must classify from rendered observation, not ids.
  (h) boxelization from privileged geometry: generate_boxels reads p.getAABB
      (boxel_env.py:1361); shadow_calculator.py raycasts true collision geometry
      (:127); free_space.py partitions from those AABBs.  Once perception
      supplies estimated poses/extents, these consume estimates instead.
  (i) planning-time ground truth: streams.py reads live p.getAABB at :384, :456,
      :1169, :1178-1193 (compute_stack_kin reads the support's true top surface).
      Execution-time ground truth: execute_place/execute_stack read live AABBs
      and poses to compute drop heights (execution.py:850-865, :1023-1037).
      Precedent from deferred #57: URDF-known SIZE is legitimate design-time
      knowledge (put it in an object_geometry dict on the registry); sensed
      POSITION is not.
  (j) sync_to_plan_client (boxel_env.py:669-702) mirrors exact GUI-world poses
      into the planning client every replan; the planner literally plans against
      ground truth.  Post-place/stack snap syncs of the plan-client shadow copy
      (execution.py:950-956, :1144-1150).  With perception, the plan client must
      be built from the estimated state.  (The plan-client MECHANISM stays; what
      changes is the source of poses.)
  (k) bridge perception adapter: segment_visible (perception_adapter.py:104-119)
      is already real segmentation-based VISIBILITY, but poses/extents still come
      from pbu.get_pose/get_aabb ground truth (:121-146, :170-175) and
      oracle_detect_objects is reused verbatim (:177-188); boxelize.py:43 reads
      true AABBs.

  Pose sampler:
  (l) sample_grasp yields exactly one grasp: fixed top-down orientation at a
      single hardcoded z-offset (_GRASP_Z_OFFSETS=[0.10], streams.py:532-577).
      No shape awareness, no side/angled grasps, no width reasoning.  "A good
      pose sampler" replaces this (TAMPURA's get_grasp library, saved/antipodal/
      top modes, is a working reference implementation; see #P5).

  ALLOWED TO STAY:  _apply_post_action_lift (execution.py:615-635; ~5 cm after
  pick :792, ~10 cm after place :941 and stack :1136).  Explicitly exempted.

  SCOPE DECISIONS RECORDED (2026-08-15):
  - The separate pre-release 15 mm lift in execute_place (execution.py:890-911,
    audit #85 wedge fix) is NOT obviously covered by the exemption ("lift AFTER a
    pick or place").  Expectation: a real friction grasp removes the tilt wedge
    that motivated it; try removing it with the weld, keep only if physically
    justified, and disclose either way.
  - Eval-harness ground-truth VERIFIERS stay: _verify_cube_on / _verify_holding /
    _verify_goal_physics and the Phase-4 "ORACLE ONLY" hidden-target setup block
    (test_full_pipeline.py:144, :221, :277, :634-696).  Scoring/logging ground
    truth in simulation is legitimate; the robot must not read it.

  KNOWN BLOCKER FOR A REAL GRASP (deferred #77) — ERRATUM 2026-08-20: the
  deferred-#77 size inventory (0.15 m occluders / 0.08 m targets) is STALE;
  fix #17 had already halved the preset scenes before the thesis eval.
  Code-verified today: default_scene occluders 0.075 m cubes / targets 0.04 m;
  stack_scene 0.05 m; mixed_shapes all < 0.08 m; ObjectSpec.lateral_friction
  already defaults to 1.0.  The remaining ungraspable geometry is
  scalability_scene (and random_pairs_scene on top of it — the eval sweep's
  scenes): occluder half-extents draw uniform(0.030, 0.045) -> 0.06-0.09 m
  full width, the upper half of the range over the 0.08 m finger opening;
  default_scene's 0.075 m occluders leave only 2.5 mm descent clearance per
  side, too tight for a friction grasp under position-control error.  The #77
  proposal still applies, narrowed: occluders become tall narrow blocks
  (~0.05-0.06 m wide, 0.10-0.15 m tall — occlusion improves with height),
  targets shrink slightly so the hidden-placement jitter window (occ_half -
  target_half) stays positive.  [LANDED 2026-08-20, branch
  p1-real-grasp-perception: scalability xy_half uniform(0.025,0.030) /
  z_half uniform(0.050,0.075), targets uniform(0.015,0.020); default_scene
  occluders 0.06 m wide x 0.075 m tall; finger-pad lateralFriction 1.2.]
  This changes every seeded scene and therefore every number: plan the
  canonical re-sweep (CB#113) AFTER this lands.

  STARTING POINTS.  Perception: segment_visible (was the bridge's; now at
  archive/tampura_bridge_v1/perception_adapter.py, reference only) for
  visibility; the
  commented-out depth/point-cloud path (f) for geometry; PyBullet's segmentation
  buffer gives object masks, from which per-object point clouds give estimated
  AABBs/poses (sim-grade "real perception": pixels in, poses out, no body-id
  reads).  Visibility density tradeoff is measured on branch
  audit-14-dense-lattice-attempt (64-ray lattice, ~3x planning cost).  Grasping:
  friction-grasp design sketch in deferred #59; belief-update honesty gaps to
  keep in mind: deferred #51 (stale blocker fallback; principled fix on branch
  audit-51-shadow-blocker-fix, reverted), #47 (blocks_view_at re-grounding,
  deferred out of thesis scope: reconsider here).

  STEP-1 FIELD BUGS F1-F4 (2026-08-20, GUI verification after step (1) landed;
  one diagnostic agent per bug; full reports with log-line evidence in
  archive/p1_field_reports_2026-08-20/; all four FIXED 2026-08-20 on this
  branch — see the [DONE] markers and the F4 fix note):
  F1. [DONE 2026-08-20] PHANTOM FIRST PICK (grip_loss.md): the Panda's fingers SPAWN CLOSED
      (panda.urdf finger lower=0; loadURDF zeroes joints; nothing initialises
      them — the defensive open_gripper was removed in audit #37/#38 on the
      false "init = open" assumption).  The first descent grinds the closed
      fingertips into the object top (+6.8 mm arrival stall), grip
      verification passes on link identity alone (both tips pressing DOWN
      count as "both pads in contact"; the 500-800 N readings are servo
      grind, NOT close transients — genuine grasps read 2x40 N), the object
      never moves, and the phantom hold survives to a place/stack that
      releases nothing (table-height places even pass drop-verify with
      err=0).  First pick only: any open_gripper leaves fingers at 0.04 for
      the rest of the episode.  FIX: (a) resetJointState fingers to 0.04 in
      _setup_scene after robot load (root cause, one line); (b) re-verify pad
      contact after the post-pick lift, abort to replan on loss; (c) held-
      contact entry assert in execute_place/execute_stack with its OWN
      failure path (the dispatcher's fingers_open classifier would misfile
      it); (d) aperture plausibility in grip verification
      (|finger_pos - cube_hw| <= ~6 mm; phantom reads ~0.00).
  F2. [DONE 2026-08-20] MOVED OCCLUDER NEVER RE-BLOCKS + PICK HAS NO GIVE-UP (pick_giveup.md):
      _build_init emits blocks_view_at hardcoded to the occluder's ORIGINAL
      boxel id (pddlstream_planner.py:439-444) and stops emitting
      obj_at_boxel for moved occluders, so the blocks_view derived predicate
      can never re-fire after a relocation — the planner believes the shadow
      clear, sense hits the real occluder identically each time, and the
      audit-#21 3-strike giveup burns the shadow.  This is the concrete
      #47/#51 mechanism.  Separately, pick failures have NO strike counter: a
      toppled block (unpickable by the single no-yaw grasp) loops
      byte-identical retries until the process is killed.  FIX: key
      blocks_view_at to the CURRENT boxel (moved_occluders.get(id, id), ~one
      line); add a pick strike counter mirroring blocked_counts; optionally
      gate on cube_tilt_deg in _release_and_verify_drop (already computed,
      never checked).  Tier 2 = step-(4) sampler: yaw 0/90 candidates from
      the live AABB, finger-width-validated before yield.
  F3. [DONE 2026-08-20] DEGENERATE/INVERTED SHADOWS for tall occluders near the camera
      (missing_shadow.md): back-corner and dominant-axis selection use the
      full 3D camera->object direction (shadow_calculator.py:81-110,
      :184-194); for a tall close occluder the downward component dominates,
      the shadow AABB collapses to the occluder footprint and the
      object-subtraction INVERTS it (observed shadow_of_red_object extent.z
      = -8.7e-6 m; wireframe drawn 17 um tall, label unaffected — hence
      "label without box" in the GUI).  test_boxel_fits is False on negative
      extents, so a target hidden behind such an occluder is STRUCTURALLY
      UNFINDABLE — a planner gap introduced by the tall-block resize, not
      cosmetics.  FIX: compute shadow direction/back corners in the
      horizontal plane only; add a degenerate-extent guard on the
      single-AABB path (shadow_calculator.py:248-255) mirroring the
      two-slab-carve guard.
  F4. [DONE 2026-08-20] STACK h=4 IK-INFEASIBLE AT THE FK GATE + SHADOWS NEVER RETIRED
      (stale_shadow_drop.md): the level-4 approach (z ~0.60 at 0.61 m
      horizontal, single top-down grasp at +0.10) sits at the Panda's reach
      margin where 100-iteration IK converges no closer than ~18 mm, so the
      #P1 10 mm FK gate rejects all 8 seeds on every replan -> "no plan
      found".  The observed "drop onto the old shadow" was the audit-#58
      release-in-place fallback hovering over the STALE plan-time stack
      config (coincidence; init-fact deltas prove the shadow poisoned
      nothing).  Underlying hygiene gap is real though: shadows are retired
      ONLY by sense actions — no path removes a shadow when its caster is
      picked/stacked (refresh_object_aabbs skips shadows by design;
      reboxelize treats them as permanent), so stack runs accumulate stale
      shadows in registry AND viz.  FIX: add a lower approach offset to
      _GRASP_Z_OFFSETS (e.g. [0.10, 0.06]); IK refinement (re-seed from own
      output / more iterations) before BOTH FK gates; execute_stack retry
      seeded from REST_POSES when the plan-config seed fails; retire the
      stacked/placed cube's shadow boxels after the OBJECT refresh
      (mirror the discovery-cleanup pattern), same in the place handler and
      release_held_object_in_place.
      [FIX NOTE 2026-08-20: retirement landed in all three sites but GATED
      on "no active search" (goal_kind == stack, or target already found).
      Ungated retirement in the place handler would delete the very shadow
      the plan senses right after relocating its occluder — the F2
      mechanism requires unsensed shadows to survive relocation, else a
      hidden target becomes structurally unfindable.  Deviation from the
      blanket proposal above, reason stated here.]
      [POST-FIX h=4 STATUS 2026-08-20 (run_2026-08-20_21-29-11, headless
      seed 0): the FK-gate dead-end is GONE — plan #1 certifies all three
      stack kins and the level-4 release lands at bottom_z err 0.1 mm.
      NEW FINDING for #P2(b): at the level-4 release the opened finger
      pad stays in light contact with the stacked cube (link 10, ~1.9-2.4
      N, penetration -0.12 mm) because the wrist is at its reach margin —
      drop-verify gate (iv) zero-robot-contact fails 3x while gates
      (i)-(iii)+(v) all pass, the retries nudge the cube (tilt 2.3->8.8
      deg), it topples when the arm departs, and the re-pick of the
      toppled cube fails (F2 strike counter fires, honest no-plan exit in
      4.5 s).  This is deferred-#84's "rim interference" hypothesis with
      concrete numbers: the h=4 residual is a PHYSICAL manipulation
      failure at the reach margin, not planning.  Candidate remedy for
      #P2: run the (allowed) post-stack lift before the verify gate, or
      an approach path that clears the tower rim.]
      [REVIEW PASS 2026-08-20: 5-lens adversarial review of the four fix
      commits confirmed and same-day-fixed: (i) aperture gate false-fail
      on non-square footprints pinched across the larger axis (now
      accepts either horizontal half-extent; phantoms still read ~0);
      (ii) pick_giveup exit_reason over-attributed unrelated no-plans
      (now only when a GOAL-CRITICAL object was withdrawn, else
      planner_failed + disclosure line); (iii) tray stacks bypassed the
      new tilt gate via expected_support_z=None (decoupled: enforce_tilt
      flag set by place/stack incl. trays); (iv) transit losses
      (EmptyHandError) had no strike counter — pick succeeded each
      cycle, resetting the pick counter — unbounded loop; now a
      transit_loss_counts 3-strike feeds the same giveup.  F3 review:
      clean.  Also: h=3 seed 0 now fails honestly on this trajectory
      (tower collapse on the level-3 release, knocked orange off green;
      end-of-run physics gate caught the stale on-relation) — the
      thesis-known 80 %-at-h=3 physical fragility surfacing because F1's
      real first pick changes the episode trajectory; #P2 scope, not
      forced here.]
      [POST-FIX SMOKE NOTES 2026-08-21: (1) F3's corrected (larger)
      shadows split into MORE fragments (run_2026-08-21_02-27-51:
      orange_object casts 7), inflating grounding and sampling —
      per-call planning time up to 455 s on find-and-tray-stack seed
      999; watch planning cost in the CB#113 re-sweep.  (2) That
      tray-stack cell now ends in an honest bounded failure: orange
      (occluder) unpickable for the single yaw-less grasp -> 3-strike
      withdrawal -> its shadows unclearable -> no plan (classified
      planner_failed + giveup disclosure; step-(4) sampler is the
      remedy).  (3) holding seed 3 solo: crash-free; the two abrupt
      smoke-chain deaths on 2026-08-20 were the KNOWN pddlstream temp/
      race with concurrently launched GUI runs — serialize always.]
      [SIBLING BATCH-SENSE 2026-08-21 (user-directed): after F3 one
      caster casts several shadow fragments, so a clear_but_empty sense
      removed only ITS fragment and the GUI kept showing the caster's
      other shadows ("shadow still there after sensing" report; the
      per-fragment removal itself was verified intact — is_shadow 6->5
      across the sense in run_2026-08-21_09-09-27).  handle_sense_action
      now re-casts the caster's remaining fragments after a
      clear_but_empty and removes every fragment ALSO observably empty
      (belief marked not_here on a real observation each); blocked /
      non-empty / target-holding fragments stay for their own planned
      sense.  retire_cast_shadows kept unchanged per user decision
      (it is what cleans stack-goal runs, where sense never fires).]
      [2026-08-21 ROOT CAUSE + FINAL USER DECISION (supersedes the
      retirement notes above): the persistent "sense doesn't remove the
      shadow" reports were NOT a removal bug — the sense classifier
      (byte-identical to main) flags still_blocked on ANY occluder or
      robot ray hit, and P1's tall occluders + F3's full-size fragments
      make 1-3 % marginal ray clips routine (runs 09-09-27 / 10-19-02:
      senses stuck at 1 % / 3 % blocked, so the clear_but_empty removal
      path never fired at all).  FIX: up to 5 % blocked rays are now
      tolerated and the sense classifies by the remaining rays (genuine
      blockages measure 12-100 %); disclosed cost: a target hiding
      exactly behind the tolerated rays is missed by that sense.
      retire_cast_shadows REMOVED WHOLESALE per explicit user
      instruction (both hygiene rounds reverted, incl. the belief
      'retired' status; stack-goal runs accumulate their spawn-time
      shadows again — accepted; the code survives in git history,
      commits 27a7fda..f66b3a9).  The sibling batch-sense stays (user
      choice) and now actually fires, since senses reach
      clear_but_empty again.]
      [PICK RELIABILITY + PARTIAL SHRINK 2026-08-21 (5-agent
      investigation synthesis, wf_f736bb62: removal machinery was
      "starved, not broken" — ~55 % pick failures from stale-position
      descents after transit shoves, ~25 % the already-fixed marginal
      veto, rest stale geometry + wall-clock plan divergence; overlay
      and physics exonerated, physics bit-identical GUI vs headless):
      (1) execute_pick re-aims the final descent at the object's LIVE
      pose (the dispatcher position was 10-28 mm stale in the GUI field
      runs — 7 of 9 picks failed single-pad); (2) pre-grasp aperture
      sized to the object (max horizontal half-extent + 8 mm) instead
      of full-open 0.04 m, narrowing the descent sweep; (3) the
      ungraspable giveup is NON-ABSORBING: parole after 4 replans, max
      2 paroles per object, then permanent (run 10-55-47's frozen
      target shadow can no longer happen); (4) USER-DIRECTED
      partial-reveal shrink: a still_blocked sense shrinks the fragment
      to the blocked rays' padded bbox — the clear rays observed the
      rest empty; belief stays 'unknown' on the remainder; siblings
      shrink during batch-sense too; (5) viz hardening:
      draw_boxel_data self-removes already-tracked ids.  Verified
      headless: holding seed 0 first pick 80 N / 0.4 mm + batch-sense
      removal; stack h=2 SUCCESS.  Deferred to #P2/#P3 from the
      synthesis: free-space reachability caching (free_001 dead
      skeleton), placement-blocking 3-slice alignment, blocker-atom
      refresh from live poses, giveup-shadow GUI recoloring.]
      [GUI DEMO RUN 11-49-53 OBSERVATIONS 2026-08-21 (user watched
      live): (a) FIRST FIELD CATCH of a transit slip — red slipped out
      of the friction pinch mid-move, the F1(c) empty-hand entry assert
      caught it and the transit-loss counter logged 1/3; re-pick+place
      recovered.  (b) Sense-while-holding occurred twice (domain allows
      sense with an object in hand; symbolically the held object stops
      blocking): the orange__01 sense fired at 100 % blocked with
      orange AT THE GRIPPER in the camera cone, and the final sibling
      re-cast of orange__01 was 25 % (37/147) blocked by the ROBOT ARM
      parked at the sense config — audit-#79 class with fresh numbers;
      an arm-retract before sensing (the commented-out #79 remedy)
      would reclaim these.  (c) The audit-#58 release-in-place dropped
      green at an unreasoned spot that landed IN shadow_of_orange__00's
      camera corridor, self-re-blocking it until the proper place at
      free_031 — recurring stale_shadow_drop.md pattern: the #58 drop
      has no placement reasoning.  (d) "Two shadows in a row" behind
      orange = the near/far fragment pair (audit-#72 two-slab carve /
      obstacle splitting), correct representation; the final sense
      removed the observed-empty front fragment and shrank the far one
      5.8->4.3 cm (partial reveal).  All items are #P2/#P3/step-(4)
      scope; no new defect.]
      [SENSE ORDERING + NO-DROP 2026-08-21 (user directive): (1) cost
      history checked — the ONLY cost change ever is 6f91d0c (stack=2,
      place-over-stack), intact at HEAD; a sense-vs-place cost never
      existed and could not have ordered the actions anyway: (pick,
      sense, place) and (pick, place, sense) share one action multiset,
      so equal total under ANY cost assignment — ordering requires a
      precondition.  (2) sense now requires (handempty): place-before-
      sense is FORCED by the domain; the doomed sense-the-held-object's-
      own-shadow pattern (100 % self-blocked) is unschedulable.  The
      arm-parked-at-sense-config blockage (#79) remains open — the
      retract-before-sense remedy is still the fix for that.  (3) the
      audit-#58 release-in-place is RETIRED ("never blind-drop", user):
      no-plan-while-holding now plans a minimal DISPOSAL — goal
      (handempty) — i.e. a reasoned place that respects the placement-
      blocking facts and kinematic streams; if that too is unplannable
      the episode ends honestly with exit_reason no_plan_while_holding,
      object still in hand.  release_held_object_in_place kept in
      execution.py per the dead-code policy, no longer called from the
      loop.  Verified (holding seed 0): every mission plan places
      before sensing, parole 1/2 fired then honest re-giveup, shrink
      2.7x27.7 -> 2.0x11.5 cm on a 10 % blockage, zero drops, bounded
      classified end; stack h=2 SUCCESS.]
      [GRIP + TRANSIT ROUND 2026-08-21 afternoon (user field tests):
      close force 40 -> 60 N (transit slips at 40 N); two-stage
      top-down pick descent (horizontal re-aim at approach altitude,
      then strictly vertical lower — the diagonal swipe was edge-
      clipping the block); strict gripper checks on the transit-path
      INTERIOR (the blanket gripper_relax certified finger sweeps
      through tall occluders — the orange side-push; endpoints stay
      relaxed); Z-aware partial shrink (top-blocked fragments now
      shrink in height, full-slice-spacing padding so unsampled volume
      is never claimed observed); one-shot grasp resample inside
      execute_pick after a close-time verification failure (retreat,
      re-read live pose, re-descend once).  User confirms picks work.]
      F5. [OPEN] HIDDEN-SPAWN CLASSIFICATION INCONSISTENT WITH SENSE
      GEOMETRY (2026-08-21, user field report on random-pairs seed 0,
      run_2026-08-21_13-44-12): Phase 4 claims "Target: blue_object —
      ORACLE: Actually hidden in shadow_of_red_object__00" (log :52-53,
      guarantee line :35 "1/1 hidden"), yet run 11-49-53 SENSED red__00
      and batch-observed it EMPTY (rays through that volume never hit
      blue), blue was plainly visible in the GUI throughout, and the
      user observed the one genuinely occluded object was NOT the
      target.  The robot cleared every occluder correctly and the
      target was never findable — the episode is structurally
      unwinnable at spawn.  Suspects: the spawn-time hidden test
      (8-corner camera-ray occlusion + "shadow AABB contains centre",
      test_full_pipeline Phase-4 ORACLE block + random_pairs_scene
      hidden placement/guarantee in boxel_env) disagrees with the
      sense-time 7x7x3 volume grid, and F3's corrected fragment
      geometry changed the shadow AABBs under the spawn logic's
      assumptions (the placement jitter window and "contains centre"
      test predate F3).  Fix direction: one shared visibility/
      containment criterion for spawn classification and sense (the
      #P1 step-(2) perception module is the principled home), or at
      minimum re-run the hidden guarantee against the FINAL F3 shadow
      fragments and re-roll the scene when the target is not genuinely
      inside a sensed-able fragment.  Blocks meaningful holding-goal
      numbers on the affected seeds — fix before CB#113.]

  ORDER OF WORK (proposal): (1) resize objects + friction-grasp + real release,
  drop weld and teleports [changes execution.py, robot_utils.py, boxel_env.py
  scenes; mirror in execution_boxel.py]; (2) perception module: segmentation +
  depth -> per-object pose/AABB estimates behind the existing detection
  interface; wire get_camera_observation to it, delete the oracle path;
  (3) sense action re-implemented over rendered observation; (4) grasp sampler;
  (5) re-baseline everything (CB#113 sweep).  Each step is its own branch per
  policy (multi-commit refactor).

================================================================================
#P2. [OPEN T0]  STACKING-ACCURACY DROP: PROVE IT IS EXECUTION, DETECT PHYSICAL
     FAILURE (KNOCKED TOWERS, OBJECTS OFF THE TABLE) AND FAIL HONESTLY
================================================================================
The paper must address why stack success collapses with height (semantic
100/80/13.3 % at h=2/3/4).  Supervisor position (matches thesis §5, CB#116,
TH#281/#304/#332): the planner finds valid plans at every height; the SIMULATED
ARM rams the tower and knocks everything over: a simulation/manipulation failure,
not a planning failure.  The thesis discloses this but never fixed or fully
instrumented it (TH#332: "not an issue to resolve" for the thesis; it IS one for
the paper).  Also required: when an object is knocked OFF the table, recognise it
and fail the episode instead of retrying forever.  This detection layer is part
of the #P1 perception system.

WHAT THE CODE DOES TODAY (2026-08-15 scout):
  - detect_execution_collisions (robot_utils.py:307-381) runs after every move
    and pick, but BOTH call sites discard the return value
    (test_full_pipeline.py:1213-1218, :1298-1303).  Collisions print and change
    nothing (CB#42 deferred this; the deferral dies here).
  - No code checks mid-episode whether any object left the table or workspace.
    _verify_cube_on/_verify_on_table/_verify_holding run only at goal-check time.
    A cube knocked off the table mid-plan is invisible to the loop; deferred #83
    even states "cube-fell-off-the-workspace recovery is out of scope".  The
    empirical marker exists: the spawn window's x lower bound (-0.251) is where
    cubes used to tip off the table edge (boxel_env.py:823-826).
  - Stack toppling is likely SILENTLY UNCAUGHT: physics_mismatch (false success)
    occurs ONLY on holding, never on stack (36 vs 0 cells), which is backwards:
    a toppled tower should trigger it (CB#122, with author notes; repro seeds
    0,4,15,19,20,34,36,39,43,44,45,48,59,63,75,87,92 on sweep_full_2026-05-28).
  - CB#121: stack-uniform fails via fast "no plan found" (~48 no-plan vs ~7
    timeout) despite the huge grounded domain.  Author hypothesis: mid-execution
    knock-down, replan from scattered state finds no plan.  Same manipulation
    fragility, different exit reason.

DELIVERABLES:
  (a) World-integrity monitor after every executed action: every non-held object
      still within table bounds and (if previously stacked) still on its support;
      violations classify the episode (knocked_off_table / tower_toppled) and
      either trigger an honest replan (object still reachable) or an honest
      FAILURE (object gone), never a blind retry.  Builds on the existing
      false-success physics checks; hooks where the discarded collision returns
      already flow.
  (b) Root-cause the h=4 collapse with the monitor's data: confirm (or refute)
      arm-tower collision as the mechanism (deferred #84's three hypotheses:
      IK drift, grasp tilt, rim interference; #85 tilt compounding).  Fix what is
      fixable (approach paths that clear the tower; grasp tilt goes away with
      #P1's real grasp), disclose the rest with numbers.
  (c) Resolve CB#122 both ways: why holding produces false successes AND why
      stack produces none (deliberately topple a completed stack and check that
      it is flagged).
  (d) Persist the classification (CB#124: blocked_giveup_shadows, spawn-time
      hidden classification, plus the new knocked_off_table/tower_toppled
      reasons) in timing_summary.json so aggregated.csv can attribute failure
      modes without prose-parsing stdout.log (also closes CB#106 part 2's
      no_summary investigation path).
  Related backlog if stacking is re-scoped: stack size-compat check, safe-inset
  XY landing test, trajectory-clipping edge case (branch audit-55-pose-aware-stack),
  all in the archived FOR LATER list.

================================================================================
#P3. [OPEN T1]  ALL OBJECTS MUST SPAWN WITHIN REACHABLE RANGE (TAMPURA-STYLE)
================================================================================
Supervisor doubt is JUSTIFIED: the reachability constraint exists but the eval's
own scene does not use it.  boxel_env defines a 0.65 m reach disk around the base
(-0.4, 0) and a _SAFE_TABLE_ window (x [-0.1,0.70], y [-0.40,0.40]) applied only
when cfg.constrain_to_reach=True (boxel_env.py:808-929).  stack_scene sets it
(:267); random_pairs_scene forces it (:394); but scalability_scene NEVER sets it
(SceneConfig default False, :148), so it samples the wider unconstrained window
(x [-0.20,0.60], y [-0.50,0.50], :830-831), and the eval sweep runs scalability
cells (eval_runner.py:416-421).  The thesis sentence "random reachable positions"
(results.tex:81) is therefore not guaranteed for part of the data.

TAMPURA'S REFERENCE PATTERN (find_dice/env_generator.py): spawn x in [0.3,0.7],
y in [-0.4,0.4] in front of the fixed base; every spawn camera-visibility
checked; whole scene regenerated (up to 20x) until the die is genuinely hidden;
PLACE destinations separately constrained to a shortened 0.75 x 1.22 m reachable
table band (get_shortened_table_dims).

DELIVERABLES: (a) constrain_to_reach=True for every scene used in evals (or
default True + explicit opt-out), keep the _assert_xys_in_reach post-spawn net;
(b) quantify the damage: count sweep cells whose spawns violated the reach disk
(recomputable from logged spawn poses), report whether any headline number moves;
(c) verify PLACEMENT sampling (streams place/stack targets) is also
reach-bounded, mirroring TAMPURA's shortened-table pattern; (d) numbers refresh
folds into the post-#P1 canonical sweep (CB#113).

================================================================================
#P4. [OPEN T0]  RUN OUR PIPELINE INSIDE THE TAMPURA ENV FOR A VALID COMPARISON
================================================================================
The thesis comparison is honest but indirect (TH#306): local TAMPURA re-run on
find_dice vs our pipeline on OUR scenes.  The paper needs ours running in THEIR
env, seed-for-seed (reuse TAMPURA's exact 20 seeds; bridge solve stays
seed-generic).

DECISION 2026-08-20 (user): the existing bridge implementation is DISCARDED —
"remove all the work done in tampura bridge. that work was trash. we should
start over."  The whole tampura_bridge/ tree (genuine bridge, smoke probes,
captures) moved to archive/tampura_bridge_v1/ (also recoverable pre-move from
git history).  The CB#120/CB#123 state notes below describe that ARCHIVED
version — they stay as the requirements list for the restart.  The restart
must build on the post-#P1 weld-free pipeline from day one; #P1's bridge-
mirroring obligations are void until the new bridge exists.

The DISCARDED vehicle was: tampura_bridge/ (ran out-of-process inside
their WSL venv via PYTHONPATH injection; BoxelPolicy drove TAMPURA's own rollout
via no-op actions + belief_bridge writes into their SceneBelief; entry point
run_boxel_on_finddice.py).

STATE OF CB#120 (the fair-comparison blocker list), verified 2026-08-15:
  FIXED in the current GENUINE bridge: (a) real streams (planner_boxel.py +
  streams_boxel.py + pddl/domain_find_dice_streamful.pddl); (b) real motion
  planning (RRT-Connect via _plan_follow); (c) collision checking; (e) visibility
  via segment_visible (render + segmentation), not the AABB oracle.
  PARTIAL: (d) physics: grasp is contact-verified and objects made dynamic, but
  the hold is still the JOINT_FIXED weld, "consistent with the shared pipeline".
  Once #P1 removes the weld in the main pipeline, the bridge MUST follow, or the
  comparison is unfaithful again.
  STILL OPEN: (f) containment regions: find_dice hides the die by CO-LOCATION
  under a cup; our lateral shadows never contain it, so the semantic partition
  does not drive the search there: model cup-footprint containment regions.
  (g) the arm-park before each sense is bridge-internal, not a planned action.
  (h) full overlay redraw each refresh (perf only).

MUST-FIX BEFORE ANY QUOTED NUMBER (CB#123, verified still present verbatim):
boxel_policy.py:372-377 discards execute_go_home's status and unconditionally
marks at_home + success ("no_path" still scores reward 1.0); execute_pick:263
ignores the lift's status ("ok" without lifting).  Thread both statuses; on
go_home no_path record fail_reason go_home_no_path.

THEN: PHASE 4, the gated >= 20-seed seed-for-seed bridge eval on their exact
seeds, honest failures included: this replaces the thesis's indirect comparison
as the paper's headline TAMPURA result.  Also regenerate the wall-clock plot
(CB#111) and fix the stale eval_plotter docstring (CB#125) when touching it.
Architectural decision recorded: the no-op-policy harness is acceptable for the
paper (it drives their env, belief, and reward untouched); switching to native
action-schema execution via their effects_fn/execute_fn is optional extra
faithfulness, decide with supervisors if time allows.

================================================================================
#P5. [OPEN T2]  CONSIDER ADOPTING TAMPURA'S CONTROLLERS
================================================================================
Supervisor suggestion: their controllers may simply work better.  What they are
(panda_utils/panda_env_utils.py, primitives.py): kinematic joint-interpolation
drivers (pick_execute/place_execute) over BiRRT motion plans (bundled
motion_planners package: functionally the same RRT-Connect + shortcut smoothing
we implement in streams/streams_boxel), plus the qualitatively different part:
get_grasp samples from a grasp LIBRARY (saved per-mesh JSON grasps, antipodal
mesh sampling via trimesh, or axis-aligned top grasps) validated by
close-until-collision at planning time.  Ours: single fixed top-down offset, but
with genuine runtime finger-contact verification (more honest, far less
expressive).  DECISION TO MAKE (with #P1's sampler work): adopt their grasp
library as the pose sampler (fits "use their controllers" AND "good pose
sampler"), keep our runtime contact verification, keep one motion planner
(theirs and ours are redundant: pick one).  Their kinematic executors conflict
with #P1's push toward real physics: adopting their grasp SAMPLING while keeping
our physical execution is the coherent combination.  Note their Panda differs:
EE frame panda_grasptarget link 14, fingers [12,13] (ours 11/[9,10]); the bridge
already resolves this by name (tampura_robot.build_robot_model).

================================================================================
#P6. [REMARK]  REAL-ROBOT EXPERIMENT: NOT NEEDED FOR ICAPS, NICE TO HAVE
================================================================================
For a robotics venue it would be required; for ICAPS it is not.  Record only.
The thesis conclusion already sketches the path (learned detector + robot-mounted
active-sensing camera on a physical Panda; planning layer claimed to transfer
unchanged behind the detection interface).  Revisit after #P1-#P4 if time
remains before the deadline; otherwise it stays future work in the paper.

================================================================================
#P7. [OPEN T1]  RELATED WORK: COMPLETE COVERAGE OF THE LAST 2-3 YEARS (2024-2026)
================================================================================
No systematic recent-literature sweep was ever done for the thesis.  Evidence:
TH#230 (supervisor asked to cite the AAAI'25 online/dynamic critical-regions
follow-up: explicitly SKIPPED, still missing); TH#280 (one-sentence patch
acknowledging a dynamic-abstraction critical-regions paper); TH#334 (Related
Work restructure used ONLY pre-existing citations, "none new").  Current
coverage: TAMPURA, IBSP, SS-Replan, Bejjani, critical regions, PDDLStream,
LW1/CLG lineage, occupancy grids/octrees.
DELIVERABLE: a proper 2024-2026 sweep across (a) TAMP under partial
observability / belief-space TAMP, (b) occlusion-aware manipulation search and
mechanical search, (c) abstraction/representation learning for planning
(critical-regions lineage incl. the AAAI'25 paper), (d) POD/contingent planning
and compilations; an annotated delta list (what each does, how we differ) for
the supervisors to write from, since they write the paper text (#P10).
Reevaluate which approaches are close enough to become baselines (feeds #P8).

================================================================================
#P8. [OPEN T1]  COMPARABLE RESULTS: RUN MODERN APPROACHES ON THE SAME ENV
================================================================================
Beyond TAMPURA-vs-ours-in-their-env (#P4), run other recent approaches on the
same environment where feasible.  Candidates come out of #P7; TAMPURA and LW1
are already on the list (LW1 also feeds #P9).  Scope rule carried over from the
archived FOR LATER note (supervisor 2026-05-06): reuse published encodings and
harnesses where they cover the task; do NOT re-encode other people's systems
ourselves; if their encoding does not cover the task, drop the comparison
rather than shoehorn.  Same seeds, same scenes, same metric hooks (eval_runner)
wherever the harness allows.

================================================================================
#P9. [OPEN, STRETCH]  FULL POD PLANNING: LW1 + PDDLSTREAM, 3D WUMPUS
================================================================================
Far stretch, in principle interesting: replace the relaxation
(plan-execute-observe-replan over an optimistic determinization) with a genuine
POD planner: compile PDDLStream's sampled groundings into LW1 (Bonet & Geffner),
gaining branching contingent plans and deductive axioms.  Then a problem that
actually NEEDS it, e.g. a continuous/3D Wumpus-style domain.
WHY IT IS JUSTIFIED (the thesis's own disclosed gap, raised 3x by Till):
TH#238/#239/#248 (simplified POD, lacks deductive axioms, does not scale without
them); TH#307 (formal statement: we solve a single optimistic determinization
and never certify goal-belief containment b_G subset S_G as CLG/LW1 would);
TH#328 (pessimistic sensing would require exactly the LW1 axiom machinery we
lack; text reverted from thesis but the reasoning stands).  The unsound
3-strike bounded give-up (thesis §5 limitation) is the visible symptom.
EXISTING SPEC: CB#105 (Wumpus-World baseline: cell-belief search, no shadow
geometry) is the natural first rung and doubles as an ablation of the semantic
partition.  Treat #P9 as post-deadline research unless #P1-#P4 finish early;
for the December paper it is future work, at most a positioning paragraph.

================================================================================
#P10. [REMARK]  DIVISION OF LABOUR AND FRAMING
================================================================================
Supervisors write the paper text; author does the code and supplies numbers,
figures, and the #P7 annotated bibliography.  OPEN FRAMING QUESTION to settle
with them before writing (carried from TH#334): is the paper "TAMP + space
abstraction" or "a space abstraction, demonstrated on TAMP"?  The thesis intro
committed to the latter.  Affects title, contributions list, and which
experiments are headline vs supporting.  Also settle early: which venue track
(main track vs. robotics track) and page budget, since #P1-#P4 all change
headline numbers and every figure will be regenerated from the post-#P1 sweep.

================================================================================
CARRIED-OVER OPEN ISSUES (from archived CODEBASE_AUDIT.txt)
================================================================================
CB#104 thesis-PDF figure refresh          -> superseded; figures regenerate for
                                             the PAPER from the post-#P1 sweep.
CB#105 Wumpus baseline                    -> #P9 (first rung).
CB#106 failure-taxonomy part 2            -> #P2(d).
CB#107 replan-cap removal re-run          -> folded into CB#113.
CB#108 part-b resolution matrix arm       -> folded into CB#113.
CB#110 NL-CLI live demo                   -> PARKED (demo, not paper-critical).
CB#111 TAMPURA wall-clock plot regen      -> #P4 (after PHASE 4).
CB#112 Wilson intervals                   -> PARKED (optional; reconsider for
                                             paper figures).
CB#113 full canonical sweep re-run        -> OPEN, gates all paper numbers; run
                                             AFTER #P1/#P2/#P3 land (system under
                                             test changes; earlier runs obsolete).
CB#120 bridge execution-faithfulness      -> #P4 (core).
CB#121 stack-uniform no-plan anomaly      -> #P2(b).
CB#122 holding-only false-success anomaly -> #P2(c).
CB#123 bridge forced-success path         -> #P4 (must-fix, first).
CB#124 persist failure classifications    -> #P2(d).
CB#125 eval_plotter stale docstring       -> #P4 (trivial, with #P4 plot work).

================================================================================
SUMMARY — STATUS (2026-08-15)
================================================================================
OPEN:  #P1 (T0, first), #P2 (T0), #P3 (T1), #P4 (T0), #P5 (T2, decision),
       #P7 (T1), #P8 (T1), #P9 (stretch).  CB#113 gates all paper numbers and
       runs after #P1-#P3.
REMARKS: #P6 (real robot: not needed), #P10 (supervisors write; framing question
       open).
ORDER: P1 -> P2 -> P3 -> (CB#113 sweep) -> P4 -> P5 -> P7/P8 in parallel with
       supervisor writing; P9 only if time.  Deadline: DECEMBER 2026.
